# CLAUDE.md — Zielinski & Rozen Art Galleries (SPINE)

> **חובה לקרוא לפני כל משימה**, ולעדכן בסיום.
> זה ה-spine: מצב הפרויקט, חוזי-נתונים, workflow, וכללי-זהב. **כל היתר ב-`docs/`** — תפתח לפי טבלת ה-Required Reading למטה.

---

## 0. TL;DR לסוכנים חדשים

1. אתה בונה **דף אחד מתוך אתר**. לא פרויקט בועתי.
2. **🔗 קישורי Figma:** ב-[`FIGMA_LINKS.md`](./FIGMA_LINKS.md). אם הדף חסר — הוסף לפני שאתה מתחיל.
3. פונטים ב-`./פונטים/` — תמיד `@font-face`. **אסור Google Fonts**.
4. נתונים מ-`./data/*.json`. שדה חסר → תוסיף ל-JSON קודם.
5. תמונות ב-`./images/<category>/<slug>/`. **לפני הורדה — `ls` קודם.**
6. **כל דף = שני לינקים מהמשתמש (desktop + mobile)**. **אל תזהה לפי שם node** — הוא משקר. תזהה לפי `absoluteBoundingBox.width`: 1440=desktop, 390=mobile.
7. **מובייל ≠ דסקטופ מצומצם.** sections ייחודיים, סדר שונה, overlay אחר. תבדוק שני frames לפני CSS.
8. **אינטראקטיביות נסתרת.** Figma סטטי; באתר יש גלילה אופקית, lightbox, hover scale. ראה `docs/lessons.md §1` — Static-vs-Dynamic. **שאל אם לא ברור.**
9. **כל אנגלית באתר = Copperplate UPPERCASE.** הכלל ב-CSS גלובלי. פרטים: `docs/conventions.md §5`.
10. **עברית+אנגלית באותה שורה/מחרוזת:** FbEzmel לעברית, `<span class="lat">` + Copperplate ל-Latin — **גודל ויזואלי** (em optical), לא אותו `font-size` px. פרטים: `docs/conventions.md §5.2`.
11. **כל אזכור אומן = קישור** (`docs/artist-linking.md`). הפרה = הדף לא מוכן.
12. בסוף — עדכן sitemap (§3), `FIGMA_LINKS.md` אם גילית URL חדש, JSON contracts (§5) אם נוסף שדה, ולקח חדש ב-`docs/lessons.md §4`.

---

## 1. Required Reading Map — תפתח לפי מה שאתה עושה

> **כלל ברזל:** לפני שאתה כותב CSS/HTML או נוגע בתוכן — תוודא שקראת את ה-docs הרלוונטיים. אל תנחש מהזיכרון. הקבצים האלה הם המקור הסמכותי.

| אם אתה עושה... | קרא חובה |
|---|---|
| כותב CSS חדש, נוגע ב-typography, RTL, casing, `text-transform`, responsive, mixed Hebrew/English, או tokens | [`docs/conventions.md`](./docs/conventions.md) |
| מוסיף/נוגע ב-lightbox, gallery, carousel, slideshow, או רואה ב-Figma תמונות עם dots | [`docs/components.md`](./docs/components.md) |
| מוסיף תוכן/קוד שמזכיר אומן (שם, תמונה, פסקת body, caption, alt) | [`docs/artist-linking.md`](./docs/artist-linking.md) — **חובה לפני סיום משימה** |
| בונה דף חדש או נתקלת ב-Figma quirk / Static-vs-Dynamic decision | [`docs/lessons.md`](./docs/lessons.md) |
| בודק אם משהו כבר נסגר או נתקלת ב-placeholder | [`docs/todo.md`](./docs/todo.md) |
| מתחיל לבנות דף — צריך URLs של Figma | [`FIGMA_LINKS.md`](./FIGMA_LINKS.md) |

---

## 2. מה אנחנו בונים

**אתר Zielinski & Rozen** — רשת 3 גלריות אומנות בתל אביב, מותג בישום שגם הוא אומנותי. ארז זילינסקי־רוזן (founder) הוא אמן בעצמו.

תוכן: **תערוכות**, **אומנים** (22), **גלריות פיזיות** (3), **מאמרי עיתונות**, **אירועים**, **קולות קוראים**, **works**.

עברית = body content; אנגלית = כותרות, ניווט, branding.

Stack: **HTML + CSS** (single-file per page), נתונים ב-`data/*.json`. JSONs מחקים schema של CMS עתידי.

---

## 3. Figma files

> **📌 קישורים ישירים לכל דף ב-[`FIGMA_LINKS.md`](./FIGMA_LINKS.md).** אל תחפש URL כאן.

שני קבצים. **תזהה תמיד מה-URL** (`figma.com/design/<FILEKEY>/...`), לא לפי node-id (שני הקבצים ממוספרים מאפס).

| Key | שם | תוכן |
|---|---|---|
| `XhGH289YTRcW811wrufRJz` | **landing** | homepage, exhibitions, artist, press, events (ktuba), open-call section |
| `Zn3N3mBQkbYER7tTJMbCcz` | **graphics** | about, works, artist pages, events (loneliness), opencalls, lightbox states |

**MCP:** `mcp__figma__get_figma_data`, `mcp__figma__download_figma_images`. `fileKey` הוא parameter — אסור hardcode.

**node-id ב-URL = `119-156`. ב-API = `119:156`.** (dash ↔ colon)

**figma_node_id מקבל משמעות רק יחד עם fileKey** — frames יכולים לעבור בין קבצים. אמת מה-URL של המשתמש תמיד.

---

## 4. SITEMAP

| Route | שם | Figma desktop | Figma mobile | סטטוס |
|---|---|---|---|---|
| `/` | Homepage | `XhGH...::144:317` (canonical; older `Zn3N...::1213:3093`) | `XhGH...::144:2` (canonical; older `Zn3N...::1213:3360`) | ✅ `index.html` — **🔴 2026-08-05: רה-דיזיין אוגוסט הושלם (כל 12 המשימות — ראה `docs/redesign-2026-08.md`). המקור הסמכותי = פריים מובייל `1318:363` "homepage-mobileNew"; דסקטופ = אדפטציה. סדר הסקשנים הנוכחי:** hero → announcement → mobile-cta/opencall → `#exhibitions-now` → `#perfume-promo` (how-many) → `#perfume-savlanut` → `#events-upcoming` (פוסטרים+view all) → `#big-news` (טקסט בלבד) → `#artworks` (שחמט+curator) → `#x-our-artists` (קרוסלת פוקוס) → `#galleries` → `#soos` → `#tribe-teaser` → `#exhibitions` (ארכיון) → `.social` (שתי שורות Bold) → `#press` (9 כרטיסים+כפתור) → footer. nav "press & events" → `/press/`. תיאורי הסקשנים הישנים למטה = היסטוריה; העדכני גובר. — exhibitions-now: how-many (current) + **the-peeler/הקולפן** (current, →`exhibitions/the-peeler/`, card `XhGH...::1124:835` desktop / `1124:95` mobile). **2026-07-26 — תוקן:** התמונה במובייל הראתה בטעות קולפן-מטבח אדום; עכשיו **שני ה-breakpoints משתמשים ב-`images/exhibitions/peeler-desktop.{webp,avif}`** (יצירת הקולפן) — הקרופ ב-Figma דסקטופ (`1124:835`) ומובייל (`1124:95`) **זהה** (imageTransform זהה → 807×902 מתוך המקור 1200×1600). קבצי `peeler-mobile.*` (התמונה השגויה) נמחקו. בנוסף שני כרטיסי ה-`.ex-now` במובייל שוּנו ל-`flex:1 1 0` = **רוחב שווה** (177px כל אחד, per Figma `1124:81` — היה 177px+fill). **2026-07-09 — סקשן `#press` נבנה מחדש לפי Figma `551:371`:** 11 כרטיסים, 6 שורות. הכרטיסים **כתובים ידנית ב-HTML** (לא נמשכים מ-`press.json` בזמן ריצה — `homepage_visible` הוא דקלרטיבי). מיפוי: **col1 בפיגמה = הכרטיס הראשון ב-HTML = מימין** (`.press-row--swap` הוא `row-reverse`), col2 = משמאל. שורה 6 = כרטיס בודד משמאל → `.press-row--swap .press-row--right` (`justify-content:flex-end`, השימוש הראשון במחלקה הזו). נוספו 2 כתבות חדשות בראש (manicure-against-darkness + peeling-a-layer), הכרטיס של **`events/artist-talk/` הוסר מהגריד** (לא קיים ב-Figma; דף האירוע נשאר חי, `homepage_visible:false` ב-`press.json`), והתג של `press/time-out/` הישן שונה מ-"מגזין TIMEOUT" ל-"TIME OUT | תרבות" לפי `551:367`. **2026-07-15 — `#press` נבנה מחדש v3 לפי Figma `1124:1354`:** 19 כרטיסים, 10 שורות (שורה 10 = כרטיס בודד). מיפוי **מאומת מול רינדור** (מחליף את הניסוח הקודם): col1 בפיגמה = הכרטיס הראשון ב-HTML = **שמאל** בדסקטופ, col2 = ימין; הכרטיס הבודד (artist-talk, col2) → `.press-row--swap.press-row--right` = מימין. מובייל = אותו סדר בעמודה אחת (אישור משתמש; אין frame מובייל). תמונת כרטיס גדלה ל-**292×162** (היה 178×112; aspect-ratio במובייל עודכן), רווח עמודות 48px, ונוספו `.img--contain` (רקע `#FAFAFA`) + `.img--white` — ל-liel (לוגו elline) ולפלייסטיישן של time-out הישן. **2 כרטיסים חדשים בלי דף** — anat wégier (7.8.2026) + tal nehoray (29.6.2026): `press-card--soon` עם `href="#"` → טוסט "בקרוב" (`.soon-toast` + IIFE ליד סקריפט האינסטגרם); `route:null` ב-`press.json`, ראה `docs/todo.md`. (**עדכון 2026-07-26:** כרטיס anat הוסב לקישור רגיל → `events/anat-wegier/` — הדף נבנה; tal nehoray עדיין `--soon`.) **כרטיס `events/artist-talk/` חזר לגריד** (`homepage_visible:true`). 5 כרטיסי אירועי יולי עברו מעברית לאנגלית (`.desc-lat` **13px** — כיול אופטי מ-16 Figma; עברית משולבת ב-`<span class="heb">` 1.23em; `desc-lat-sm` 10px בוטל בכל ה-breakpoints). תמונות חדשות `images/events/…`: `<slug>/card.{webp,avif}` ל-risa/zohar/nir (crops מ-Figma), `close-look/card` (נאפה עם טקסט האוברליי, מחליף את intro-photo בכרטיס), `ktuba/card` (צילום מיצג חדש), `artist-talk/card` (רצועת חלון הראווה — משותפת לכרטיסי tal+artist-talk), `liel-salman/logo`, `anat-wegier/card`. `press.json` (cover_image + 2 רשומות חדשות) ו-`homepage.json item_ids` סונכרנו. **2026-07-12 — סקשן חדש `#events-upcoming` (Figma `1057:97`, דסקטופ בלבד — אין frame מובייל):** יושב **בין `#exhibitions-now` ל-`#exhibitions` (הארכיון)**. כותרת "events / upcoming", וגריד של 2 עמודות (דסקטופ, כרטיס 693px; מובייל 2-up בגובה 214.55px כמו `ex-now`). כל כרטיס = שם גלריה בעברית מעל התמונה, ובתוך התמונה: תג `upcoming` (שמאל-עליון) + בלוק **ממורכז אנכית** — שם האירוע (Copperplate bold) + פס `the exhibition` / `exhibition • volume 2` (**Solway** — נוספו `@font-face` + `--sol` ל-`index.html`, לא היו שם) + שם התערוכה. **הכרטיסים מרונדרים ב-JS** מ-`<script type="application/json" id="events-upcoming-data">` (מירור inline של כל 10 האירועים מ-`events.json`, כי file:// לא יכול fetch): הסקריפט מסנן `date >= היום`, ממיין עולה, ואם **לא נשאר אף אירוע — הסקשן נמחק מה-DOM** (`hidden` כברירת מחדל, נחשף רק אם יש פריטים). לכן אירוע שתאריכו עבר נושר לבד. שדה חדש ב-`events.json`: **`card_title_en`** (הכותרת הקצרה על הכרטיס — "pop up tattoos" / "artist talk" / "save the date"). אירוע חדש = הוסף ל-`events.json` **וגם** לרשומה ב-`#events-upcoming-data`. האירועים ממשיכים להופיע גם ב-`#press` (בנוסף, לא במקום). **🔴 2026-07-23 — `#press` הוחזר לגריד סטטי (בקשת המשתמש): הסליידר/קרוסלה הוא רק ב-`#events-upcoming`.** ניסיון ביניים להפוך את `#press` לקרוסלה + לסנן ממנו אירועי-עתיד בוטל — `#press` שוב גריד רגיל של 19 כרטיסים ב-10 שורות (2 עמודות), בלי JS, בלי סינון תאריכים; מציג את כל הכרטיסים כולל אירועים שכבר עברו. (הסינון לפי תאריך קיים רק ב-`#events-upcoming`.) **🔴 2026-08-04 — `#press` צומצם ל-9 כרטיסים בעיצוב חדש (רה-דיזיין פרומפט 10, Figma `1322:258` מובייל; דסקטופ = אנטומיית הכרטיס של `/press/`):** בקשת קורין — בהומפייג' נשארים **כל כתבות העיתונות + שני אירועי הפתיחה בלבד** (רשת13, מניקור, ora, התחנה האחרונה, פתיחת how-many, פתיחת loneliness, פורטפוליו, וואלה, קאונטדאון — כרונולוגי חדש→ישן); כל שאר האירועים חיים ב-`/press/`. כרטיס = תג chip `#EEF0EF` + כותרת + תאריך + תמונה (292×162 דסקטופ / 171×108 מובייל), אותם כיולים כמו `press/index.html` (תג/תאריך EN 11.5, כותרת EN 13, HE 16); פורטפוליו משתמשת ב-`00-home-card-v2`. סטטי ב-HTML (הוסרו `#press-data` וה-renderer של מצב-הביניים, וגם טוסט "בקרוב" — אין יותר כרטיסי `--soon` בהומפייג'; טל נהוראי רק ב-`/press/`). דסקטופ = `grid-auto-flow:column`+rtl על `repeat(5,auto)` — כרטיסים 1-5 בעמודה הימנית, 6-9 בשמאלית (**הוספת כרטיס = לעדכן את מספר השורות ל-`ceil(n/2)`**). מתחת: כפתור ממוסגר **"more press & events"** (`.press-viewall`, מתכון `.ev-up-viewall`) → `press/`. דאטה: `press.json homepage_visible` (true רק ל-9) + `homepage.json press_section.item_ids`. העוגן `#press` נשמר. **2026-07-15 — redesign v2 לסקשן (Figma `1124:901` דסקטופ / `1124:133` מובייל) + קרוסלה:** הכותרת "events/upcoming" הוחלפה ב**כותרת מעגלית** — SVG outlines ירוק `#2B4C39` (`images/homepage/upcoming-events-circle.svg`, רונדר מ-node `1124:904`) משמאל לכרטיסים (דסקטופ: grid `1fr 3.1fr`; מובייל: זולג מחוץ לקצה השמאלי — `left:-93px`, בלוק 242px). כרטיס חדש לגמרי: תמונה מלאה + גרדיאנט `180deg rgba(102,102,102,.2)→rgba(0,0,0,.8) 81%`, שם גלריה **באנגלית** (Copperplate bold 20, שדה חדש `gallery_en[]` — מערך שורות: `["dizengoff","square"]`/`["medina","square"]`) + תאריך `dd.mm` (light 20) למעלה, שם אירוע (bold 14) + שם תערוכה (light 14) למטה; אין יותר תג `upcoming`/פס Solway/שם גלריה בעברית מעל. **קרוסלת לופ אינסופי**: 3-up דסקטופ (כרטיס 300px, aspect `300/592`, gap 16) עם **חיצי chevron דקים 48px מתחת מימין** (SVG מ-nodes `1124:914/916`); מובייל = כרטיסי 200×301 (aspect `200/301`, gap 8, padding-left 16), **בלי חיצים — swipe בלבד** (per Figma). שדה חדש נוסף: `soon:true` (אירוע בלי דף — מקבל `press-card--soon`+`href="#"` → טוסט "בקרוב"; שימש את anat-wegier עד 2026-07-26, כשקיבלה דף `events/anat-wegier/` וה-`soon` הוסר מהרשומה שלה ב-`#events-upcoming-data`; תמונה `images/events/anat-wegier/upcoming.{webp,avif}`+וריאנטים, מ-imageRef `615a95e…`). **🔴 2026-08-04 — `#events-upcoming` הוחלף כליל (רה-דיזיין אוגוסט, Figma `1318:476` מתוך פריים המובייל `1318:363`; דסקטופ = אדפטציה):** אין יותר עיגול מסתובב, קרוסלה, חיצים ורנדרר — הוסרו המירור `#events-upcoming-data` וה-IIFE (ה-SVG `upcoming-events-circle.svg` והתמונות הישנות נשארו בדיסק, כלל-זהב 8). במקום: **סקשן סטטי** — כותרת מוערמת "upcoming" (Copperplate Bold 58; מובייל 46) מעל "events" (Light 91; מובייל 72; חפיפה ‎-16px/-12px) + **שני פוסטרים חודשיים ("הרשימות")** זה-לצד-זה: `images/homepage/upcoming/poster-dizengoff-v2.{webp,avif}` (941×1672; **2026-08-06 — הוחלף לפוסטר "AUGUST EVENTS"** מ-Figma `1318:482`, imageRef `bbe3b9b1…`; קבצי `poster-dizengoff.*` הישנים [יולי-אוגוסט] נשארו בדיסק — כלל-זהב 8. ⚠️ ה-node המעודכן = fill יחיד ב-FILL/cover, אבל באתר נשמר המבנה הקיים: שכבת רקע `poster-bg` ב-cover מאחורי הפוסטר ב-contain — הפרש האספקטים זניח) + `poster-hamedina` (1620×2880, ללא שינוי — אומת RMSE 0 מול `2000bf6f…`); `object-fit:contain`, aspect `175/301`, דסקטופ עד 380px לכרטיס gap 16, מובייל gap 8 (175×301 ב-390) + כפתור ממוסגר **view all** (Light 16, stroke 1px, padding 4px 16px). **עדכון הלוח = החלפת קבצי הפוסטרים** (תהליך תוכן של המעצבת — לא קוד). הכותרת, הפוסטרים והכפתור כולם → `events/` (הנחיית המעצבת). הוסר מנגנון ה-`hidden` (הסקשן כבר לא תלוי-תאריכים). **2026-08-04 (המשך, פידבק משתמש): במובייל (≤768) הפוסטרים = קרוסלת scroll-snap** — `.ev-up-posters` full-bleed (`margin:0 -16px`, `padding:0 16px`, `overflow-x:auto`, `scroll-snap-type:x mandatory`, scrollbar מוסתר), פוסטר `flex:0 0 76%` (עד 340px, `scroll-snap-align:center`) כך שהפוסטר הבא מציץ ~90px מהקצה (affordance ברור לגלילה); בלי חיצים (קונבנציית מובייל=swipe). `sizes` של תמונות הפוסטר עודכן 46vw→76vw. דסקטופ נשאר 2-up סטטי ממורכז. **Solway הוסר מ-`index.html`** (@font-face + `--sol` שימשו רק את הקרוסלה; קבצי הפונט נשארו ב-`פונטים/`). **2026-07-15 — סקשן `#x-our-artists` סונכרן מחדש ל-Figma `1124:1181` ("mobile order"):** גדל מ-26 ל-**38 פריטים** (גריד 3×13, row-major). גם הגריד (דסקטופ) וגם הקרוסלה (מובייל) + הנקודות משתמשים באותו סדר. התמונות = רינדור-node של כל 38 ה-RECTANGLE composites (crop/FIT/רקע `#FAFAFA` נאפים ל-PNG → `object-fit:cover`), `work-1..38.{webp,avif}` במיפוי 1:1 למיקום. `homepage.json x_our_artists.items[]` עודכן (מקור-אמת). אומנים חדשים שנחשפו בסקשן: lahav-barak, alice-debellis, elsa-ars-brush, livay-levi, amnon-lipkin, jessica-tabarovsky, talia-zoref, michael-konovalenko, hadas-tuval, maria-artamonova, bar-cohen. **2026-08-05 — `#x-our-artists` הפך לקרוסלת פוקוס בשני ה-breakpoints (רה-דיזיין אוגוסט פרומפט 9, Figma מובייל `1318:695`; דסקטופ = אדפטציה):** גריד ה-38 הדסקטופי **נמחק מה-markup**; הסקשן כולו רץ על קומפוננטת `.tri` (triptych) — 38 שקופיות עם `data-caption`/`data-caption-href`/`data-artist-href` (קליק על המרכז/הכיתוב → דף האומן), נפתח על **nir giorgio levin** (`data-tri-start="13"`) עם zohar ron/gilad kenan בצדדים per הפריים. דסקטופ: מרכז 481×726, peeks 302×453 באטימות ‎.6, ו**חיצים בדפוס ig-nav** (only-desktop) שמשגרים ArrowLeft/Right ל-`.tri`; מובייל: swipe בלבד, peeks זולגים מהקצוות, בלי dots (מוסתרים גלובלית). caption Copperplate אפור `#989898`. Archive redesigned 2026-06-08 → big single exhibition card (`.ex-archive`, loneliness, `XhGH...::474:97`/`467:45`); old tabs+thumbs archive backed up in `trash/`. **2026-06-15:** נוסף curator footer בתחתית סקשן `artworks` (`.aw-curator`, "korin avraham / the curator" + כפתור view →`/curators/korin-avraham/`, Figma `710:177` desktop / `710:117` mobile; מובייל הופך סדר ל-role-מעל-name). **2026-08-04 — סקשן `#artworks` עוצב מחדש (רה-דיזיין אוגוסט פרומפט 8; Figma מובייל `1318:560` בתוך `homepage-mobileNew 1318:363` — אין frame דסקטופ, דסקטופ = אדפטציה):** גריד שחמט `.aw-grid` — מובייל 2×3 (תאים ריבועיים ~179px, זרימת source טבעית: תמונה/the / art-works/תמונה / תמונה/more), דסקטופ 3×2 (I·T·I / T·I·T דרך `grid-area` ב-`@media (min-width:769px)`; אותו DOM). **3 תמונות חדשות** `images/homepage/artworks/aw-{1,2,3}.{webp,avif}`+`-480w` ב-**`object-fit:contain`** (FIT בפיגמה — הן כבר לא נחתכות לריבוע; aw-2 = אותו imageRef של hero natasha-zeriker, `3f0a517d…`, בלי crop). אריחי טקסט: "the" / "art\works" (תא אחד, שתי שורות) / "more"+chevron **ימינה אחרי** הטקסט → `works/` (Bold/Light 36 פיגמה → 26px מובייל בכיול אופטי, clamp עד 64px דסקטופ). האריחים הישנים `images/works/tile-*` נשארו בדיסק (כלל-זהב 8); `works.json works[]` עודכן מ-4 ל-3 רשומות (אריחי תמונה בלבד). בלוק ה-curator: role מעל name בשני ה-breakpoints, כפתור **"read more"** (היה "view") → `curators/korin-avraham/`; מובייל `min-height:179px`, padding ‎0 48. **2026-08-04 (המשך — תיקון משתמש): הדסקטופ יושר ל-Figma `1124:988` "Work Section"** (פריים דסקטופ קיים בתוך `homepage-desktop 1124:754` הישן — המשתמש הצביע עליו כמקור; מחליף את אדפטציית ה-3×2): דסקטופ = **אותו שחמט 2×3 בזרימת source טבעית** (בוטלו כללי ה-`grid-area`), תאים **624×600** (`aspect-ratio:624/600`), padding עליון 32 (היה 64), `.artworks` קיבל `max-width:var(--max)`. **תמונות דסקטופ נפרדות ב-cover** — `aw-{1,2,3}-desktop.{webp,avif}` + וריאנטים 480/768/1080 (רינדורי-node של `1124:991/996/997`; לתא 1 מחסנית fills — רק רינדור אמין; **imageRefs שונים מהמובייל**, אלו יצירות אחרות) מוגשות ב-`<picture>` עם `source media="(min-width:769px)"` — כל ה-sources ידניים כי `picture-upgrade.js` מדלג על img שכבר בתוך `<picture>`; חובה `.aw-img picture{display:contents}` והגריד הוקשח ל-`repeat(2,minmax(0,1fr))` (כמו בפיגמה). **המשך 2026-08-04 — תמונות הדסקטופ הוחלפו לטריו החדש (בקשת משתמש):** התברר ש-`1124:988` הוא פריים ישן שתאיו עדיין הכילו את יצירות האריחים הישנות — ממנו נלקח ה-**layout בלבד**; עכשיו **שני ה-breakpoints מגישים את אותן 3 תמונות** (`aw-{1,2,3}`) — ה-`source media` הדסקטופיים הוסרו, ה-cover נעשה ב-CSS, נוספו וריאנטים לרטינת דסקטופ (`aw-1-1248w`/`aw-2-1248w`/`aw-3-690w`.{webp,avif}) ו-`object-position` דסקטופי לתא 1 (`50% 10%`) ותא 4 (`50% 18%`) כדי לשמור את הראשים בפריים. קבצי `aw-*-desktop*` נשארו בדיסק (כלל-זהב 8). דסקטופ בלבד (scoped ≥769): שורות "ART/works" מיושרות שמאלה בתוך הבלוק הממורכז, **"‹ more" = chevron לפני הטקסט** (`row-reverse` + `scaleX(-1)`), curator **name מעל role** (CSS `order`) ב-**64px** (כיול מ-80) וכפתור **"view"** — החלפת תווית ב-CSS דרך `.lbl-d`/`.lbl-m` (המובייל נשאר role→name + "read more"). המובייל לא השתנה (אומת בצילום 390 אמיתי דרך iframe — ⚠️ headless Chrome במק לא יורד מרוחב חלון 500, צילום ישיר ב-390 נחתך ומטעה). **2026-07-15 — סקשן חדש `#big-news` (Figma `1124:1011` דסקטופ / `1124:240` מובייל, בתוך frames `homepage-desktop 1124:754`/`homepage-mobile 1124:21`):** "BIG BIG NEWS" — נקודת מכירה חדשה (חנות בתוך גלריית כיכר המדינה), יושב **בין `#artworks` ל-`#perfume-promo`** (סדר Figma). דסקטופ: טקסט משמאל (כותרת FbEzmel 80px "פתחנו לכם נקודת\nמכירה נוספת!", תת-כותרות 28/20) + תמונה 492² מימין; מובייל: תמונה מעל, טקסט מתחת (36/24/18). על התמונה scrim `rgba(0,0,0,.33)` + טבעת "BIG"×6 (SVG `textPath` — **6 `<textPath>` נפרדים ב-`startOffset` שווים; `textLength` על textPath לא נאכף בכרום**) + "news" ממורכז (Copperplate 56px — כיול אופטי מ-55.7 Figma). תמונה `images/homepage/big-news/shop.{webp,avif}` + וריאנטים 480/768. דאטה דקלרטיבי: `homepage.json :: big_news`. הכתובת מקושרת ל-`#galleries`. **🔴 2026-08-04 — `#big-news` הוחלף בעיצוב חדש (רה-דיזיין אוגוסט 2026 פרומפט 7, Figma `1318:583` מובייל בלבד — ⚠️ שם ה-node "Exhibitions Archive" משקר; דסקטופ = אדפטציה):** הסקשן עכשיו **טקסט בלבד, ממורכז, על לבן** — כותרת `BIG NEWS` (Copperplate Bold, `26px` מובייל = כיול אופטי מ-36 Figma, `clamp` עד `44px` דסקטופ) → כותרת עברית (FbEzmel Regular, 36 מובייל / עד 64 דסקטופ) → תת-כותרת (Light 24/28) → כתובת (Light 18/20, עדיין → `#galleries`). **אין תמונה, אין טבעת BIG, אין scrim** — ה-markup הישן (`.nb-media`/`.nb-overlay`/`.nb-ring`/`.nb-info`) הוסר; קבצי `images/homepage/big-news/shop.*` נשארו בדיסק לא-מקושרים (כלל-זהב 8). `homepage.json::big_news` עודכן — הוסרו `ring_text_en`/`badge_en`/`image`, נוסף `heading_en:"big news"`, `figma_node_mobile:"1318:583"`, `figma_node_desktop:null`. **🔴 2026-08-04 — סקשן הבשמים עוצב מחדש והוכפל (רה-דיזיין פרומפט 5, Figma `1318:599`+`1318:1169` מובייל בלבד בתוך `1318:363` — שמות ה-nodes "Exhibitions Archive" משקרים; דסקטופ = אדפטציה two-col ברוח הישן):** `#perfume-promo` (how-many): **תמונה אחת חדשה** `images/homepage/perfume-promo/how-many-v2.{webp,avif}` 1200² +480/768 (imageRef `69ac0bc6…` crop `af2d6c`) — **הוסרו גלריית 5 ה-thumbs, סקריפט ההחלפה (`ppMain`) וה-`pp-bio`** (קבצי perfume-1..5 נשארו בדיסק — כלל-זהב 8); טקסטים ממורכזים + כפתור "עוד על הבושם באתר" → zrp.co.il (ללא שינוי). כיולים מובייל: כותרת 36→**26px**, sub-en 20→**16px** + `<br class="mbr">` (מחלקה חדשה ב-index.html — שבירת ה-Figma "HOW MANY PARTNERS / HAVE YOU HAD?"); עברית px verbatim; padding מובייל 32px. **סקשן חדש `#perfume-savlanut`** מיד אחריו — בושם **"סבלנות"** של תערוכת הקולפן: `savlanut.{webp,avif}` 931×934 +480/768 (imageRef `922a369f…` crop `53b863`), "סבלנות" (FbEzmel Regular 20) / "בושם הנושא של תערוכת הקולפן" (Light 300, **"תערוכת הקולפן" מקושרת** ל-`exhibitions/the-peeler/`, אנדרליין רבע-שקוף כקונבנציית `artist-link`) / פס `EXHIBITION • VOLUME 2` (`.pp-exbar`, Copperplate Light 20→16, נקודה 4px) / קו 32×1 / "בלעדי לרכישה בגלריה בכיכר המדינה" (Light 18) — **🔴 בלי כפתור ובלי קישור רכישה: אין לבושם עמוד באתר הבישום, מכירה אופליין בגלריית כיכר המדינה בלבד**. דסקטופ savlanut = מראה מהופכת (`row-reverse`, תמונה מימין); ה-gap טקסטים↔קו = 32 דסקטופ / 16 מובייל (Figma). דאטה דקלרטיבי: `homepage.json :: perfume_promo.perfumes[]` (שדות: `name_he`, `exhibition_bar_en[]`, `exclusive_he`, `cta_*` nullable). מובייל אומת מול רינדורי ה-frames (`_originals/figma-checks/pp-*`). **2026-08-04 — סקשן חדש `#soos` (חסות soos.sound, רה-דיזיין פרומפט 11; Figma `1318:316` מובייל בלבד — ⚠️ שם ה-node "Exhibitions Archive" משקר; דסקטופ = אדפטציה):** יושב **מעל `#tribe-teaser`** (הכרעת משתמש 2026-08-04 — גוברת על "אחרי `#galleries`" שבפרומפט). לוקאפ אנכי ממורכז — "הקולפן" (FbEzmel Light 36 → `exhibitions/the-peeler/`) / X (Copperplate Light 36 verbatim — גליף בודד, בלי כיול) / לוגו soos SVG inline 88×21.59 (`fill:currentColor`, class `soos-logo` — לא "logo", לקח ההתנגשות) → `sponsors/soos/`; קרדיטים ממורכזים gap 8 ("soos.sound | מודל : Bella" + `www.soos.audio` חיצוני) עם חריגת ה-lowercase המוכרעת (`.soos-brand`); רצועת 3 תמונות = מיחזור `images/sponsors/soos/gallery-01..03.webp` — **דסקטופ בגאומטריית ה-thumbs של tribe (158px, gap 16; אותו יחס .825 בדיוק), מובייל = Figma verbatim (100.25px, gap 4)**; כפתור ממוסגר `read more` (4px/16px, border 1px, UPPERCASE גלובלי) → `sponsors/soos/`. דאטה דקלרטיבי: `homepage.json :: soos_sponsor`. אין רצועות/לייטבוקס — תמונות סטטיות. **2026-08-06 — `#galleries` תמונות עודכנו (Figma `1318:657` בפריים המובייל `1318:363`; דסקטופ = אותם קבצים):** medina → `images/galleries/medina/hero-v2.{webp,avif}` (חלון הראווה עם כיתוב הקולפן, imageRef `c937cf2f…`), dizengoff → `dizengoff/hero-v2.{webp,avif}` (פנים הגלריה עם חלון How Many, imageRef `a67703c8…` עם קרופ ה-Figma) — שניהם 822×996 (cover ליחס הכרטיס 33/40, זהה בשני ה-breakpoints) + וריאנטי `-480w` + srcset; `hero.webp` הישנים נשארו בדיסק (כלל-זהב 8). **כרטיס הפשפשים = ירוק שטוח `#2B4C39` בלי תמונה:** ב-Figma (`1318:686`) שכבות התמונה מוסתרות (אומת ברינדור-node — flat green); ה-`<picture>` המוסתר הוסר מה-markup (ה-`::after` של `dim--green` ממילא כיסה אותו באטימות מלאה — חיסכון ~200KB), קבצי `flea-market/hero.*` נשארו בדיסק. `galleries.json` עודכן (`image_hero` → hero-v2; פשפשים → null + `figma_node_mobile` לשלושתן) + סונכרן מירור `fallback-galleries` בעמוד האוצרת. ⚠️ טקסטים של הפריים לא סונכרנו (המשתמש ביקש תמונות בלבד) — פערים פתוחים ב-`docs/todo.md`. **🔴 2026-08-12 — שעות פתיחה עודכנו (אותו node `1318:657`):** מדינה שונתה מ-**"א'-ה' 10:00-19:00 / ו' 10:00-14:00"** ל-**"א'-ה' 18:00-11:00 / ו' 14:00-11:00"** (verbatim מהטקסט הגולמי ב-Figma — **הכרעת משתמש: ספרות מוצגות/נשמרות LTR כמו אותיות לועזיות, בלי "לתקן" סדר**; שבת סגור ללא שינוי). **דיזינגוף קיבלה בלוק שעות בפעם הראשונה** (עד כה `hours:[]`) — אותן שעות בדיוק כמו מדינה (כך גם ב-Figma: אותו תבנית-טקסט חוזרת על שתי הכרטיסים). עודכן ב-`galleries.json` (`hours[].time`) וב-`index.html` (`.gallery-card .hours`). **⚠️ פערים שנתגלו ולא טופלו (לא התבקש לתקן):** (1) כתובת הפשפשים ב-`index.html` = "שוק הפשפשים, **יפו**" בעוד ב-`galleries.json` וב-Figma זה "שוק הפשפשים, **תל אביב**" — סטייה קיימת בין ה-JSON ל-HTML הסטטי, לא נגזרת בזמן ריצה (סקשן הגלריות כתוב ידנית ב-HTML). (2) בנוד של Figma לכרטיס **מדינה אין טקסט כתובת רחוב כלל** (רק שעות) — האתר עדיין מציג "ז'בוטינסקי 131, תל אביב"; לא הוסר, טעון בירור אם זו השמטה מכוונת בעיצוב. **2026-08-12 — כרטיס אירוע `events/gala-night/` נוסף בראש גריד `#press`** (הכרעת משתמש — חריגה מכלל הרה-דיזיין "רק כתבות + 2 פתיחות", תועדה גם ב-`_note` של `homepage.json`): 10 כרטיסים, שורות נשארו `repeat(5,auto)` (= ceil(10/2)); רשומת `press.json` `event-gala-night` (`homepage_visible:true`) + `homepage.json item_ids`; נוסף כלל CSS `.press-card .ttl.en .heb` (מילה עברית בכותרת אנגלית — הכרטיס הראשון כזה בהומפייג'). |
| `/about/` | Biography of Erez | `Zn3N...::1213:2970` | `Zn3N...::1213:2888` | ✅ `about/index.html` |
| `/works/` | The Art Works | `XhGH...::542:467` (עמוד מלא; הגריד = `542:500`. canonical 2026-06-11) | `XhGH...::542:1365` (עמוד מלא; גריד `542:1390`. ⚠️ **סדר שונה מהדסקטופ בפיגמה** — האתר עוקב אחרי הדסקטופ) | ✅ `works/index.html` — **גריד 3 עמודות דסקטופ / 2 מובייל**, כרטיסי יצירה (אומן/כותרת/גלריה/נמכר/פרטים, `white-space:pre-line`), לייטבוקס single. Data ב-`works.json` `art_works[]` = **140 יצירות** (**2026-07-12 — עדכון סטייטמנט natasha-zeriker מ-Figma `668:13682`:** רשומת ה-`exhibition_statements` שלה (הקולפן) הוחלפה בטקסט חדש בן **6 פסקאות** ("לאורך השנים אני חוקרת את המתח שבין אחיזה להרפיה..."), ו-`exhibition_title_he` (ברשומה + ב-natasha-zeriker-1) → **"הקולפן | מעורר רגש: אובדן שליטה"** (תקדים amnon/jessica/zohar). הופץ עם `sync_works_mirrors.py` + סנכרון ידני של המירורים ב-`_staging/artists-tali-zelnik/`.) (**2026-07-06 — hadas-tuval, אומנית חדשה מלאה (Figma `797:106`, יצירות `797:146`):** נוספה ל-`artists.json` (ביו, פורטרט `images/artists/hadas-tuval/`, אינסטגרם `@hadastuval.ayni`) + 2 יצירות בתערוכת הקולפן/גלריית המדינה — hadas-tuval-1 "falling into movement" (1086×1448) + hadas-tuval-2 "סף" (**2026-07-26 — צילום חדש מ-Figma `797:152`, imageRef `8dfed7d2…`:** `img:"hadas-tuval-2-v2"` 998×1330 + וריאנטים 480/768 + OG חדש; הקבצים הישנים `hadas-tuval-2.*` 900×1747 לא נמחקו — כלל-זהב 8. הופץ עם `sync_works_mirrors.py`), תמונות+וריאנטים ב-`images/works/v2/`. **עודכן 2026-07-09:** הביו הוחלף בטקסט הנוכחי מ-Figma `797:137` ("הדס טובל (בת 41, פועלת בתל אביב)...", 2 פסקאות), ונוספה רשומת `exhibition_statements` → **"הקולפן | מעורר רגש: החזקה והרפיה"** (`797:148`/`797:149`, 2 פסקאות). באותו תיקון: `exhibition_title_he` של tali-zelnik-1/2 → **"הקולפן | מעורר הרגש: שייכות"** (Figma `854:1082`). נוצרו דף אומנית (תבנית bar-cohen) + 2 עמודי יצירה; regen `__ARTISTS_INLINE__` בכל 42 הדפים + `sync_works_mirrors.py`; הכרטיס שלה בעמוד הקולפן הפך קליקאבילי. **נוספה גם לגריד `/artists/`** (כרטיס 40, סוף הגריד — בקשת המשתמש 2026-07-06 למרות שאין לה עדיין כרטיס ב-Figma `523:145`; תמונת גריד `images/artists/grid/hadas-tuval.{webp,avif}` 810²+480w מהפורטרט).) (**2026-07-15 — סדר עבודות amnon-lipkin בדף האומן עודכן לפי Figma `798:173` (המעצבת שינתה):** שתי עבודות הקולפן ירדו מראש הקבוצה לסופה — `artist_page_pos`: amnon-lipkin-1..11 → 1..11, **amnon-lipkin-13 "קולפן - מילים" → 12, amnon-lipkin-12 "קולפן - לב" → 13** (לב אחרונה, בודדת בשורה). הופץ עם `sync_works_mirrors.py`; אומת בצילומי headless מול רינדור הפיגמה. ראה lessons 2026-07-15 על קונבנציית LTR-פיגמה/RTL-אתר.) (**2026-07-06 — עדכון טקסטים amnon-lipkin מ-Figma `798:203`/`798:214`:** ביו חדש ב-`artists.json` (נפתח "אמנון ליפקין הוא..."; "בדיקנות" בפיגמה תוקן ל"בדייקנות") + `exhibition_title_he` של 13 יצירותיו + רשומת `exhibition_statements` שלו → **"הקולפן | מעורר רגש: מאבק מייסר ומרגש"** (תקדים zohar-ron), ופסקה 1 של הסטייטמנט הורחבה ("...ומציפות את הקונפליקט שבין החלק המייסר במאבק לבין המרגש שבו."). הופץ עם `sync_works_mirrors.py` + regen `__ARTISTS_INLINE__` בכל 41 דפי אומן.) (**2026-07-06 — עדכון טקסטים jessica-tabarovsky מ-Figma `668:11043`/`668:13332`:** ביו חדש בגוף שלישי ב-`artists.json` (נפתח "ג׳סיקה טברובסקי (בת 57, פועלת מבאר שבע) היא...") + רשומת `exhibition_statements` שלה (הקולפן) תומצתה מ-4 ל-2 פסקאות ("הציור \"התנערות\" נולד מתוך זיכרון אישי..."). **המשך 2026-07-06 (Figma `668:11042`/`668:13235`):** כותרת הקבוצה עודכנה לכותרת המלאה מ-Figma `668:13236` — `exhibition_title_he` של jessica-tabarovsky-1 + רשומת ה-statement → **"הקולפן | מעורר רגש: השתוקקות"** (תקדים amnon/zohar); הביו והסטייטמנט עצמם כבר היו verbatim. **המשך-2 2026-07-06 (Figma `1050:16`):** +**jessica-tabarovsky-2 "בחורה ממתינה לרכבת של 6:48"** (30x40 ס\"מ שמן על קנבס, גלריית המדינה, הקולפן, `artist_page_pos:1`; jessica-tabarovsky-1 הוסט ל-pos 2 — סדר גריד Figma `668:13241`), תמונות `images/works/v2/jessica-tabarovsky-2*` + עמוד בודד `works/jessica-tabarovsky-2/` + OG. במקביל עודכנו בפיגמה שוב הביו (`668:11043`, "...לאחר שפעלה לאורך השנים...") והסטייטמנט הורחב ל-**4 פסקאות המכסות את שתי היצירות** ("העבודות המוצגות בתערוכה..."); עודכנו גם 4 ה-meta descriptions בדף האומנית. הופץ עם `sync_works_mirrors.py` + regen `__ARTISTS_INLINE__` בכל 40 דפי אומן. הופץ עם `sync_works_mirrors.py` + regen `__ARTISTS_INLINE__` בכל 41 דפי אומן; עודכנו גם meta description/OG/twitter/JSON-LD בדף האומנית.) (**2026-06-24:** +2 ל-noemi-safir בתערוכת How Many — noemi-safir-11 "התמכרות רכה" + noemi-safir-12 "לחץ סגול", שתיהן 50x40 אקריליק, גלריית דיזנגוף, Figma `XhGH...::943:237`/`668:12584` (מתוך דף האומן המלא `668:12489`); נוספו לראש קבוצת How Many בדף האומן (`artist_page_pos` 4/5, שאר עבודות How Many הוסטו +2), מצורפות לסוף המערך. **+2 ל-amnon-lipkin בתערוכת הקולפן** — amnon-lipkin-12 "קולפן - לב" + amnon-lipkin-13 "קולפן - מילים", שתיהן 36/66 ריקמה ידנית, גלריית המדינה, Figma `XhGH...::944:257`/`800:491` (מתוך דף האומן המלא `798:173`); נוספו לראש קבוצת הקולפן (`artist_page_pos` 1/2, שאר עבודות amnon הוסטו +2). סנכרון דרך `tools/sync_works_mirrors.py`.) (**2026-06-23:** +elsa-ars-brush-6 "מושעה בזמן" 59x39 אקריליק על בד, גלריית המדינה, תערוכת הקולפן, Figma `XhGH...::833:944`. ) (**2026-06-23 — תערוכת הקולפן, 6 frames מובייל `landing`:** +17 יצירות ל-4 אומנים חדשים — michael-konovalenko (`798:374`, ×3), maria-artamonova (`798:240`, ×1), alice-debellis (`849:963`, ×2), amnon-lipkin (`798:173`, ×11) — כולן גלריית המדינה/`medina`, `exhibition the-peeler`, statement_he לכל אומן; מצורפות לסוף המערך (לא בגריד Figma `542:500`). +talia-zoref-2 "שלווה בסגנון|Serenity in Style" 60x80 + talia-zoref-3 "להתחבר|Connect" 100x85, Figma `XhGH...::668:13734`/`867:1138`/`870:1148`, גלריית המדינה; talia-zoref-1 pos 1→2), סדר **1:1 מול גריד הדסקטופ `542:500`** — אומת 2026-06-11 ברמת מיקום+תמונה (node-id binding + imageRef diff), וברמת **טקסט גלוי**: כל 109 הכרטיסים רונדרו כ-PNG ונבדקו מול ה-data (שכבות מוסתרות לא נשאבות — ראה lessons). (⚠️ הפריים `mode:none` = absolute; סדר ויזואלי = מיון לפי y,x). תמונות ב-`images/works/v2/`. **עדכון Figma 2026-06-11:** +7 יצירות: **zohar-ron-5..8** (דג 1/2/3 + בּוּרְקָה|برقع, ראש הגריד, nodes `613:67..91`), **gal-rotem-1** (טביעה בחשיכה, `607:33`, pos20), **raz-ronen-1..2** (`616:129/149`, pos37-38, שתיהן נמכר) — שני אמנים חדשים שכבר היו עם דפי placeholder; +4 תגי נמכר חדשים: anat-wegier-4, alon-2, nir-giorgio-levin-2, la-raz-porta-1. עדיין מוסתר-ולא-נשאב: בולרפלייט "60x80 שמן על קנבס" ב-26 כרטיסים אחוריים + כותרת "התנערות" ב-holy-kadosh-3 + פרטי zohar-shtrit-1; "קרמיקה" **כן גלוי** בפיגמה על tal-nehoray 1-5 (אומת ברינדור 2026-06-11). **כפילויות תמונה ב-Figma (לא שוכפלו):** jessica=anat-4, noemi-4=risa, noemi-6=alon-1, NGL-2≈alon-1, tanya crop2/aura1=aura2, sami 64=66 — לתקן ב-Figma. **טעינה מדורגת:** 12 ראשונים ואז צ'אנקים של 18 (IntersectionObserver sentinel, rootMargin 1800px, re-arm); fallback מרנדר הכל. כתיב שמות עברית = הקנוני של `artists.json`; גרשיים מנורמלים ל-`ס"מ`. legacy: `542:499` (לפני המובייל החדש), `491:928`, `489:4`, `Zn3N...::1213:2615` |
| `/works/:id/` | Single artwork (template) | `XhGH...::674:14072` | `XhGH...::674:14022` | ✅ **כל 111 היצירות = עמוד משלהן** (`works/<id>/index.html`, 2026-06-16; +talia-zoref-2/3 ב-2026-06-23). **טמפלט data-driven:** `<html data-artwork-id>` + `#artwork-data` inline (מירור compact של רשומת `art_works[]` המתאימה). שני עמודות דסקטופ (תמונה contain משמאל / מידע מימין), מובייל column (תוקן באג `.aw-media{flex:1 1 0}` שהתמוטט לגובה 0 והתמונה גלשה מעל הטקסט → `flex:0 0 auto`; כפתורים מיושרים ימין `justify-content:flex-start` תחת row-reverse). תמונת היצירה = **לייטבוקס** (click→`artwork-lightbox.js`, `[data-artwork-src]`). כפתורי **share** (data-share-btn) ו-**contact us** (mailto לקורין אברהם — אוצרת כל התערוכות, גוף ממולא מראש). מטא: כותרת, שם אומן (→דף אומן), פרטים, גלריה (→`/#galleries`); **ה-divider+סקשן תערוכה מותנים** — מרונדרים רק אם יש `exhibition_title_he`/`statement_he`. שם הגלריה בשורה אחת (בוטל פיצול כפוי). **קישור מהגריד:** כל הרשומות עכשיו `page:true` → כל כרטיס ב-`/works/` הוא `<a href="<id>/">` (אין יותר לייטבוקס בגריד; הלייטבוקס עבר לעמוד היצירה). **שדות אופציונליים ב-`art_works[]`:** `page`, `exhibition_title_he`, `exhibition_slug`, `exhibition_route`, `statement_he[]`. **גנרטור:** `/tmp/gen_artwork_pages.py` (לא בריפו) — קורא `works.json`, מסמן `page:true` לכולן, מסנכרן `#works-data` + 34 מירורי `__ART_WORKS_INLINE__`, ומרנדר עמוד לכל id מהתבנית (`works/zohar-ron-5/index.html`). עמוד/יצירה חדשים: הוסף ל-`works.json` והרץ מחדש. דפי האומן מרנדרים כרטיסי יצירה כלייטבוקס (מתעלמים מ-`page`) — לא הושפעו. |
| `/galleries/` | Galleries index | — | — | ⏳ אין עיצוב; בפועל `#galleries` בהומפייג' משמש אינדקס (הכרטיסים קליקאביליים → דפי הגלריות מ-2026-08-12) |
| `/galleries/:slug/` | Single gallery (medina + dizengoff) | medina `XhGH...::1468:305` / dizengoff `XhGH...::1473:311` | medina `XhGH...::1467:840` / dizengoff `XhGH...::1473:447` | ✅ `galleries/medina/index.html` + `galleries/dizengoff/index.html` (2026-08-12, workflow רב-סוכני; ⚠️ שמות ה-frames "event-hmp1-*" משקרים — זוהו לפי רוחב+תוכן. NOT `1213:2725/2820/2854` — אלו lightbox states). **מבנה (זהה בשני הדפים):** hero אפור `#EEF0EF` (שם עברי FbEzmel 80/32 + כתובת EN Copperplate + שעות RTL עם נקודות 4px + צילום; מובייל: צילום full-bleed 390×520 ואז בלוק לבן) → `gallery exhibitions` → `the artworks` → `the gallery manager`. **תמונות hero:** מדינה = צילום חלון-ראווה חדש `images/galleries/medina/page-hero.webp` (imageRef `281cf784…` crop `10b0bd`; שדה `image_page_hero` — נפרד מ-`image_hero` של כרטיס ההומפייג'); דיזינגוף = מיחזור `hero-v2` (ה-imageRef `c937cf2f` שבפריימי דיזינגוף = רקע מכוסה-לגמרי, leftover של מדינה; ה-STRETCH הלא-אחיד של הפיגמה לא שוחזר — cover). **דאטה-דריבן:** פרטי הגלריה fetch-first מ-`data/galleries.json` + מירור `#fallback-galleries` (דפוס עמוד האוצרת); תערוכות ממירור `#g-exhibitions-data` (slice של `exhibitions.json`) — **סינון לפי `gallery_id` בלבד, בלי סינון סטטוס/תאריך בשני הדפים** (מציג גם ארכיון — per Figma מדינה שמציגה את בדידות; כרטיס לא נעלם כשהתערוכה מסתיימת), מיון `start_date` יורד + שורה RTL = החדשה מימין; כרטיס → `exhibitions/<slug>/`. תאריכי אמת נוספו ל-`exhibitions.json` (2026-08-12): loneliness 19.01-20.05, how-many 26.05-15.08, the-peeler 29.07-15.09.2026; תאריכי הכרטיס מוצגים התחלה→סיום ב-`direction:ltr` (היפוך ה-bidi שברינדור הפיגמה לא שוחזר). **🔴 the artworks (הכרעת משתמש 2026-08-12 — סטייה מכוונת מהפיגמה):** במקום 3 צילומי-החלל שבפריימים — קרוסלת `.tri` (הקומפוננטה המשותפת, בלי dots/חיצים) של **כל יצירות הגלרייה** ממירור `#g-artworks-data` (slice של `works.json art_works[]` לפי `gallery_slug`, סדר המערך הקנוני: דיזינגוף 43, מדינה 98): `object-fit:contain` על `#F9F9F9` (לא חותכים אומנות), כיתוב `tri-caption` = `artist_en` → דף האומן (כלל-זהב 10), קליק על המרכז → `works/<id>/`; **שקופיות נטענות בהדרגה** (hydration של ±2 סביב האינדקס דרך `tri:change` — 98 תמונות לא יורדות בבת אחת). צילומי החלל (`interior-*`, `artworks-*`) נשארו בדיסק לא-מקושרים (כלל-זהב 8). **הוספת יצירה ל-works.json = לסנכרן גם את מירורי `#g-artworks-data` בשני הדפים.** **the gallery manager:** נטלי זיגל (ישות `manager` ב-`galleries.json`, אותה מנהלת בשני הדפים), פורטרט משותף `images/galleries/managers/natalie-zigel.webp` 1080×1206, אימייל מוצג UPPERCASE + `mailto:` lowercase. **חיווט (2026-08-12):** כרטיסי `#galleries` בהומפייג' הפכו `<a>` לדפים (פשפשים נשאר article "coming soon"), קישורי הפוטר ב-`site-chrome.js` (מדינה/דיזינגוף) → הדפים, sitemap.xml עודכן. **פתוח (docs/todo.md):** "1 Reines st." (הוכרע 2026-08-13; בפיגמה נכתב בטעות Raines), פער Zigel/siegel באימייל המנהלת. flea-market: אין עיצוב → אין דף. OG: `og/galleries-<slug>-hero.jpg`. |
| `/exhibitions/` | Index | (כיום ארכיון בהומפייג') | — | ⏳ |
| `/exhibitions/loneliness/` | Single | `XhGH...::119:156` | `XhGH...::119:331` | ✅ `exhibitions/loneliness/index.html` |
| `/exhibitions/how-many/` | Single | `XhGH...::119:435` | `XhGH...::119:642` | ✅ `exhibitions/how-many/index.html` |
| `/exhibitions/the-peeler/` | Single (הקולפן — current, גלריית כיכר המדינה, אוצרת קורין אברהם) | `XhGH...::721:14300` | `XhGH...::721:14513` | ✅ `exhibitions/the-peeler/index.html` — **תבנית `how-many` data-driven** (אותו `tpl-page`, אין שכפול קוד). רשומה ב-`exhibitions.json` slug `the-peeler`: `status:current`, `volume_en:"VOLUME 2"` (verbatim מ-Figma — **שים לב:** כפול מול how-many; באישור המשתמש 2026-06-25), מכתב-אוצרת ארוך (`description_he`, פסקה ראשונה `heading-en` = "Welcome aboard."), **22 כרטיסי אומנים ב-strip (מ-2026-07-26, עם tali-zelnik — תואם "עשרים ושניים אמנים" שבמכתב)**. **חידוש renderer additive:** flag `hero_overlay_hide_en:true` (CSS `.ex-hero--overlay-hide-en .overlay .title-en{display:none}`) — אוברליי מציג רק title-bar + `title_he` (הקולפן), בלי הכותרת האנגלית, כמו Figma; `title_en:"The Peeler"` נשמר ל-`<title>`/OG/SEO בלבד. **תמונות:** hero desktop = **שכבות CSS** (Figma `721:14345`): `hero-base.{webp,avif}` (620×754 cover) + `hero-art.{webp,avif}` (1200×1600, crop `146%/128.73%`, offset `-20.88%/-14.41%`) + gradient `rgba(0,0,0,.7)`; `hero.{webp,avif}` = קומפוזיט flat ל-OG/JSON; overlay ממורכז (`top:50%`, Figma `721:14346`); **אין `<image-gallery>`** ב-hero. thumbs ל-20 אומנים = מיחזור `images/artists/grid/<slug>.{webp,avif}` (`focus:"50% 40%"`); hadas-tuval thumb ייעודי `images/exhibitions/the-peeler/artists/hadas-tuval.{webp,avif}` (רונדר מ-Figma node `980:175`). OG `og/exhibitions-the-peeler-hero.jpg`. **סדר ה-strip = Figma desktop `980:92` verbatim** (zohar-ron…michael-konovalenko, ואז hadas-tuval). **2 חריגים בהחלטת המשתמש (2026-06-25):** (1) ~~**tali-zelnik דולגה** — נשארת מוסתרת (`_staging`/404) עד חשיפה רשמית, לא ב-strip~~ **בוטל 2026-07-26 — נחשפה:** כרטיס `is-linked` נוסף בסוף ה-strip (אחרי hadas-tuval, עריכה ישירה ב-HTML הסטטי; thumb = `images/artists/grid/tali-zelnik`, focus `50% 40%`) + נוספה ל-`artists[]` ברשומת `exhibitions.json`; (2) ~~**hadas-tuval** = אומנית חדשה בלי דף/יצירות → כרטיס **לא-קליקאבילי**~~ **בוטל 2026-07-06:** hadas-tuval קיבלה דף אומנית מלא (Figma `797:106`) → הכרטיס שלה ב-strip הוסב ל-`<a class="card is-linked" href="../../artists/hadas-tuval/">` (עריכה ישירה ב-HTML הסטטי) + נוסף `slug` לרשומתה ב-`exhibitions.json`. **how-many נשארה current** (לא הוחלפה לארכיון — בקשת המשתמש). curator_slug=korin-avraham (נוסף `the-peeler` ל-`exhibition_slugs` ב-`curators.json`). **🔴 2026-06-25 — העמוד "הוקפא" לסטטי עצמאי (בקשת המשתמש, לחופש עיצובי מלא):** הוסרו ה-`<template id="tpl-page">`, שלושת ה-`fallback-*` JSON, וה-renderer IIFE. שלושת הסקשנים (hero/artists/curator) עכשיו **markup סטטי קבוע** בתוך `<main id="page">` (נלכד מפלט המנוע ברינדור, אז ויזואלית זהה 100%). **המשמעות:** תוכן (טקסטים/אומנים/תמונות) **כבר לא נמשך מ-`exhibitions.json`** — עריכה ידנית ב-HTML. הרשומה ב-`exhibitions.json` נשארה (לא מזיקה; לא מקור-אמת יותר לעמוד הזה). **עדיין components (לא קופאו):** `<site-header>`/`<site-footer>` (ריקים → `site-chrome.js` מרנדר; שמירה על nav/footer גלובליים). hero = `.hero-stack` סטטי (שכבות, לא gallery). `is-hero-slide-0` קבוע על `.img-frame` (אוברליי גלוי תמיד). הוסרה `is-loading-data` מ-`<body>`. **how-many/loneliness עדיין data-driven מהתבנית** — לא הושפעו. שינוי עיצובי/מבני לעמוד הזה = עריכה ישירה ב-`exhibitions/the-peeler/index.html`, בלי השפעה על אחרים. |
| `/curators/korin-avraham/` | Curator (Korin Avraham) | `XhGH...::314:4` | `XhGH...::314:97` | ✅ `curators/korin-avraham/index.html` |
| `/artists/` | All artists | `XhGH...::523:107` | `XhGH...::523:2` | ✅ `artists/index.html` — **גריד 33 אומנים** (3 עמ' דסקטופ / 2 מובייל), שמות דו-שורתיים (Copperplate, אפור #828282, רינדור UPPERCASE לפי כלל גלובלי — כמו רצועת "our artists" בהומפייג'). כותרת "THE artists" + פסקת מבוא. כל כרטיס → `/artists/<slug>/`. **סדר verbatim מ-Figma `523:145` (Frame 395) — 33 כרטיסים, אומת 1:1 (2026-06-11).** תמונות גריד `images/artists/grid/<slug>.{webp,avif}` (810²+480w, cover-square). Data inline ב-`#artists-grid-data`. `row-gap:32px` (Figma `gap-y`). **2 אומנים placeholder** (**dan-ben-ary, racheli-reuven**) = דפי placeholder עם תמונת גריד; `_pending_artist_pages` ב-`artists.json`. (8 הנותרים — gilad-kenan, jessica-tabarovsky, baruch-torgeman, chen-ziv, lahav-barak, livay-levi, natasha-zeriker, talia-zoref — קיבלו ביו+פורטרט מסקשן `artist-pages-קולפן` ב-2026-06-10.) **2026-06-11:** **gal-rotem (`613:281`) + raz-ronen (`613:285`)** נוספו לסוף הגריד (nodes חדשים ב-Figma); **קיבלו דף אומן מלא** (ביו+פורטרט+יצירות, ראו שורת `/artists/:slug/`). **2026-07-26:** tali-zelnik נחשפה ונוספה לסוף הגריד (פריט 41, אחרי hadas-tuval; תמונת גריד כבר הייתה ב-`images/artists/grid/`). |
| `/artists/:slug/` | Single artist (×42) | `Zn3N...::1213:3` (alon = canonical) | `Zn3N...::1213:3672` (`artist-pages`: 13×390) | ✅ **🔴 2026-08-12 — סדר קבוצות התערוכה: התערוכה החדשה למעלה (הכרעת משתמש).** `buildExGroups` ממיין את הקבוצות לפי `exRank(exhibition_slug)` יורד, כאשר הדירוג = **מיקום התערוכה ב-`data/exhibitions.json`** (המערך שם כרונולוגי **עולה**, ולכן אינדקס גבוה = תערוכה חדשה יותר). קבוצה בלי תערוכה מוכרת מקבלת ‎-1 ויורדת לסוף; שוויון דירוג (למשל שתי קבוצות של הקולפן) שומר על סדר ה-`artist_page_pos`, כך שהפיגמה עדיין קובעת בתוך תערוכה. הדירוג מוזרק כמירור inline רביעי — `<script id="ex-order-inline">window.__EX_ORDER_INLINE__` — ע"י `tools/sync_works_mirrors.py` (file:// לא יכול fetch). **תערוכה חדשה = להוסיף ל-`exhibitions.json` בסוף המערך ולהריץ את הסקריפט.** נכון להיום הסדר בפועל לא השתנה באף דף (ה-`artist_page_pos` שנגזר מהפיגמה כבר היה חדש→ישן ב-8 האומנים הרב-תערוכתיים) — השינוי הופך את זה מצירוף מקרים לכלל. אומת גם בבדיקה פונקציונלית: הזרקת דאטה שבה how-many קודם עדיין רינדרה את הקולפן ראשון. **2026-07-15 — תוקן renderer בכל 42 הדפים (+`_staging`):** הבדיקה `__ARTISTS_INLINE__.length` נכשלה כי המירור הוא אובייקט `{artists:[...]}` ולא מערך → ב-file:// כל דפי האומן נפלו ל-fetch שנכשל ("ARTIST NOT FOUND"); ב-HTTP עבד ולכן לא נצפה. עכשיו הבלוק מפרק `.artists` לפני הבדיקה. ראה lessons 2026-07-15. **2026-07-06 — hadas-tuval (אומנית חדשה, `XhGH...::797:106` מובייל):** דף מלא — ביו+פורטרט+2 יצירות בקולפן (ראה שורת `/works/`); קבוצת הקולפן בלי סטייטמנט. 23 עם תוכן figma + 2 placeholder (dan-ben-ary, racheli-reuven). **2026-06-24 — tali-zelnik (אומנית חדשה, `XhGH...::854:1040` מובייל):** דף מלא (**2026-07-06: נוסף ביו** מ-Figma `854:1071` → `bio_he` ב-`artists.json`, "צלניק"→"זלניק" תוקן גם בגוף הטקסט; **+רשומת `exhibition_statements`** לתערוכת הקולפן מ-Figma `854:1083` — הופץ עם `sync_works_mirrors.py` + regen `__ARTISTS_INLINE__` ב-41 דפים + סנכרון ידני ל-3 דפי `_staging`. במקור נבנה בלי ביו — Figma="חסר מידע" → `bio_he:null`) + פורטרט (`images/artists/tali-zelnik/portrait.{webp,avif}`, נחתך מ-Profile) + 2 יצירות בתערוכת **הקולפן**/גלריית המדינה (tali-zelnik-1 "ענבר"/inbar, tali-zelnik-2 "סלע"/sella — גופי תאורה מקרמיקה וינטג'). **🔴 בקשת אמנית — תצוגת שם מיוחדת:** שם מלא בשורה אחת (`.first`, bold) + שם הסטודיו בשורה השנייה/שלישית (`.last`, light) — כמו samy d. אך מנגנון אחר: שדה חדש `studio_en` ב-`artists.json` ("piece of mine studio"); `heroNameParts` ב-**דף טלי בלבד** מחזיר `{first:name_en, last:studio_en}` כש-`studio_en` קיים; CSS `.first{white-space:nowrap}` (שם לא נשבר) + `.last{white-space:normal}` (סטודיו גולש). שני תיקוני renderer נוספים בדף טלי בלבד: בדיקת placeholder סופרת גם `art_works[]` (`_richCount`) כך שאומן עם יצירות בלי ביו לא נופל ל-placeholder; `bioHTML=""` כשאין `bio_he` (אין divider/עמודת ביו ריקה). instagram `@pieceofmine.studio`. **2026-07-06 — שם הסטודיו גם בכרטיסי היצירות** (בקשת אמנית, Figma `854:1085`): `artist_he` של tali-zelnik-1/2 ב-`works.json` = "טלי זלניק | סטודיו פיס אוף מיין סטודיו" (שינוי דאטה בלבד, בלי נגיעה ב-renderer — השורה מעל כותרת היצירה בכרטיס נמשכת מ-`artist_he`); הופץ עם `sync_works_mirrors.py` + עדכון ידני ל-3 דפי `_staging` (הסקריפט לא נוגע בהם). **שם עברי = "טלי זלניק" (כבקשת המשתמש; ב-Figma נכתב בטעות "טלי צלניק"). ✅ נחשפה במלואה 2026-07-26 (בקשת המשתמש):** 3 הדפים הוחזרו מ-`_staging/` לעץ המוגש, הוסרו `hidden` מ-`artists.json`+`works.json` (הורץ `sync_works_mirrors.py` + regen `__ARTISTS_INLINE__` בכל 42 הדפים), הכרטיס חזר לסוף `#artists-grid-data` (פריט 41), נוסף כרטיס ל-strip של `exhibitions/the-peeler/` (22 כרטיסים) + לרשומה ב-`exhibitions.json`, ו-3 URLs נוספו ל-sitemap.xml. robots היה כבר `index,follow` (בניגוד לתיעוד הישן — לא נדרש שינוי). **התוכן אומת מול Figma `854:1040` ב-2026-07-26** (ביו 3 פסקאות, סטייטמנט, שני כרטיסי היצירות + פרטים, אינסטגרם — הכל תואם; ענבר=pos1, סלע=pos2 לפי קונבנציית קריאת-LTR של lessons 2026-07-15). מנגנוני ההסתרה (סינון `hidden` בגריד works, תמיכת `_staging`) נשארו בקוד לשימוש עתידי. **תיעוד היסטורי — 🔒 מוסתרת לגמרי עד חשיפה (2026-06-24):** 3 הדפים **הוצאו מהעץ המוגש** ל-`_staging/` (gitignored, לא מתפרסם ב-GitHub Pages) — `_staging/artists-tali-zelnik/`, `_staging/works-tali-zelnik-1/`, `_staging/works-tali-zelnik-2/`. **מבקר ב-`/artists/tali-zelnik/` או בעמודי היצירה מקבל 404 אמיתי** (הקובץ חסר → GitHub Pages מגיש `404.html`). בנוסף (defense-in-depth, נשאר גם אחרי החזרה): `hidden:true` ב-`artists.json` (tali) וב-`art_works[]` (tali-zelnik-1/2); הוסרה מ-`artists/index.html` `#artists-grid-data`; גריד `works/index.html` מסנן `items.filter(w=>!w.hidden)`; 3 הדפים (ב-`_staging`) עם `robots:noindex,nofollow`. התמונות (`images/works/v2/tali-zelnik-*`, `images/artists/tali-zelnik/*`, גריד) נשארו ב-repo אך לא מקושרות. **חשיפה:** (1) `mv _staging/artists-tali-zelnik artists/tali-zelnik` + `mv _staging/works-tali-zelnik-1 works/tali-zelnik-1` + `mv _staging/works-tali-zelnik-2 works/tali-zelnik-2`; (2) הסר `hidden` מ-`artists.json`+`works.json` (2 יצירות) → הרץ `sync_works_mirrors.py` + regen `__ARTISTS_INLINE__`; (3) החזר את הכרטיס ל-`#artists-grid-data`; (4) הפוך robots ל-`index,follow` ב-3 הדפים. **2026-06-11 — gal-rotem (`XhGH...::605:7711`) + raz-ronen (`XhGH...::605:7995`):** קיבלו דף מלא — ביו (`bio_he`) + פורטרט ייעודי (`images/artists/<slug>/portrait.{webp,avif}`, נחתך מתמונת ה-Profile של הפריים) + סקשן יצירות (gal-rotem-1 / raz-ronen-1,2 שכבר היו ב-`art_works[]`). ב-Figma raz הוצגו 4 כרטיסים אך 2 מהם placeholder ("חסר שם בינתיים"/"חסר מידע") — פורסמו רק 2 היצירות עם מטא אמיתי (שתיהן נמכר). ה-handle בפיגמה לשניהם = leftover (`@samyd`/`@rachelli_reuven`) → `instagram_handle:null`. **באותו תיקון:** רונדר מחדש `__ARTISTS_INLINE__` בכל 34 הדפים מ-`artists.json` עם `json.dumps` — תיקן באג שבו הזרקת ביו קודמת הותירה **תווי שורה גולמיים בתוך מחרוזות JS** (שבר את `__ARTISTS_INLINE__` ב-file:// לכל האומנים עם ביו; HTTP עבד דרך fetch fallback). **2026-06-10 — סקשן `XhGH...::560:3384` (`artist-pages-קולפן`, 14 frames מובייל 390):** 8 דפי placeholder קיבלו ביו (`bio_he`) + פורטרט (`images/artists/<slug>/portrait.{webp,avif}`) — jessica-tabarovsky (`560:4910`; ל-`560:4678` יש variant ישן עם leftover של hila-loterstein), chen-ziv (`560:5552`), baruch-torgeman (`560:5637`), gilad-kenan (`560:5857`), lahav-barak (`560:5967`), livay-levi (`560:6036` — ⚠️ ה-Profile Text בפיגמה אומר בטעות "lahav barak", הביו והיצירות של ליוואי לוי), natasha-zeriker (`560:6159`, +אינסטגרם), talia-zoref (`560:6232`); וביו עודכן לתוכן הקולפן אצל noemi-safir (`560:4741`), elsa-ars-brush (`560:5350`), anat-wegier (`560:5792`), nir-giorgio-levin (`571:6354`, +7 יצירות). yarden-amir (`561:1154`) — ללא שינוי תוכן. בביו של chen-ziv הושמטו 2 פסקאות leftover על "התנערות" (שייכות לג'סיקה). Each `artists/<slug>/index.html` is a copy of the shared template with `data-slug` set on `<html>`. הרנדרר מציג placeholder hero (שם+פורטרט) כש-`bio_he=null` ו-`works=[]`. `__ARTISTS_INLINE__` (מירור inline של `artists.json` בכל דף — file:// לא fetch) חייב לכלול את ה-slug; הוא זהה בכל הדפים — בעת הוספת אומן ל-`artists.json` הרץ regen על כל `artists/*/index.html`. **The Artworks section (2026-06-08): כרטיסי גריד בסגנון עמוד `/works/` — 2 עמודות גם במובייל (היה עמודה אחת), מטא = שם אומן + כותרת + פרטים + נמכר + גלריה.** מקור: ל-30 האומנים שמופיעים ב-`works.json art_works[]` (102 יצירות נכון ל-2026-06-10, כולל racheli-reuven-1) הכרטיסים נמשכים משם (כותרות דו-לשוניות/נמכר/גלריה אמיתיים, תמונות `images/works/v2/`) דרך מירור inline `__ART_WORKS_INLINE__`; לשאר — `artists.json works[]` (שם+כותרת+medium). **כל הקבצים זהים פרט ל-`data-slug` → ערוך אחד והפץ.** **2026-06-23 — תערוכת הקולפן (6 frames מובייל `landing`):** 4 אומנים חדשים מלאים (ביו+פורטרט+יצירות) — **michael-konovalenko** (`798:374`), **maria-artamonova** (`798:240`), **alice-debellis** (`849:963`), **amnon-lipkin** (`798:173`); כולם `the-peeler`, גלריית המדינה. **aharon-bas** (`781:9`) + **bar-cohen** (`798:307`) — בעבר היצירה המשותפת `aharon-bas-1` "ניגודים" הוצגה כ-collab אחד עם title/details per-artist. **2026-06-24 — פוצלה לשתי יצירות עצמאיות, בלי קרדיט-שיתוף:** `aharon-bas-1` "ניגודים"/"שירה" (אהרן בלבד) + רשומה חדשה `bar-cohen-1` "במקרה"/"85x266 ס\"מ חוט כותנה על בד קנבס" (בר בלבד, אותה תמונה משוכפלת `images/works/v2/bar-cohen-1.*`, עמוד בודד `works/bar-cohen-1/`). הוסרו כל שדות ה-collab (`artist_pages`/`collab`/`collab_connector_he`/`title_he_by_artist`/`details_he_by_artist`). הסטייטמנטים נשארו נפרדים ב-`exhibition_statements` (לכל אומן שלו). bar-cohen instagram = `@akra.studioz`. הפורטרטים נחתכו מ-Profile של כל frame; טמפלט הדף = `aharon-bas` (renderer עם ex-groups). |
| `/press/` | Press & events index (ארכיון מלא) | `XhGH...::1323:244` (⚠️ שם ה-node "press/events-phone" — משקר, זה ה-1440) | `XhGH...::1323:528` | ✅ `press/index.html` (2026-08-04, רה-דיזיין 2026-08 פרומפט 2) — hero אפור `#EEF0EF` (מובייל לבן) עם כותרת מוערמת "press"(Bold)/"& events"(Light), כיול 80→60px (46 טאבלט / 27 מובייל — זהה ל-`events/index.html`). **21 כרטיסים סטטיים ב-HTML** (לא JS), כרונולוגי חדש→ישן; דסקטופ = גריד `grid-auto-flow:column` + `direction:rtl` על `grid-template-rows:repeat(11,auto)` → המחצית הראשונה יורדת בעמודה הימנית והשאר בשמאלית (= פריסת ה-Figma, שתי ריצות כרונולוגיות); מובייל = flex עמודה אחת (= סדר ה-DOM). **⚠️ הוספת כרטיס = לעדכן את מספר השורות ל-`ceil(n/2)`**, אחרת נוצרת עמודה שלישית. כרטיס = תג chip `#EEF0EF` (אנגלית Copperplate Light 11.5, עברית FbEzmel 14) + כותרת (EN 13px / HE 16px) + תאריך 11.5 (או "פרטים בהמשך" FbEzmel 14 — zohar-ron) + תמונה 292×162 (מובייל 171×108). **סטיות מהפריים (מוכרעות):** +alice-debellis 6.8 (הושמטה בפיגמה — תא ריק בגריד), +the-space-between 3.9 בראש (מאוחר לפריים), ktuba = "כְּתוּבָּה \| QTUBA" (במובייל-פיגמה כותרת שגויה), zohar-ron = "פרטים בהמשך" (19.7 שבמובייל מיושן), תג timeout הישן = "time out \| תרבות". **close-look = אוברליי חי** (scrim `rgba(27,27,27,.3)` + Copperplate Bold 14/FbEzmel 16 לבן, `direction:ltr` על ה-EN!) מעל צילום נקי חדש `images/events/close-look/card-photo.{webp,avif}` (584×324, קרופ מרכז מ-imageRef `6658a272…`); פורטפוליו קיבלה `images/press/press-1/00-home-card-v2.{webp,avif}` (584×370 — הישנה 171px בלבד). tal-nehoray = `--soon` + טוסט "בקרוב". שאר 18 התמונות ממוחזרות (אומת RMSE מול קרופי הפריים). **nav גלובלי עודכן:** "press & events" ב-`site-chrome.js` → `press/` (היה `/#press`). **2026-08-06:** כרטיס zohar-ron קיבל תאריך אמיתי **10.8.2026** (Figma עודכן) והוזז למקומו הכרונולוגי — שני, בין the-space-between ל-anat. אין יותר "פרטים בהמשך" בעמוד (ה-CSS `date--tbd` נשאר לשימוש עתידי). **2026-08-12 — +כרטיס `events/gala-night/`** (29.7.2026, בין alice-debellis 6.8 ל-risa-and-noemi 24.7; אנטומיה לפי Figma `1323:570` — תג `gallery event`, כותרת "gala night - הקולפן" עם `<span class="heb">`, תמונת `card.webp` 584×369 שנאפתה מה-hero עם קרופ ה-Figma). מספר הכרטיסים/השורות מתעדכן באותו יום גם ע"י אירועי noemi-safir/hadas-tuval המקבילים — הכלל הקבוע: `grid-template-rows = ceil(n/2)`. **🔴 2026-08-12 — העמוד הפך ל-ארכיון עבר בלבד (בקשת המשתמש):** אירועים שתאריכם **היום או אחריו נושרים בזמן ריצה** — הם חיים ב-`/events/`. מימוש: לכל כרטיס-אירוע נוסף `data-kind="event" data-date="YYYY-MM-DD"` (כרטיסי עיתונות **בלי** `data-kind` ולכן אף פעם לא מסוננים), ו-IIFE קצר לפני טוסט ה"בקרוב" מוחק את הכרטיסים העתידיים ומחשב `--pc-rows = repeat(ceil(נותרו/2),auto)`. הפילטר הוא **המשלים המדויק** של `date >= today` ב-`events/index.html` — אירוע עובר מ-`/events/` ל-`/press/` מעצמו למחרת. ה-`grid-template-rows:repeat(13,auto)` הסטטי נשאר **רק כ-fallback ללא-JS** (= ceil של כל 25 הכרטיסים; **הוספת כרטיס = לעדכן אותו**). הכרטיסים העתידיים **נשארים ב-markup וב-`press.json`** — רק מוסתרים, כדי שיופיעו לבד כשיעבור התאריך. |
| `/press/walla/` | Article | `XhGH...::119:740` | `XhGH...::119:868` | ✅ |
| `/press/press-1/` | Article (קורין אברהם) | `XhGH...::119:972` | `XhGH...::119:1163` | ✅ |
| `/press/time-out/` | Article (time out — "דה פיינל קאונטדאון", 31.12.25 — **הכתבה הישנה**) | `XhGH...::119:1340` | `XhGH...::119:1433` | ✅ |
| `/press/the-last-station/` | Article (time out — "התחנה האחרונה: 25 תערוכות", 27.5.26 — **הכתבה החדשה**, על תערוכת how-many) | `XhGH...::412:1118` | `XhGH...::412:1214` | ✅ `press/the-last-station/index.html` — מבוסס על תבנית `press/time-out/`. תמונת גיבור = יצירת **אלסה ארס ברוש** (cover, `images/press/the-last-station/`, imageRef `2e374464…`). קפשן + כותרת הסקשן (HOW MANY PARTNERS HAVE YOU HAD? → `/exhibitions/how-many/`) מקושרים. "לכתבה המלאה" → timeout.co.il חיצוני. כרטיס הומפייג' `#press` (412:583/996) הופנה מקישור חיצוני → לדף הפנימי. JSON: `press.json` id `time-out` (route עודכן ל-`press/the-last-station/`, `exhibition_id:how-many`). ⚠️ headings מעורבי-כיוון: `unicode-bidi:plaintext` + `min-width:0` על ילדי הפלקס (מונע flex min-content overflow בנייד צר). |
| `/press/the-sixth-scent/` | Article (ora magazine — interview w/ Erez, **English long-form**) | `XhGH...::545:1669` | `XhGH...::545:1762` | ✅ `press/the-sixth-scent/index.html` — ראיון אנגלי, Copperplate UPPER (גובר על `textCase:LOWER` ב-Figma, conventions §5). 5 תמונות `images/press/the-sixth-scent/`. נוסף לגריד הומפייג' `#press` ככרטיס הכי חדש (7.6.2026, top-right; כרטיס `551:372`, גריד מלא `551:371`). |
| `/press/manicure-against-darkness/` | Article (time out — "מניקור נגד האפלה וגוף אנושי שמתפרק: 18 תערוכות לסופ״ש", 1.7.26 — על תערוכת **הקולפן**) | `XhGH...::1051:291` | `XhGH...::1051:396` | ✅ `press/manicure-against-darkness/index.html` — תבנית `the-last-station`, אבל **סדר הפוך**: סקשן הגוף (h2 "הקולפן // גלריה ארז זילינסקי רוזן" + פסקה) **מעל** תמונת הגיבור, לפי Figma. תמונת גיבור = יצירת **אלסה ארס ברוש** (`images/press/manicure-against-darkness/hero.*`, 1000×1336, imageRef `37edc008…`). ב-Figma הקפשן כתוב "אלסה ארס ברוס" — תוקן לכתיב הקנוני **ארס ברוש**. "הקולפן" → `/exhibitions/the-peeler/`, כתובת הגלריה → `/#galleries`, "לכתבה המלאה" → timeout.co.il. |
| `/press/peeling-a-layer/` | Article (רשת 13 — "מה נפגוש כשנקלף שכבה מהזיכרון שלנו? לתערוכה הבאה יש תשובה", 1.7.26 — על תערוכת **הקולפן**) | `XhGH...::1051:26` | `XhGH...::1051:154` | ✅ `press/peeling-a-layer/index.html` — 3 תמונות ב-`images/press/peeling-a-layer/` בשלוש פרופורציות שונות: `01-hitnaarut` (`.img-wide`, 600/338 דסקטופ / 358/201 מובייל, "התנערות" — ג׳סיקה טברובסקי, מקושרת ל-`works/jessica-tabarovsky-1/`), `02-ishur` (`.img-tall`, 600/848 דסקטופ, "אישור" — ענת ויז׳ייה), `03-blown-heart` (ברירת מחדל 600/909.62 — ירדן אמיר). המקור (`רשת 13`) בעברית → `.src.src-heb` (FbEzmel, לא Copperplate). פסקת הפתיחה + שמות האומנים/האוצרת ב-Regular (`.body-text strong{font-weight:400}`), כולם מקושרים (זוהר רון, ג׳סיקה טברובסקי, נעמי ספיר, חן זיו, ברוך תורג׳מן, ענת ויז׳ייה, קורין אברהם). "The Art Gallery by Erez Zielinski Rozen" = `<span class="lat">` → הומפייג'. |
| `/press/the-shared-list/` | Article (פורטפוליו — "הרשימה המשותפת // 25.6.26", 25.6.26 — roundup תערוכות שכולל את **הקולפן**) | `XhGH...::1537:2880` (⚠️ שם ה-frame "press-1-desktop" משקר — כתבה חדשה, זוהתה לפי רוחב 1440) | `XhGH...::1537:3091` ("press-1-mobile") | ✅ `press/the-shared-list/index.html` (2026-08-18) — תבנית `peeling-a-layer` + `.body-section-h` של manicure. מבנה: head (מטא "פורטפוליו \| אמנות", כותרת 48→36, תת-כותרת רשימת התערוכות עם `Startup` ב-`<span class="lat">`, byline = תאריך בלבד, בלי מחבר) → **גוף מעל התמונות** (h2 "הקולפן בגלריה זלינסקי רוזן" + 2 פסקאות) → 3 פיגורות 600/909.62 (358/542.74 מובייל) → divider → "לכתבה המלאה" → prtfl.co.il/archives/254898. **כל 3 התמונות = מיחזור `images/works/v2/`** (אומת RMSE ≤0.2% מול ה-imageRefs): `zohar-ron-5` ("דג 1"), `nir-giorgio-levin-8`, `elsa-ars-brush-1` ("חלום השרף") — אפס תמונות חדשות; OG = מיחזור `og/works-v2-zohar-ron-5.jpg`. כתיב "זלינסקי רוזן" (בלי י׳) + "20 אמנים" = verbatim מהכתבה המקורית (לא תוקן). קישורים: הקולפן (תת-כותרת+h2+גוף) → `exhibitions/the-peeler/`, הגלריה בגוף → `galleries/medina/`, קורין אברהם → דף האוצרת, 3 האומנים בקפשנים מקושרים. כרטיסים: `/press/` (27 כרטיסים → fallback `repeat(14,auto)`) + גריד `#press` בהומפייג' (13 כרטיסים → `repeat(7,auto)`, בין manicure ל-ora); `press.json` `portfolio-the-shared-list` + `homepage.json item_ids` + sitemap.xml. |
| `/events/` | Upcoming events index | `XhGH...::1318:1728` | `XhGH...::1318:2012` | ✅ `events/index.html` (2026-08-04, רה-דיזיין 2026-08 פרומפט 1) — hero אפור `#EEF0EF` (דסקטופ; מובייל לבן) עם כותרת מוערמת "upcoming"(Bold)/"events"(Light) צמודה ימין — **כיול אופטי מכויל-מדידה: 80 Figma→60px קוד, 46 טאבלט, 36→27 מובייל** (אומת מול רינדור ה-frame, ±2%). גריד 2 עמודות (gap 48) / עמודה במובייל (gap 24). כרטיס = `<a>` row: עמודת טקסט צמודה-ימין `space-between` (תג chip `#EEF0EF` padding 4/8 "gallery event \| kikar hamedina"/"dizengoff square" — **במובייל שתי שורות בלי pipe** (`.sep{display:none}`), כותרת `title_en` 13px (כיול מ-16), תאריך `d.m.yyyy` 11.5px או "פרטים ותאריך בהמשך" FbEzmel 14 ל-`date_tbd`) + תמונה (דסקטופ אספקט `292/162.25`, מובייל `171×108` קבוע). **הכרטיסים מרונדרים ב-JS ממירור inline `#events-list-data`** (מירור של `events.json`; file:// לא fetch): מסנן `date>=היום` או `date_tbd`, ממיין עולה (TBD בסוף = סדר ה-Figma הדסקטופי, הקנוני) — אירועים שעברו נושרים לבד; 0 אירועים → `#events-none` (הודעה עברית). **אירוע חדש = הוסף ל-`events.json` וגם למירור כאן** (+`list_image` אם יש קרופ ייעודי). תמונות כרטיס: `list_image` (שדה חדש) — **רינדורי-node אפויים** `images/events/<slug>/list-card.{webp,avif}` (876×487, מ-nodes `1318:3242/3216/3229`) ל-alice/zohar/amnon; anat ממחזרת את `card.webp` הקיים (קרופ זהה, RMSE 0.9%); the-space-between (3.9, נוסף תוך-כדי מסשן מקביל) = `hero-480w` עם cover. nav "press & events" עדיין →`/#press` (ישתנה ל-`/press/` בפרומפט 2). **🔴 2026-08-06 — סנכרון מול הפריימים המעודכנים:** (1) **סדר דסקטופ = זרימת עמודות RTL** — `grid-auto-flow:column`+`direction:rtl` על `.ev-list` (אותו טריק כמו `/press/`), ה-JS קובע `--ev-rows=repeat(ceil(n/2),auto)`; הכרונולוגיה יורדת בעמודה הימנית ואז בשמאלית (Figma `1318:1761`: col2=alice/anat/zohar-דיזינגוף, col1=amnon/zohar-medina); מובייל = עמודה אחת באותו סדר עולה (`1318:2012`). (2) **תאריכים נקבעו:** zohar-ron 10.8.2026, amnon-lipkin 8.9.2026 — אין יותר `date_tbd` במירור (הענף ברנדרר נשאר לעתיד). (3) **אירוע חדש בלי דף: `zohar-ron-medina`** — "artist talk with zohar ron" בכיכר המדינה 9.9.2026 (כרטיס `1318:3218` דסקטופ / `1403:999` מובייל; תמונה imageRef `951e06ff…` — צילום דג של זוהר, נאפתה `images/events/zohar-ron-medina/list-card.{webp,avif}` 876×487): רשומה ב-`events.json` עם `route:null`, במירור `soon:true` → כרטיס לא-קליקאבילי + טוסט "בקרוב" (`.ev-card--soon`, דפוס /press/). the-space-between (3.9) חסר בפריימים אך נשאר ברשימה (אירוע חי, data-driven; משתבץ כרונולוגית בעמודה השמאלית). |
| `/events/ktuba/` | Event (zohar ron live art) | `XhGH...::119:1651` (canonical 2026-05-11; legacy `Zn3N...::1213:1644`) | `XhGH...::119:1509` (legacy `Zn3N...::1213:1502`) | ✅ (2026-08-18: סליידר הצילומים מחווט ל-artwork-lightbox — `data-tri-lightbox`, title "live art performance") |
| `/events/loneliness/` | Event (opening) | `Zn3N...::1213:1873` | `Zn3N...::1213:2099` | ✅ (2026-08-18: לייטבוקס על שלושת הסליידרים — invitation tri "the invitation", גלריה שנייה "the atmosphere", moments grid "moments from the opening") |
| `/events/close-look/` | Event (מבט מקרוב — open meeting, tanya shin, how-many) | `XhGH...::420:1520` | `XhGH...::420:1625` | ✅ `events/close-look/index.html` |
| `/events/artist-talk/` | Event (artist talk — gal pollak + elsa ers brosh, how-many, 11.6.26 11:00, גלריית כיכר המדינה) | `XhGH...::818:159` | `XhGH...::818:17` | ✅ `events/artist-talk/index.html` — מבוסס על תבנית `close-look`. **כותרת ראשית באנגלית** (Copperplate UPPER, לא עברית כמו close-look). Hero **שונה בין breakpoints** (`<picture>` עם `media`): דסקטופ = חלון ראווה (6fb63f12), מובייל = מיצג קליגרפיה חיה (97bfe6a2). Overlay = "artist talk" + "how many partners have you had?". טריפטיך מ-6 תמונות `images/events/artist-talk/`. שני אומנים מארחים (gal-polk, elsa-ars-brush) — strip מ-portraits קיימים, חץ →`/exhibitions/how-many/`. הומפייג': כרטיס `#press` במקום **שני** (`event-how-many-artist-talk` ב-press.json + homepage.json). |
| `/events/how-many/` | Event (opening — how many, 15 artists, korin avraham) | `XhGH...::433:2` | `XhGH...::433:179` | ✅ `events/how-many/index.html` |
| `/events/natasha-zeriker/` | Event (save the date — נטאשה זריקר, **הקולפן**, 9.7.26, כיכר המדינה) | `XhGH...::1055:615` | `XhGH...::1055:519` | ✅ **משפחת עמודים חדשה (2026-07-09) — ראה הערה מתחת לטבלה.** ייחודי: כותרת `save the date`, גוף באנגלית (Copperplate), ו**כרטיס LINGER ממוסגר** (`.linger`, border 1px שחור) עם סטייטמנט עברי; פס תערוכה = `EXHIBITION • VOLUME 2` + `הקולפן` → `/exhibitions/the-peeler/`. |
| `/events/liel-salman/` | Event (**pop up tattoos** — ליאל סלמן נועם + סטודיו elline, how-many, 16.7.26, דיזינגוף) | `XhGH...::1055:927` | `XhGH...::1055:813` | ✅ ⚠️ שם ה-frame ב-Figma הוא "artist talk with liel salman noam" אבל **הכותרת הנראית = "Pop up tattoos"** — ה-slug נבחר לפי שם האומן (החלטת המשתמש). גיבור = `f14d30bd…` (שכבת `3f0a517d…` שמתחת מוסתרת). ספן לטיני `elline` בתוך העברית. הכרטיס ברצועה = "liel salman noam" verbatim, מקשר ל-`artists/liel-salman/`. |
| `/events/nir-giorgio-levin/` | Event (artist talk — ניר ג׳ורג׳יו לוין, how-many, 17.7.26, דיזינגוף) | `XhGH...::1083:1289` | `XhGH...::1083:1180` | ✅ סטייטמנט עברי בן 4 פסקאות; זמן `14:00-12:00` verbatim מ-Figma; כותרת עם רווח כפול "nir&nbsp; giorgio levin". |
| `/events/risa-and-noemi/` | Event (artist talk — ריסה ברוק עוז + נעמי ספיר, how-many, 24.7.26, דיזינגוף) | `XhGH...::1083:1543` | `XhGH...::1083:1432` | ✅ היחיד עם **שני** אומנים ברצועה (`risa-oz`, `noemi-safir`). אין הערת הרשמה. |
| `/events/zohar-ron/` | Event (artist talk — זוהר רון, SEX?, how-many, דיזינגוף) | `XhGH...::1083:1817` | `XhGH...::1083:1704` | ✅ ספן לטיני `SEX?` בתוך העברית; גיבור = `d64bdefda…` עם cropTransform (שכבת בסיס `9f6efde…` מוסתרת). **2026-07-23 — התאריך הוסר:** ה-Figma עודכן ל-**"פרטים בהמשך..."** במקום 19.7.26 (התאריך שונה, טרם נקבע). שורת התאריך/שעה במטא הוחלפה לטקסט זה; ב-`events.json` `date:null` + `date_tbd:true`, שדות `time_*` הוסרו. **🔴 2026-08-06 — התאריך נקבע (Figma עודכן, nodes `1394:660` דסקטופ / `1394:642` מובייל):** מטא = **10 אוגוסט 2026 · 20:30-19:00** (תבנית alice: date·dot·time); `events.json` — `date:"2026-08-10"`, `time_*` הוחזרו, `date_tbd` הוסר; עודכנו 3 ה-descriptions + JSON-LD `startDate`; כרטיס `/press/` קיבל 10.8.2026 והוזז למקומו הכרונולוגי (שני, אחרי the-space-between). **שים לב: בפיגמה קיים עכשיו אירוע zohar ron שני — כיכר המדינה 9.9.2026** (`zohar-ron-medina`, ראה שורת `/events/`) — אירוע נפרד, לא הדף הזה. |
| `/events/amnon-lipkin/` | Event (artist talk — אמנון ליפקין, ערב רישום ספונטני במכונת תפירה, **הקולפן**, כיכר המדינה) | `XhGH...::1145:1124` | `XhGH...::1145:1009` (נוסף בפיגמה, התגלה 2026-08-06) | ✅ **דף חדש (2026-07-23), משפחת "artist talk" (=natasha-zeriker: הקולפן/VOLUME 2/כיכר המדינה).** גוף עברי בן 3 שורות ("...אגיע עם הזינגר של סבתא..."), מטא = גלריה + ~~"פרטים בהמשך..." (אין תאריך; `date:null`+`date_tbd:true`)~~ **🔴 2026-08-06 — התאריך נקבע (Figma עודכן, nodes `1391:572` דסקטופ / `1391:624` מובייל): 8 ספטמבר 2026 · 20:30-18:30** (date·dot·time); `events.json` — `date:"2026-09-08"`, `time_*` נוספו, `date_tbd` הוסר, `figma_node_mobile:"1145:1009"`; עודכנו descriptions + JSON-LD `startDate`. פס תערוכה `EXHIBITION • VOLUME 2` + `הקולפן` → `/exhibitions/the-peeler/`. גיבור = imageRef `165831324d…` (**390×520** — הרזולוציה שהמעצב העלה; אין וריאנטים/srcset כי <800px). רצועת אומן בודד → `/artists/amnon-lipkin/` (ממחזר `images/artists/grid/amnon-lipkin`). `homepage_visible:false` (לא נוסף ל-`#press`/`#events-upcoming` — אין תאריך; אפשר בהמשך). |
| `/events/alice-debellis/` | Event (artist talk — אליס דה בליס + סדנת Chakra Union Spiral בשיתוף BENEDETTA/SolarLunar, **הקולפן**, 6.8.26, כיכר המדינה) | `XhGH...::1237:518` | `XhGH...::1237:185` | ✅ `events/alice-debellis/index.html` — תבנית משפחת artist-talk (בסיס natasha-zeriker, בלי LINGER ובלי הערת הרשמה). גוף עברי+לטיני מעורב (`.h-body.heb .lat` 13px אופטי), פס `EXHIBITION • VOLUME 2` + הקולפן (36px Light בסקשן presented — Figma; באוברליי 29px), אוברליי דסקטופ `top:48%`. גיבור = imageRef `671b231e…` crop `6d053e` — **המקור בפיגמה עצמו 238×1118 בלבד**; הקרופ (192×256) נמתח ל-984×1488 בדיוק כמו ה-STRETCH של Figma (אומת RMSE מול רינדור node `1237:580`; השכבה `165831…` מוסתרת מתחת). מחווט: `events.json`, `#events-upcoming-data` (קרוסלה), כרטיס `#press` (שורה 10 — משמאל ל-artist-talk, הפכה לזוג), `press.json` (`event-alice-debellis-talk`), `homepage.json`, sitemap.xml. |
| `/events/anat-wegier/` | Event (artist talk — ענת ויז׳ייה, סדנת העברת דימוי "הופכים היסטוריה לאמנות", **how-many**, 7.8.2026 11:00–13:00, דיזינגוף) | `XhGH...::1107:2090` | `XhGH...::1107:1981` | ✅ `events/anat-wegier/index.html` — משפחת artist-talk, וריאנט how-many (תבנית liel/risa: פס `the EXHIBITION` + ex-name אנגלי → `/exhibitions/how-many/`; בלי LINGER/הערת הרשמה — "ללא תשלום | בהזמנה מראש" בתוך הגוף). גיבור = imageRef `615a95e0…` (אותו מקור של `upcoming.*`) נאפה עם ה-crop/STRETCH מ-Figma ל-`hero.{webp,avif}` 984×1488 + וריאנטים 480/768 (אומת RMSE 0.38% מול רינדור node `1107:2003`); אוברליי `top:50%`. רצועת אומנית = `images/artists/anat-wegier/portrait.webp` (זהה ל-imageRef בפיגמה, אומת RMSE) עם `object-position:50% 50%`. חיווט: `events.json` (רשומה מלאה), `#events-upcoming-data` (הוסר `soon`, נוסף route), כרטיס `#press` (הוסר `--soon`), `press.json` route, sitemap.xml, OG `og/events-anat-wegier-hero.jpg`. |
| `/events/the-space-between/` | Event (**The Space Between** — ערב שיח עם **מור צופיה געש**, מעצבת פנים אורחת — לא אומנית של האתר; **ללא שיוך תערוכה**; 3.9.2026 יום חמישי, כיכר המדינה) | `XhGH...::1361:434` | `XhGH...::1361:232` | ✅ `events/the-space-between/index.html` (2026-08-04) — משפחת artist-talk, וריאנט מצומצם: **בלי** פס `presented as part of` (קונטיינרי התערוכה בפיגמה ריקים בכוונה), **בלי** רצועת אומנים, **בלי** הערת הרשמה. ייחודים: כותרת 4 שורות (80→64; `.dbr` = שבירות דסקטופ-בלבד — היפוך של `.mbr`); אוברליי hero = הכותרת עצמה (Bold 38.5→30, מובייל 30→23) ב**שליש העליון** (`top:27.2%` דסקטופ / `28%` מובייל); scrim דסקטופ **הפוך מהמשפחה** — `linear-gradient(180deg, rgba(0,0,0,.8), transparent)` (כהה למעלה; בפיגמה שכבות ה-scrim שקעו בטעות מתחת לתמונה — הוכרע ב-`docs/redesign-2026-08.md` §ב.5); מטא = תאריך `·` **יום־בשבוע** (בלי שעה; נקודה רק בדסקטופ) — שדות חדשים `day_he`/`host_he` ב-`events.json`; גוף עברי ממורכז 358px, השעות בתוכו. hero יחיד לשני breakpoints = imageRef `8ad5c38a…` (מור בגלריה, "ניגודים" של אהרן בס ברקע) cover ממורכז 984×1488 (אומת RMSE 0.5% מול רינדור `1361:498`); ה-fill `d64bdefda…` (crop `5bc24f`, ההירו של zohar-ron) = leftover מוסתר. חיווט: `events.json`, `#events-upcoming-data` (הקרוסלה; `ex_bar:[]`, `ex_name` = תת-הכותרת), `press.json` (**`homepage_visible:false`** — לפי רה-דיזיין 2026-08 האירוע לא בגריד `#press`; כרטיס בראש `/press/` כשייבנה), sitemap.xml, OG `og/events-the-space-between-hero.jpg`. |
| `/events/zohar-ron-medina/` | Event (artist talk — **קָמִיעַ** \| שיח גלריה עם הצלם **זוהר רון**, **הקולפן**, 9.9.2026 19:00–20:30, כיכר המדינה) | `XhGH...::1399:801` | `XhGH...::1399:691` | ✅ `events/zohar-ron-medina/index.html` (2026-08-06) — משפחת artist-talk, **וריאנט הקולפן הרגיל** (תבנית alice-debellis: פס `EXHIBITION • VOLUME 2` + הקולפן → `exhibitions/the-peeler/`, בלי LINGER/הערת הרשמה), רצועת אומן בודד. **🔴 ה-slug לפי הגלריה ולא לפי האומן** — `zohar-ron` תפוס ע"י אירוע ה-how-many/דיזינגוף שלו (כלל-זהב 6). ייחודים: (1) **שבירת כותרת שונה בכל breakpoint** — דסקטופ `artist talk` / `with zohar ron` (`.dbr`), מובייל `artist talk with` / `zohar ron` (`.mbr`); (2) **scrim שטוח `rgba(27,27,27,.4)` בשני ה-breakpoints** (לא הגרדיאנט המשפחתי) — בפיגמה זה fill על הרקטנגל בשני הפריימים, **ואומת שהוא באמת מרונדר** (ממוצע אפור 0.358 ברינדור מול 0.352 בחישוב מקומי; ⚠️ `%[fx:mean]` מכליל ערוץ אלפא — ראה lessons 2026-08-06); (3) מטא = תאריך+שעה ב-FbEzmel **Light** בדסקטופ / Regular במובייל (Figma), אוברליי ממורכז `top:50%`. גיבור = imageRef `951e06ff…` (צילום דג, מקור 1380×2048 בפרופיל Adobe98) → cover ממורכז 984×1488 + 480/768. thumb האומן = מיחזור `images/events/zohar-ron/artist-thumb.webp` (imageRef `889e3ab7`, RMSE 2.5%); ה-fill `36e2f912…` (אומנית בסטודיו) בכרטיס הדסקטופ = leftover לא-נראה. חיווט: `events.json` (route + `exhibition_id:the-peeler`), מירור `#events-list-data` ב-`/events/` (הוסר `soon` — הכרטיס היה טוסט "בקרוב" עד שהפריימים נמסרו), כרטיס בראש `/press/` (22 כרטיסים → 11 שורות, בלי שינוי `grid-template-rows`), `press.json` (`event-zohar-ron-medina-talk`, `homepage_visible:false`), sitemap.xml, OG `og/events-zohar-ron-medina-hero.jpg`. |
| `/events/noemi-safir/` | Event (artist talk — **לקלף את הקוד** \| שיחת אמנים עם **נעמי ספיר** בשיתוף Idit Klaiman, **הקולפן**, 19.8.2026 18:00–19:30, כיכר המדינה) | `XhGH...::1450:1762` | `XhGH...::1450:1648` | ✅ `events/noemi-safir/index.html` (2026-08-12) — משפחת artist-talk, **וריאנט הקולפן הרגיל** (תבנית zohar-ron-medina: פס `EXHIBITION • VOLUME 2` + הקולפן → `exhibitions/the-peeler/`, scrim שטוח `rgba(27,27,27,.4)` בשני ה-breakpoints, בלי LINGER/הערת הרשמה), רצועת אומנית בודדת. **🔴 ה-slug לפי האומנית** — `risa-and-noemi` (how-many/דיזינגוף) הוא אירוע אחר שלה; אין התנגשות. ייחודים: (1) **שני קרופי גיבור** — הפריים המוביילי (390×520) שומר את כל הציור בעוד הדסקטופ (492×744) חותך את הצדדים, ולכן `<picture>` **ידני** עם `source media="(max-width:768px)"`: `hero.{webp,avif}` 984×1488 + 480/768 לדסקטופ, `hero-mobile.{webp,avif}` 780×1040 + 480 למובייל (⚠️ `picture-upgrade.js` מדלג על img שכבר ב-`<picture>` — **כל webp חייב אח avif ידני**). imageRef `5c64359f…` (ציור של נעמי, מקור 2897×3621); אומת RMSE 5.6% דסקטופ / 7.5% מובייל מול רינדורי ה-nodes (ההפרש = טקסט האוברליי הלבן). (2) אוברליי מובייל **מעל המרכז** — `top:47.5%` (Figma group y:189 מתוך 520), דסקטופ `top:50%`. (3) מטא = תאריך+שעה FbEzmel **Light בשני ה-breakpoints** (בניגוד ל-zohar-ron-medina שבו המובייל Regular). (4) גוף = פסקה עם ריצת **Regular יחידה** בתוך גוף Light — `.lead` ("לקלף את הקוד", Figma ts3); `Idit Klaiman` = יזמת אורחת שאינה אומנית הגלריה → `<span class="lat">` **בלי קישור** (תקדים `host_he` של the-space-between). thumb האומנית = מיחזור **`images/artists/noemi-safir/portrait-v2.webp`** (RMSE 2.9% מול imageRef `1214412249…`) מיושר לראש `object-position:50% 0%` — ⚠️ **לא** תמונת הגריד שלה (צילום אחר, RMSE 27%). חיווט: `events.json` (route + `exhibition_id:the-peeler` + `host_he`), מירור `#events-list-data` ב-`/events/` (`list_image` = `list-card.webp` 876×487 מקרופ Figma `69e73b`), כרטיס ב-`/press/` במקום הכרונולוגי (24 כרטיסים → 12 שורות), `press.json` (`event-noemi-safir-talk`, `homepage_visible:false`), sitemap.xml, OG `og/events-noemi-safir-hero.jpg`. |
| `/events/gala-night/` | Event (**gala night** — ערב הגאלה של **הקולפן**, 29.7.2026 19:30–21:30, כיכר המדינה) | `XhGH...::1451:1901` | `XhGH...::1451:2070` | ✅ `events/gala-night/index.html` (2026-08-12) — **תבנית אירוע-פתיחה (בסיס `events/how-many/`), לא משפחת ה-artist-talk:** hero band אפור (כותרת Copperplate `gala night` 80→64 אופטי, ls .1em; מובייל 24→18) + מטא (נקודת 1×1 per Figma; שעות `19:30-21:30` verbatim — התחלה-סיום, הפוך מהמשפחה) + **מכתב האוצרת המלא ב-HTML** ("Welcome aboard." = `.lat` 18→14/13; "הקולפן" הראשון → `exhibitions/the-peeler/`; קורין אברהם → דף האוצרת; שבירות `.dbr` דסקטופ-בלבד) | hero = פוסטר ההזמנה (imageRef `93313e4b…` 984×1488, זהה בשני ה-breakpoints; ה-fill השני במובייל = leftover מוסתר, אומת ברינדור) → **שתי גלריות טריפטיך** `.event-tri` (3 שקופיות כ"א, 3 dots-פסים 32×2, גאומטריית how-many בדיוק; בלי lightbox) → **סקשן אומנים** (atmosphere.webp = imageRef `b8f85e72…` לרוחב בקרופ מרכזי 984×1488, אומת RMSE 1.6% מול הרינדור — שכבות ה-SVG/לבן בפריים לא נראות; רשימת **22 אומנים מקושרים** 28/32, שבירות שונות לכל breakpoint דרך `.dsep`/`.msep`+`.dbr`/`.mbr`; תוקנו: פייפ חסר מיכאל↔אהרן, אליצ׳ה→אליס דה בליס, צלניק→זלניק) → **moments strip** (14 רינדורי-node אפויים 534², תאים 178×148 דסקטופ / 100.25×121.52 מובייל, `object-position:50% 0`, scroll-cue חיצים במובייל; חץ הכותרת → `exhibitions/the-peeler/`). **2026-08-18 — לייטבוקס הוחל על כל הסליידרים בעמוד (בקשת משתמש, דפוס how-many):** רצועת ה-moments (`data-artwork-gallery="event-moments"`, title "moments from the opening") + שתי גלריות ה-tri (`data-tri-lightbox="true"`, scopes `event-gallery-1/2`, title "gala night"); נוספו artwork-lightbox css/js + `cursor:zoom-in`. מובייל = `display:contents`+`order` (סדר: hero→כותרת→מטא→גלריה1→מכתב→גלריה2→אומנים→moments). header דסקטופ לבן (`body.page-event-gala-night`). התאריך עבר → הכרטיס נושר מ-`/events/` אוטומטית; קיים ב-`/press/` ובגריד `#press` בהומפייג' (Figma `1323:570`). OG `og/events-gala-night-hero.jpg`. |
| `/events/hadas-tuval/` | Event (artist talk — **הדס טובל**, מיצב פרפורמטיבי בהשראת שיבארי, **הקולפן**, 28.8.2026 11:30–13:00, כיכר המדינה) | `XhGH...::1468:1324` | `XhGH...::1468:1211` | ✅ `events/hadas-tuval/index.html` (2026-08-12) — משפחת artist-talk, **וריאנט הקולפן הרגיל** (תבנית zohar-ron-medina: פס `EXHIBITION • VOLUME 2` + הקולפן → `exhibitions/the-peeler/`, בלי LINGER/הערת הרשמה), רצועת אומן בודד → `artists/hadas-tuval/`. ייחודים: (1) **🔴 scrim `rgba(27,27,27,.7)` בשני ה-breakpoints — כפול מהמקובל במשפחה (.4)**, ואומת שהוא באמת מרונדר (אלפא נגזרת 0.698 בשתי רצועות מדידה; ממוצע אפור הכרטיס אחרי בנייה 83.5 מול 83.6 ברינדור הפיגמה, ובמובייל 84.3 מול 85.3); (2) אוברליי מובייל `top:47.5%` (בפיגמה הלוקאפ מעט מעל המרכז — y 189..304 מתוך 520), דסקטופ `top:50%`; (3) במטא של המובייל התאריך והשעה ב-**FbEzmel Light** (בשאר המשפחה Regular); (4) שבירת הכותרת זהה בשני הפריימים (אחרי "with") ⇒ `<br>` רגיל, בלי `.dbr`/`.mbr` — ⚠️ בפריים הדסקטופ הכותרת נראית ב-3 שורות כי 80px גולשים בתיבה של 676, אבל השבירה שהמעצבת כתבה היא אחת בלבד; ב-64px (הכיול האופטי) היא נכנסת ל-2 שורות, כמו במובייל. גיבור = imageRef `bcfaadef…` (המקור 945×2048 = צילום מסך עם פסים שחורים למעלה/למטה) — **נאפה חלון ה-cover של הדסקטופ בלבד** (y 309..1738, מחוץ לפסים) ל-`images/events/hadas-tuval/hero.{webp,avif}` 984×1488 + 480/768, ולכן `object-fit:cover` משחזר גם את קרופ המובייל (390×520) פיקסל-בפיקסל. thumb האומנית = מיחזור `images/artists/grid/hadas-tuval.{webp,avif}` (RMSE 0.5% מול imageRef `2249c4b3`), `object-position:50% 0%` (בפיגמה הריבוע מוצמד לראש חלון 178×148); ה-fill השני בכרטיס הדסקטופ (`1214412249…` — הפורטרט של **נעמי ספיר**) = leftover מוסתר. `list-card.{webp,avif}` 876×487 = קרופ רוחבי מהמקור (אין frame כרטיס בפיגמה). חיווט: `events.json`, מירור `#events-list-data` ב-`/events/`, כרטיס ב-`/press/` (25 כרטיסים → 13 שורות), **כרטיס בראש גריד `#press` בהומפייג׳** (12 כרטיסים → 6 שורות (אחרי שנוסף גם livay-levi); בקשת המשתמש 2026-08-12, אותה חריגה כמו gala-night מכלל "רק כתבות + פתיחות"), `press.json` (`event-hadas-tuval-talk`, `homepage_visible:true`), `homepage.json press_section.item_ids`, sitemap.xml, OG `og/events-hadas-tuval-hero.jpg`. |
| `/events/livay-levi/` | Event (artist talk — **ליוואי לוי**, ערב פתוח ואישי אל תוך עולם היצירה שלו, **הקולפן**, 18.8.2026 18:30–20:30, כיכר המדינה) | `XhGH...::1496:413` | `XhGH...::1496:300` | ✅ `events/livay-levi/index.html` (2026-08-12) — משפחת artist-talk, **וריאנט הקולפן הרגיל** (תבנית zohar-ron-medina: פס `EXHIBITION • VOLUME 2` + הקולפן → `exhibitions/the-peeler/`, scrim שטוח `rgba(27,27,27,.4)` בשני ה-breakpoints, בלי LINGER/הערת הרשמה), רצועת אומן בודד → `artists/livay-levi/`. ייחודים: (1) **שבירת הכותרת זהה בשני הפריימים** (אחרי "with") ⇒ `<br>` רגיל, בלי `.dbr`/`.mbr`; (2) אוברליי מובייל `top:47.4%` (Figma group y:189 מתוך 520), דסקטופ `top:50%`; (3) מטא = גלריה FbEzmel Regular, תאריך+שעה **Light בשני ה-breakpoints**. גיבור = imageRef `0b7f23e3…` (יצירה פיסולית בחלל מגורים, מקור 2244×2804) → cover ממורכז `984×1488` + 480/768; `list-card.{webp,avif}` 876×487 = קרופ רוחבי מאותו מקור (אין frame כרטיס בפיגמה). thumb האומן = מיחזור `images/artists/grid/livay-levi.webp` עם `object-position:50% 0%` (בפיגמה הריבוע מוצמד לראש חלון 178×148 / 96×80). חיווט: `events.json`, מירור `#events-list-data` ב-`/events/`, כרטיס ב-`/press/` במקום הכרונולוגי (26 כרטיסים → 13 שורות, `--pc-rows` מחושב ב-JS), `press.json` (`event-livay-levi-talk`, `homepage_visible:true`), **כרטיס בגריד `#press` בהומפייג׳** (נוסף 2026-08-12 בבקשת המשתמש — שני מלמעלה, אחרי hadas-tuval; 12 כרטיסים → 6 שורות) + `homepage.json press_section.item_ids`, sitemap.xml, OG `og/events-livay-levi-hero.jpg`. |
| `/works/livay-levi-3/` | Artwork (**interdependence** — יצירה משותפת **ליוואי לוי × הדס טובל**, הקולפן / כיכר המדינה) | `XhGH...::668:13584` | — | ✅ `works/livay-levi-3/index.html` (2026-08-12) — **השימוש החי הראשון בשדות ה-collab** (ראה §6). רשומה **אחת** ב-`art_works[]` עם `artist_pages:["hadas-tuval"]` + `collab[]` + `collab_connector_he:"×"` ⇒ מופיעה **בשני דפי האומנים** (פעמיים, בדפים נפרדים — לא צמודות) ובגריד `/works/` **פעם אחת**, עם עמוד יצירה קנוני יחיד. קבוצה ייעודית `exhibition_title_he:"הקולפן | interdependence"` (לא מתמזגת לקבוצות הקיימות של השניים, שכותרותיהן שונות) + **שתי רשומות `exhibition_statements`** (livay-levi + hadas-tuval) עם אותו טקסט — אפשר לפצל לנוסח שונה לכל אומן בלי שינוי קוד. **🔴 `collab_connector_he:"×"` (U+00D7) ולא "x"** — ל-FbEzmel אין גליף לטיני `x` בכלל (יש `×`), ולכן "x" היה נופל מהפונט העברי; ראה lessons 2026-08-12. עמוד היצירה קיבל תמיכת collab ברנדרר (`artistHtml` — שני שמות מקושרים; שאר 140 העמודים לא נגעו). תמונה `images/works/v2/livay-levi-3.{webp,avif}` 1080×810 +480/768 (imageRef `4ce7697e…`), OG `og/works-v2-livay-levi-3.jpg`. תיקוני כתיב מוכרעים מול Figma: "גןף"→"גוף", "ותלוייה"→"ותלויה". |
| `/sponsors/soos/` | Sponsor (**הקולפן X soos** — סטודיו הרמקולים soos.sound של דן סרוסי, גלריית כיכר המדינה) | `XhGH...::1318:3287` | `XhGH...::1318:3480` | ✅ `sponsors/soos/index.html` (2026-08-04) — **משפחת עמודים חדשה `sponsors/`** על בסיס פאנל ה-artist-talk: פאנל `#EEF0EF` (padding 48/96/32; טקסט משמאל, כל התוכן מיושר לימין: צ'יפ ממוסגר "גלריית כיכר המדינה"→`/#galleries`, לוקאפ [לוגו soos]‑X‑הקולפן (הקולפן→`/exhibitions/the-peeler/`, הלוגו→soos.audio), גוף 3 פסקאות FbEzmel Light 18/16 ברוחב 358 RTL, divider 72×1 `#D2D2D2` **דסקטופ-בלבד** (padding 24/16/16), קרדיט **"מודל: Bella \| soos.sound"** (`.line--model{direction:rtl}` — הכרעת משתמש 2026-08-04: ה-LTR של Figma הפך את הסדר לקורא עברית; תוקן גם ב-`.soos-line--model` בטיזר ההומפייג') + `www.soos.audio`; hero 492×744 מימין — imageRef `69c2a352…`, גרדיאנט `0deg rgba(0,0,0,.7)→transparent`, לוקאפ לבן ממורכז `top:50%`), ומתחת **רצועת גלריה על לבן** — 4 תמונות gap 8: שלוש `flex:1` + רביעית צרה `flex-basis:20.8181%` (סטריפ `aspect-ratio:1248/389.80853271484375`; crops של 01/03 נאפו מ-Figma). מובייל (`1318:3480`): hero full-bleed 390×520 מתחת ל-header (scrim שטוח `rgba(27,27,27,.4)`), ואז תוכן על לבן בסדר **צ'יפ→לוקאפ→גוף→גלריה→קרדיט** (הגלריה בין הגוף לקרדיט — `display:contents`+`order`; קרדיט ממורכז; סטריפ `aspect 358/106.5`, צרה `70.48px`). **🔴 חריגת-איות מוכרעת:** `soos.sound`/`www.soos.audio`/`Bella` נשארים **lowercase** (מותג; Figma `textCase:LOWER`) — מחלקת `.brand` (Copperplate Light + `text-transform:none`, **1em גם בגוף** — הכרעת משתמש 2026-08-04: lowercase ב-Copperplate = small-caps שכבר קטנים אופטית, ה-`.82em` המקורי נראה קטן מדי); כל שאר האנגלית UPPERCASE כרגיל. ה-X בלוקאפ = **36 verbatim** (כיול ה-~28% של Copperplate הוא לרוחב טקסט רץ, לא לגליף בודד). לוגו soos = **SVG inline ×2** (`fill:currentColor` — שחור בעמודה, לבן ב-hero; מקור `images/sponsors/soos/soos-logo.svg`, node `1323:1013`, 88×21.59). **⚠️ לקח (ראה lessons):** מחלקה בשם `logo` התנגשה עם `.logo` הגלובלי ש-`site-chrome.css` מזריק בזמן ריצה → שם ייחודי `soos-logo`. תמונות `images/sponsors/soos/` (hero 984×1476+וריאנטים 480/768, gallery-01..04 ≤660w), OG `og/sponsors-soos-hero.jpg`. דאטה: **`data/sponsors.json` חדש** (§6; meta בלבד — הגוף ב-HTML, תקדים long-form). frames ריקים בפריים המובייל (`1318:3523`, wrappers של הקרדיט) = שרידי תבנית — לא נבנו. אין אזכורי אומנים בעמוד (דן סרוסי אינו אומן גלריה). **🔴 2026-08-18: פריימי הפיגמה `1318:3287`/`1318:3480` שוכתבו in-place לעיצוב sumii** — לעמוד soos אין יותר מקור חי בפיגמה; העמוד נשאר כפי שנבנה. |
| `/sponsors/sumii/` | Sponsor/residency (**sumii X the art gallery** — המותג SUMII של שירה טורוביץ׳, POP UP ART RESIDENCY בגלריית כיכר דיזינגוף, 31.08–05.10.2026, אוצרות קורין אברהם) | `XhGH...::1318:3287` ("sumii - desktop") | `XhGH...::1318:3480` ("sumii - mobile") | ✅ `sponsors/sumii/index.html` (2026-08-18) — **⚠️ אותם node-ids של עמוד soos: המעצבת שכתבה את הפריימים in-place** (זיהוי לפי שם פריים+תוכן, node-id אינו מצביע יציב). תבנית משפחת soos עם הבדלים: (1) **גוף ברוחב מלא** של עמודת הטקסט (לא 358px), 7 פסקאות עבריות עם ריצות לטיניות `.lat` .88em (SUMII/POP UP ART RESIDENCY/FIGURES/CENTRAL SAINT MARTINS/LVMH — **בלי חריגת lowercase**: ל-sumii אין הכרעת `case_exception`, הכלל הגלובלי UPPERCASE חל; `case_exception:[]` ב-JSON); טווח התאריכים עטוף `<bdi dir="ltr">` = מוצג התחלה→סיום (תקדים דפי הגלריות — היפוך ה-bidi של הפיגמה לא שוחזר); פסקה 7 מכילה יפנית 住井 (נופלת ל-fallback מערכת — אין פונט CJK בפרויקט). (2) **לוקאפ** = [לוגו Sumii וקטורי] X [`the art gallery by`/`erez zielinski rozen` — Copperplate 16 Figma → 13px אופטי, ls .05em בשורה 1] — הלוגו inline פעם אחת כ-`<symbol id="sumii-mark">` (מ-node `1546:3666`, נוקה ל-`currentColor`) + שני `<use>`; X = 20.1117/36 verbatim (גליף בודד). הלוגו → sumiiworld.com, שם הגלריה → הומפייג' (תקדים peeling-a-layer). (3) **hero 492×744 בלי scrim בשני ה-breakpoints** (אין בפיגמה — התמונה כהה; אומת ברינדור); אוברליי לבן ממורכז `top:50%`: שם הגלריה (30→23px), X 36, לוגו לבן 87.97×40 (מובייל: 24→18px, X 28.8, לוגו 70.38×32). תמונה אחת `images/sponsors/sumii/hero.{webp,avif}` 1620×2160 = **חלון-איחוד באספקט המובייל 3:4** (crop מרכזי 2025×2700 מהמקור 2160×2700) — ה-cover הדסקטופי משחזר את קרופ הפיגמה, המובייל = הקובץ במלואו; וריאנטים 480/768/1080. (4) **רצועת גלריה = 3 תמונות שוות** (לא 4 כמו soos), דסקטופ `object-fit:contain` על לבן (FIT בפיגמה, אותו גובה סטריפ 389.80853...), מובייל **cover** באספקט `358/138.18`; `gallery-01..03` ≤624×780. (5) **סקשן `the curator`** — דפוס רצועת האומנים של דפי האירועים (label 28→16px, כרטיס 178×148/96×80, שם אפור 13px מובייל) עם כרטיס יחיד → `curators/korin-avraham/`; thumb `curator-thumb.{webp,avif}` 712×561 נאפה מקרופ הפיגמה `1b9e5e` של `7f582c67…` (קורין — אותו צילום של פורטרט האוצרת; ה-fill השני `889e3ab7…` = צילום זוהר רון, leftover מוסתר — אומת ברינדור). (6) divider 72×1 מוצג **גם במובייל** (#D2D2D2 דסקטופ / #EFEFEF מובייל, בניגוד ל-soos שהסתיר); אתר `www.sumiiworld.com` Copperplate UPPER 14/13px (18/16 Figma); **קרדיט אוצרות שונה בין breakpoints** (Figma): דסקטופ "אוצרות: קורין אברהם" / מובייל "אוצרת התערוכה - קורין אברהם" — swap ב-`.only-d`/`.only-m`, השם מקושר לעמוד האוצרת בשניהם. סדר מובייל: hero→chip→לוקאפ→גוף→גלריה→divider→אתר→קרדיט→curator (display:contents+order; הגלריה בין הגוף לקרדיטים). שירה טורוביץ׳ אינה אומנית גלריה — בלי קישור (תקדים host_he). חיווט: `sponsors.json` (רשומת sumii + שדות חדשים `curator_slug`/`date_start`/`date_end`, null ב-soos), sitemap.xml, OG `og/sponsors-sumii-hero.jpg`. פערים פתוחים ב-`docs/todo.md` (חשדות טעות-כתיב בפיגמה: "בפסל חומר", "קימור ובשם"). |
| `/opencalls/` | Index | — | — | ⏳ |
| `/opencalls/the-peeler/` | Single | `Zn3N...::1213:2417` | `Zn3N...::1213:2518` | ✅ |
| `/opencalls/how-many/` | Single | `Zn3N...::1213:2340` | `Zn3N...::1213:2263` | ✅ |
| `/contact/` | Form | — | — | ✅ `contact/index.html` |
| `/accessibility/` | Statement | — | — | ✅ `accessibility/index.html` |
| `/privacy/` | Privacy policy | — | — | ✅ `privacy/index.html` |

### 🆕 משפחת עמודי האירוע "artist talk" (2026-07-09, 13 עמודים)

`natasha-zeriker`, `liel-salman`, `nir-giorgio-levin`, `risa-and-noemi`, `zohar-ron`, `amnon-lipkin` (נוסף 2026-07-23), `alice-debellis`, `anat-wegier` (נוסף 2026-07-26), `the-space-between` (נוסף 2026-08-04 — וריאנט מצומצם: בלי presented/אומנים/הרשמה, אוברליי-כותרת בשליש העליון, scrim דסקטופ הפוך; ראה שורתו ב-§4), `zohar-ron-medina` (נוסף 2026-08-06 — וריאנט הקולפן הרגיל; **scrim שטוח בשני ה-breakpoints** ושבירת-כותרת שונה לכל breakpoint), `noemi-safir` (נוסף 2026-08-12 — וריאנט הקולפן הרגיל; **שני קרופי גיבור ב-`<picture>` ידני** ואוברליי מובייל מעל-מרכז), `hadas-tuval` (נוסף 2026-08-12 — וריאנט הקולפן הרגיל; **scrim `rgba(27,27,27,.7)` — כפול מהמשפחה**, ואוברליי מובייל `top:47.5%`), `livay-levi` (נוסף 2026-08-12 — וריאנט הקולפן הרגיל; scrim שטוח `.4`, שבירת כותרת זהה בשני הפריימים, אוברליי מובייל `top:47.4%`) — **עמודים סטטיים עצמאיים** עם CSS זהה. **הם לא נבנים מהתבנית של `events/artist-talk/`** (שם יש triptych/וידאו; כאן אין בכלל סקשן artworks).

- **מבנה:** פאנל `#EEF0EF` עם 2 עמודות — טקסט משמאל, פורטרט `492×744` מימין; ואז רצועת `artists`. סדר הבלוקים: כותרת → meta → גוף → `presented as part of` → כתובת → [כרטיס LINGER] → [הערת הרשמה].
- **מובייל (390):** `display:contents` + `order:` — התמונה ראשונה, וכל בלוק טקסט על רקע לבן.
- **Scrim:** דסקטופ = `linear-gradient(0deg, rgba(0,0,0,.8), transparent)`; מובייל = `rgba(27,27,27,.4)` שטוח.
- **פונט Solway** (`--sol`, כבר ב-`פונטים/`) — רק לתווית `THE EXHIBITION` / `EXHIBITION • VOLUME 2`. **אין קווים מלווים לפס** (אומת ברינדור מ-Figma; אל תסתמך על ה-layout data).
- **🔴 התאמת גודל אופטית:** ה-Copperplate שלנו רחב ~28% מזה שב-Figma. לכן כותרת 80px ב-Figma = **64px/58** בקוד. **מובייל (כויל מול רינדורי Figma, 2026-07-09):** כותרת 24px→**18px** (+`.mbr` — `<br class="mbr">` שנראה רק ≤768 לשבירות המפורשות של Figma), אוברליי `.en` 36px→**22px/.2em**, אוברליי `ex-name` 29px→**17px** (רוחב 313px ⇒ ‎"HOW MANY PARTNERS" בשורה אחת), `ex-name` בגוף 24px→**19px/.12em**, קפשנים אנגליים 16px→**13-14px**, תווית `artists` 20px→**16px**, שם אומן **13px** על כרטיס 112px (מונע שבירה ל-3 שורות). **עברית (FbEzmel) = px של Figma כמו-שהם** (`הקולפן` 29px, גוף 16px) — רק לטינית מקבלת כיול. Solway = אותו פונט כמו Figma ⇒ 26px verbatim. אוברליי ממורכז `top:50%` במובייל.
- **🔴 `picture-upgrade.js` עוטף `<img>` ב-`<picture>` בזמן ריצה** — ולכן `.card>img` מפסיק להתאים. חייבים `.card>picture{display:contents}` + `.card img{height:100%}`, אחרת התמונה מתכווצת לגובה הטבעי שלה ורקע הכרטיס השחור נחשף.
- **תמונות גיבור:** `images/events/<slug>/hero.{webp,avif}` (984w + 480/768w). לגיבורים עם שתי שכבות תמונה ב-Figma — רק העליונה נראית (רונדר את ה-node כדי לוודא, אל תנחש מסדר ה-fills).
- **רצועת האומנים** ממחזרת את `images/artists/grid/<slug>.{webp,avif}`.
- **גנרטור:** `/tmp/.../gen_event_pages.py` (לא בריפו) — CSS משותף + בלוקי תוכן לכל עמוד.
- ~~`homepage_visible:false`~~ **עודכן בהמשך אותו יום: `homepage_visible:true` בכל החמישה** — נוספו ככרטיסים בראש סקשן `#press` בעמוד הבית (3 שורות חדשות: risa+zohar / nir+liel / natasha יחיד), + רשומות `press.json` (type event) + `homepage.json item_ids` + sitemap.xml (בקשת המשתמש 2026-07-09 ערב).

**🔎 Audit רוחבי 2026-07-09 (קומיטים `b18bacc`+`0ab21e0`) — הכרעות state שחשוב להכיר:** (1) **כותרת loneliness הרשמית = "בדידות בתוך סביבה תוססת"** (תוקן כפל-ב' שהיה ב-Figma/JSON; אם ב-Figma עדיין "בתוך בסביבה" — זו טעות מוכרעת, לא לשחזר). (2) **`exhibition_route` של כל יצירות הקולפן = `exhibitions/the-peeler`** (לא `opencalls/the-peeler`), וה-renderer בעמודי `works/<id>` בונה עכשיו את קישור התערוכה מ-`w.exhibition_route` (לא hardcode `opencalls/`). (3) **איות אנגלי קנוני: "adi duek", "zohar shitrit"** (הכרעת משתמש; ה-slugs נשארו `adi-duak`, `zohar-shtrit` — כלל-זהב 6). (4) פירורי-לחם JSON-LD לא מפנים יותר לעמודי אינדקס שלא נבנו (`/press/` וכו') — כשבונים את עמודי האינדקס, אפשר להחזיר. (5) ~~5 דפי האירועים החדשים מוסתרים~~ **בוטל בהמשך אותו יום** — נחשפו: כרטיסים בהומפייג' `#press`, רשומות `press.json`/`homepage.json`, ו-sitemap.xml.

**סטטי (shell):** brand, nav, footer, newsletter copy, copyright, palette + typography. ב-`data/site.json` או CSS.
**דינמי (JSON):** galleries, artists, exhibitions, events, press, opencalls, works, instagram, homepage curation, announcement, **curators**.

**📬 טופס ניוזלטר (פוטר):** עובד דרך **Wix Velo**, לא Firebase (Firebase נפסל — Spark חוסם קריאות יוצאות, Functions דורש Blaze בתשלום). הטופס ב-`components/site-chrome.js` שולח `POST https://zrp.co.il/_functions/subscribe` (אתר ה-Wix "ZIELINSKI & ROZEN", siteId `03bb4cff-b492-498b-9864-9258e743d0a2`). מקור ה-backend: `newsletter-backend/wix-velo/http-functions.js` → להדביק ל-Backend ב-Wix ולפרסם. כל נרשם: honeypot (שדה `website`) + rate-limit לפי IP (5/דקה, דרך קולקשן אופציונלי `NewsletterThrottle` עם שדה `ip`; fail-open אם חסר) → `appendOrCreateContact` → לייבל **`custom.art-newsletter` ("Art Newsletter" segment)** → **סימון SUBSCRIBED** דרך REST Email Subscriptions API. **חשוב:** הסימון SUBSCRIBED דורש **Wix API key** עם הרשאת "Manage Email Subscriptions", שמור ב-Wix Secrets Manager בשם `WIX_API_KEY` (Velo לבדו לא יכול לשנות subscriptionStatus — read-only). בלי הסוד → איש הקשר עדיין נוצר+מתויג אבל לא מסומן מנוי. רואים נרשמים ב-Wix → Contacts מסונן ל-label "Art Newsletter". **גילוי פרטיות:** ההרשמה מצרפת גם לרשימת התפוצה של האתר הראשי (הבשמים, zrp.co.il) — מצוין ב-`privacy/index.html` ובטקסט ההסכמה שבטופס. **לא להחזיר Firebase.**

---

## 5. STACK & FILE LAYOUT

```
.
├── index.html                 # Homepage entry → /
├── about/index.html           # → /about/
├── works/index.html           # → /works/
├── contact/, accessibility/, privacy/   # Static pages, each <dir>/index.html
├── artists/<slug>/index.html  # Per-artist pages (shared template + data-slug)
├── exhibitions/<slug>/index.html
├── opencalls/<slug>/index.html
├── press/<slug>/index.html
├── events/<slug>/index.html
├── curators/<slug>/index.html
├── components/                # artwork-lightbox, gallery, stacked-gallery, triptych-gallery, site-chrome
├── data/                      # JSON content (mirrors future CMS)
├── images/                    # see docs/conventions.md §1
├── docs/                      # conventions, components, artist-linking, lessons, todo
├── פונטים/                    # Copperplate {300/400/700/900}, FbEzmel {300/400}, NotoSansArabic {300/400}
├── .nojekyll                  # Tells GitHub Pages to serve files as-is (no Jekyll processing)
├── FIGMA_LINKS.md             # All page-by-page Figma URLs
└── CLAUDE.md                  # ← you are here
```

> **URLs:** Directory-based for clean paths. `/about/` instead of `/pages/about.html`. Templates (`exhibition`, `opencall`, `artist`) are copied per slug with `data-slug` attribute on `<html>` so each page is fully self-contained at its own clean URL.

**שמות:** kebab-case, ASCII. סלאגים = `id` ב-JSON. תיקיות עברית רק לפונטים ולקובץ הזה.

**תיקיות חסרות** — תיצור. **אל תשנה חלוקה קיימת.**

---

## 6. JSON Data Contracts

**עיקרון:** דף קורא JSONs, לא מטמיע inline. שדה חסר → תוסיף ל-JSON לפני שימוש. שדה חדש → לכל הרשומות (null אם לא ידוע).

| Schema | שדות עיקריים |
|---|---|
| `site.json` | brand, nav, footer, announcement, social — **גלובלי בכל דף** |
| `galleries.json` | id, slug, name_he/en, address_he/en, status (`open\|coming-soon\|closed`), hours[] (⚠️ `time` שמור בקונבנציית bidi "18:00-11:00"), image_hero (כרטיס ההומפייג'). **מ-2026-08-12 (דפי גלריה):** `route` (nullable), `image_page_hero` (hero של דף הגלריה — נפרד מ-image_hero), `address_street_en`/`address_area_en` (הכתובת כפי שמוצגת בדף, verbatim מהפיגמה), `figma_page_desktop`/`figma_page_mobile`, `manager` (nullable — `{name_en_first, name_en_last, email (lowercase ל-mailto; תצוגה UPPERCASE דרך הכלל הגלובלי), portrait{src,width,height,alt_he,figma_image_ref}, note}`; נטלי זיגל משותפת למדינה+דיזינגוף). **צרכנים:** דפי `galleries/<slug>/` + עמוד האוצרת — לכל אחד מירור inline `#fallback-galleries` שחייב סנכרון בכל שינוי |
| `artists.json` | id, slug, name_he/en, portrait, bio_he/en, works[] (objects: `{id,title,image,year?,medium?}` inline, לא ref ל-works.json), instagram_handle, hero_images[], **`hero_image_focus[]`** (object-position לכל תמונה בדואו), **`hero_collab`** (שיתוף: `artists[{name_en, bio_he_short, instagram}]` — hero מובייל Figma `119:3917`), figma_artist_page_desktop/mobile, homepage_featured, **אופציונלי:** `studio_en` (שם סטודיו — כשקיים, ה-hero מציג את השם המלא בשורה אחת ואת הסטודיו בשורה/ות שמתחת; tali-zelnik), `works_caption_each_image` (`true` = תמיד כיתוב מתחת לכל תמונה; דורש כשכותרות זהות אך הקשר How Many ולא loneliness) |
| `exhibitions.json` | **🔴 סדר המערך = כרונולוגי עולה (ישן→חדש), וזה מקור-האמת לדירוג "התערוכה החדשה למעלה" בדפי האומנים** (§4, `__EX_ORDER_INLINE__`) — תערוכה חדשה נוספת **בסוף** המערך, ואז `tools/sync_works_mirrors.py`. שדות: id, slug, title_en, subtitle_he, status (`current\|upcoming\|archived`), gallery_id, start/end_date, hero_image, thumbnails[], description_he/en (paragraphs: `[{weight, lines:[...]}]`), artist_ids[], `gallery_images[]`, `artists[]` (פריט: `name_en`, `thumb`, `slug` אופציונלי, `artist_page_slug` אופציונלי = קישור הכרטיס לדף אומן אחר — לשיתופי פעולה), **`curator_slug`** (אופציונלי: `slug` מ-`curators.json` — מציג סקשן אוצרת ב-`exhibition.html` מתחת לאומנים), `archive_thumbnails`, `hero_overlay_en_only` (אופציונלי: `true` = אוברליי תמונת גיבור רק `title_en`, בלי פס volume / כותרת עברית על התמונה), figma_node_* |
| `events.json` | id, slug, title_he/en, **`card_title_en`** (כותרת קצרה לכרטיס ב-`#events-upcoming` בהומפייג'), date, gallery_id, cover_image, description_he, figma_node_*, figma_file. **אופציונליים (2026-08-04, the-space-between):** `day_he` (יום־בשבוע — מוצג במטא במקום שעה), `host_he` (מנחה אורח/ת שאינם אומני האתר — אין קישור לדף אומן), `exhibition_id:null` = אירוע ללא שיוך תערוכה (אין פס presented), **`list_image`** (2026-08-04 — קרופ רוחבי לכרטיס בעמוד `/events/`; רינדור-node אפוי `images/events/<slug>/list-card.webp` או מיחזור קרופ קיים; אירוע בלי השדה → cover של ה-hero) |
| `press.json` | type (`press\|event`), tag_he/en, source_he/en, subtitle_he, author_he, route, cover_image, date, homepage_visible, figma_article_*. **כתבות long-form: גוף נשאר ב-HTML, רק meta ב-JSON.** |
| `opencalls.json` | id, slug, title_en, status (`open\|archived`), deadline, city, hero_image, **אופציונליים:** `emotional_triggers` (`title_he`, `items[]`, אופציונלית `lines[]` לשבירת שורות כמו ב-Figma), `gallery_images[]` |
| `works.json` | `works[]` = 3 אריחי התמונה של שחמט ההומפייג' (רה-דיזיין 2026-08; אריחי הטקסט the/art works/more = HTML סטטי). `art_works[]` = גריד עמוד The Art Works: `id`, `artist_slug`(→קישור אומן), `artist_he`/`artist_en` (אם `artist_he`=null → שם לטיני מוצג, כמו la-raz-porta), `title_he`/`title_en` (אופציונלי, דו-לשוני), `gallery_slug`+`gallery_en` (→ `/#galleries`), `sold` (bool → תג "נמכר"), `details_he` (טקסט אפור אופציונלי), `img` (תחת `images/works/v2/`), `w`/`h`, `widths[]` (לבניית srcset; האחרון = הקובץ הראשי `<img>.webp`), **`artist_page_pos`** (int/null — סדר התצוגה בדף האומן לפי frames `artist-pages-קולפן`; הרנדרר בדפי אומן ממיין לפיו אחרי הסינון, null=סוף לפי סדר המערך; עמוד `/works/` מתעלם וממשיך לפי סדר המערך = Figma `542:499`). **מירור inline פעמיים** כי file:// לא יכול fetch — **לסנכרן את שניהם** בכל שינוי ל-`art_works[]`: (1) `works/index.html` `#works-data`; (2) `artists/<slug>/index.html` `#art-works-inline` (`__ART_WORKS_INLINE__`, זהה בכל 30 הקבצים — ערוך את התבנית והפץ; ⚠ בעת הזרקה תכנותית השתמש ב-replacement function ולא במחרוזת, אחרת `\n` שבתוך ה-JSON יהפוך לתו שורה אמיתי וישבור את ה-JS). דפי האומן מציגים את כרטיסי `art_works[]` המסוננים לפי `artist_slug`; אם האומן לא ב-`art_works[]` → fallback ל-`artists.json works[]`. **🔴 יצירה משותפת (collab):** ✅ **2026-08-12 — השדות בשימוש חי לראשונה: `livay-levi-3` ("interdependence", ליוואי לוי × הדס טובל).** המתכון המאושר (הכרעת פיגמה `668:13584`): **רשומה אחת** + `artist_pages[]` לאומן/ית השני/ה + `collab[]` (שני שמות מקושרים) + `collab_connector_he:"×"` + `exhibition_title_he` ייעודי לקבוצה משותפת + **רשומת `exhibition_statements` לכל אומן** (אותו טקסט כברירת מחדל, ניתן לפצל). ⇒ בגריד `/works/` פעם אחת, בדפי האומנים פעמיים (בדפים נפרדים — לא צמודות), עמוד יצירה קנוני אחד. `title_he_by_artist`/`details_he_by_artist` **עדיין לא ממומשים ברנדרר הנוכחי** (מתועדים למטה כחוזה עתידי — אם צריך כותרת/פרטים שונים לכל אומן, יש להחזיר את הלוגיקה ולהפיץ ל-42 הדפים). תיעוד היסטורי: **2026-06-24 — לא הייתה אז אף רשומה שמשתמשת בשדות האלה** (היצירה היחידה, `aharon-bas-1`, פוצלה לשתי יצירות עצמאיות `aharon-bas-1`+`bar-cohen-1` ללא שיתוף). השדות נשארים בחוזה (additive/backward-compatible) לשימוש עתידי. **תיעוד היסטורי (2026-06-18):** שדות אופציונליים — **`artist_pages[]`** (slugs נוספים שאצלם היצירה תוצג; הפילטר בדף-אומן = `artist_slug===slug || artist_pages.includes(slug)`), **`collab[]`** (`[{slug,name_he,name_en}]` → קרדיט עם **כל שם מקושר בנפרד** + `collab_connector_he` ברירת מחדל "בשיתוף"; מרונדר ב-3 אתרים: `artistCreditHtml` בדף-אומן, `artistHtml` ב-`/works`, ו-`works/<id>` single), **`statement_he_by_artist{slug:[...]}`** (סטייטמנט תערוכה שונה לכל אומן — `buildExGroups` מעדיף `statement_he_by_artist[a.slug]`). **2026-06-23:** נוספו **`title_he_by_artist{slug:"..."}`** ו-**`details_he_by_artist{slug:"..."}`** (כותרת/פרטים שונים לכל אומן ביצירה משותפת — `buildRichCard` בדף-אומן מעדיף `_th=title_he_by_artist[a.slug]||title_he`, `_dh=details_he_by_artist[a.slug]||details_he`; עמוד `/works/<id>` ו-`/works/` נשארים קנוניים). כל השדות additive/backward-compatible. **🔴 הגירת סטייטמנטים (2026-06-23 — `tools/migrate_exhibition_statements.py`, idempotent):** טקסט-אומן-על-תערוכה כבר **לא** נגזר מ-`art_works[].statement_he` של היצירה הראשונה. עבר ל-מפתח top-level חדש **`exhibition_statements[]`** ב-`works.json` (`{artist_slug, exhibition_title_he, exhibition_slug, exhibition_route, statement_he[]}`, מפתח לוגי = `(artist_slug, exhibition_title_he)`). **משמעות `art_works[].statement_he` השתנתה:** עכשיו = **טקסט על יצירה בודדת** (per-work), מרונדר כבאנר רוחב-מלא **מעל** היצירה (`.work-statement`, `grid-column:1/-1`). בדף-אומן (renderer חדש בלבד): `buildExGroups` שולף סטייטמנט-קבוצה מ-`exStatement(slug,title)` מעל `__EX_STATEMENTS_INLINE__` (מירור inline שלישי שצריך סנכרון, מוזרק אחרי `#art-works-inline`); הכרטיס מרנדר באנר per-work אם ל-`w.statement_he` יש ערך. **עמוד בודד `/works/<id>`:** ה-renderer לא שונה (קורא `w.statement_he` מ-`#artwork-data`); הסקריפט מזריק ל-snapshot את הטקסט לפי סדר עדיפות — **(1)** `statement_he` per-work אם יש, **(2)** אחרת fallback ל-`exhibition_statements[(artist_slug, exhibition_title_he)]` (טקסט-האומן-על-התערוכה), **(3)** אחרת ריק. `alon`, `dan-ben-ary`, `zohar-ron-dan-ben-ari` רצים על renderer **ישן** ללא grouping/statements (§14 regression) — לא נוגעים בהם; `alon-1/2` נשארו עם `statement_he` per-work (אין להם `exhibition_title_he`). **לסנכרן 4 מקומות בכל שינוי** (או פשוט הרץ את הסקריפט): `works.json`, `works/index.html #works-data`, `artists/*/index.html` (`__ART_WORKS_INLINE__` + `__EX_STATEMENTS_INLINE__`), `works/*/index.html #artwork-data`. **🔴 שימוש per-work אמיתי ראשון + סנכרון שוטף (2026-06-23 — `tools/sync_works_mirrors.py`, idempotent, המקור-אמת = `works.json` ללא extraction):** זוהר רון בתערוכת **How Many** = **אין `exhibition_statements` לזוהר** בקבוצה הזו; במקום זה **שתי יצירות עם `statement_he` per-work** — `zohar-ron-dan-ben-ari-1` (sex?, אוחדה לכותרת הנקייה "How Many Partners Have You Had?" מ-"...| sex?") + **`zohar-ron-9` (הקוסם, חדשה)**. **באנר ה-per-work שודרג לעיצוב Figma `863:228`:** ה-**באנר** רוחב-מלא **`.work-statement`** (`grid-column:1/-1`) = קו קצר `.ws-rule` (72px) + כותרת היצירה `.ws-head` (mixedHtml — Copperplate ללטינית/FbEzmel לעברית) + `.ws-body` (הסטייטמנט) **מעל** הכרטיס; אבל **הכרטיס עצמו נשאר אייטם-גריד רגיל (חצי רוחב)** גם כשהוא יחיד — הבאנר והכרטיס הם ילדי-גריד נפרדים (אין wrapper `.work-featured` שמתח את התמונה לרוחב מלא), `margin-bottom:-40px` על הבאנר מצמצם את המרווח לכרטיס. מופיע רק כשל-`w.statement_he` יש ערך; שאר הקבוצות עם `exhibition_statements` ללא שינוי. **כל שינוי ל-`art_works`/`exhibition_statements` → הרץ `sync_works_mirrors.py`** (לא את migrate — הוא יחזיר extraction שגוי כי per-work כבר נראה כמו exhibition-level). עמוד בודד חדש = העתק תבנית + החלף `data-artwork-id`/מטא/`#artwork-data` + OG `og/works-v2-<id>.jpg`. |
| `instagram.json` | snapshot זמני |
| `homepage.json` | composition layer — `*_id`/`*_ids` לפריטים מ-JSONs אחרים; בלוקים כמו `x_our_artists`, `tribe_teaser` (כותרת/קרדיטים/גלריה מעל press; **מובייל:** `gallery_images[]`, `figma_gallery_strip` `250:98`, thumbs = לייטבוקס בלי CTA — ראה `docs/components.md` §1.4.1) |
| `curators.json` | `curators[]`: `id`, `slug`, `name_en_first`/`name_en_last`, `instagram_handle`, `badge_en`, `section_heading_en`, `portrait` (`src`, `width`, `height`, `alt_he`), `exhibition_slugs[]` (סדר דסקטופ שמאל→ימין; מובייל — CSS `order` אם נדרש), `figma_desktop`/`figma_mobile` |
| `sponsors.json` | `sponsors[]`: `id`, `slug`, `name`, `founder_he`, `title_lockup`, `exhibition_slug`/`exhibition_route`, `gallery_id`, `website`, `model_credit`, `route`, `cover_image`, `logo`, `case_exception[]` (מחרוזות מותג שנשארות lowercase — הכרעת משתמש 2026-08-04, מיושם עם `.brand`), `figma_file`/`figma_node_desktop`/`figma_node_mobile`/`figma_logo_node`. **meta בלבד — גוף העמוד נשאר ב-HTML** (תקדים כתבות long-form). |

---

## 7. WORKFLOW לדף חדש

1. קרא `CLAUDE.md`.
2. בדוק ב-§4 sitemap אם הדף כבר רשום. אם לא — תוסיף שורה.
3. קבל URLs מ-`FIGMA_LINKS.md` (או הוסף אם חסרים).
4. ספאון 2 subagents במקביל (desktop + mobile) לקרוא Figma. Prompt:
   > "קרא את הקובץ `<path>` ב-chunks של 600 שורות. החזר blueprint מובנה: dimensions, sections (top→bottom), כל הטקסטים verbatim (כולל עברית), צבעים, imageRefs, layout (gap/padding/justify/align). אל תסכם."
5. אסוף imageRefs **ייחודיים**. **`ls images/<category>/<slug>/` קודם.** הורד ב-batches מקבילים.
6. **לפני HTML — עדכן JSONs.** הוסף entity חדש לקובץ המתאים.
7. בנה HTML יחיד עם CSS מוטמע. Pattern: `font-face → tokens → sections → media queries → casing block (conventions.md §5)`.
8. הוסף לינק לדף ב-nav של כל הדפים הקיימים (אין partial loader — copy-paste; כשמעל 8 דפים, ראה `docs/todo.md`).
9. **לפני סיום:** עבור על `docs/artist-linking.md §7` (בדיקת anchors), וודא `text-transform`/font compliance (`conventions.md §5`), **בדוק חיתוך כותרות מלמעלה** (Copperplate caps ב-`line-height ≤1.2`; **אסור `overflow-x` על `body`** — רק על `html` — ראה `lessons.md` 2026-06-16/06-15), והרץ perf checklist (§8.11): כל `<img>` עם `src="*.webp"` + `width`/`height` + `loading="lazy" decoding="async"` (חוץ מה-LCP שמקבל `fetchpriority="high"`).
10. עדכן: §4 sitemap status, §6 אם הוספת שדה, `docs/lessons.md §4` אם יש לקח חדש.

**שיתוף קבצים:** לפני edit ל-`index.html`, `data/site.json`, `CLAUDE.md`, או כל קובץ shared — **Re-Read קודם**. סוכנים מקבילים יוצרים race conditions.

---

## 8. כללי-זהב (אסור לשבור) 🔴

1. **לא משכפלים תוכן** מ-JSON ל-HTML. שדה חסר → תוסיף ל-JSON.
2. **לא Google Fonts** כתחליף לפונטים מקומיים. **גם לא Inter/sans-serif כברירת מחדל** — אם צריך גופן ללא-Copperplate, להוריד מקומית.
3. **🔴 כל אנגלית = Copperplate UPPERCASE** — כולל אימיילים, handles, URLs, שמות אומנים, caption. הפרה = bug. פרטים ב-`docs/conventions.md §5`.
4. **🔴 עברית+אנגלית מעורבבים** (גם במחרוזת/שורה אחת): עברית = FbEzmel (`var(--heb)`), אנגלית = Copperplate UPPERCASE ב-`<span class="lat">` — **התאמת גודל ויזואלי** (לא אותו px). פרטים ב-`docs/conventions.md §5.2`.
   - **🔴 ערבית = Noto Sans Arabic** (OFL, `./פונטים/NotoSansArabic-{Light,Regular}.woff2`, 300/400). אוטומטי — fallback בסוף מחסניות `--heb`/`--cop` עם `unicode-range` ערבי בלבד; **בלי span, בלי לגעת ב-data**. כל גליף ערבי מנותב אליו (FbEzmel/Copperplate חסרי ערבית). שני ה-`@font-face` של Noto + ה-fallback במחסניות הם כבר חלק מבלוק הפונטים שכל דף מעתיק. פרטים: `docs/conventions.md §3.2`. (נפרס לכל 57 הדפים המסוגננים, 2026-06-15.)
5. **לא Figma node IDs ב-CSS/HTML.** רק ב-JSONs (כ-meta) וכאן.
6. **לא לשנות slugs** של entities שכבר קיימים.
7. **לא לעגל pixel values** מהפיגמה.
8. **לא לדרוס תמונות קיימות** — `portrait-v2.webp` אם הצילום שונה. (מקור גולמי ב-`_originals/`, gitignored; ב-`images/` רק WebP+AVIF.)
9. **Re-Read לפני edit** של קובץ shared (`index.html`, `data/site.json`, `CLAUDE.md`).
10. **🔴 כל אזכור של אומן חייב להיות קישור** לדף האומן (`pages/artists/<slug>.html`). פרטים ב-`docs/artist-linking.md` — **קרא לפני סיום משימה**.
11. **לא לבנות קומפוננטה שכבר קיימת** (lightbox, gallery, carousel). `docs/components.md` הוא ה-source of truth.
12. **🔴 Perf checklist לכל `<img>`:**
    - `src="...webp"` (לא `.png`. `picture-upgrade.js` מוסיף source ל-AVIF אוטומטית.)
    - `width="N" height="N"` (מהפיקסלים האמיתיים, מונע CLS).
    - `loading="lazy" decoding="async"` — חוץ מה-img הראשון בדף.
    - ה-img הראשון בדף = ה-LCP, מקבל `fetchpriority="high"` במקום `loading="lazy"`.
    - **תמונה ≥80KB וגם ≥800px רוחב → חובה srcset:** צור וריאנטים `<name>-{480,768,1080}w.webp+avif` (רק רחבים מ-90% מהמקור: `magick src.webp -resize 480x -strip -quality 85 out.webp`, avif עם `-quality 60`), והוסף `srcset="...-480w.webp 480w, ..., <name>.webp <naturalW>w" sizes="100vw"`. `picture-upgrade.js` ממפה את ה-srcset ל-AVIF אוטומטית — חובה ש**כל** וריאנט webp יהיה לו אח avif.
13. **כן** לעדכן את הקובץ הזה כש-state משתנה (sitemap, golden rules, contracts).
14. **🔴 git commit אחרי כל מצב-עבודה תקין.** אסור לעבוד שעות בלי commit. **הנתונים** (`data/*.json`) הם מקור-האמת — הם תמיד שורדים; אבל **ה-renderer/CSS בתבנית** (`artists/<slug>/index.html`) הם קוד שאפשר לאבד ב-rebuild שגוי (קרה: כל לוגיקת ה-`buildExGroups`/grouping/`bio-col` נמחקה כי תבנית-מקור ישנה דרסה את zohar, ולא היה commit לשחזר ממנו). לכן: לפני כל rebuild גורף של דפי אומנים, ואחרי כל פיצ'ר/תיקון שעובד — `git add -A && git commit`. אם משחזרים תבנית, לשחזר מ-HEAD (git), לא מקובץ-אחר-שאולי-נסוג.

---

> *"Yes, the files are everywhere — but the system is in your head. Document it."*
