# lessons.md — Policies, Recurring Lessons, Recent

> **קרא את הקובץ הזה כש:** אתה מתחיל דף חדש (לבדוק אם יש לקח קרוב לתוכן שאתה בונה), נתקלת ב-Figma quirk, מקבל החלטה Static-vs-Dynamic, או רוצה לתעד לקח חדש. **בסיום משימה — תוסיף לקח ל-§3 רק אם הוא לא חלק מ-§2 (אם קיים — תחזק את הקיים).**

---

## 1. Static-vs-Dynamic Policy

ה-Figma הוא snapshot סטטי. **האתר לא.** לכל section, החלט:

**אינטראקטיבי (אסור לשלוח כתמונה שטוחה):**
- grids של artworks → **lightbox** (components.md §1).
- thumbnails מרובים לתערוכה → **carousel/lightbox**.
- שורות אופקיות (Instagram, archive, artists strip) → **scroll-snap horizontal**. גם בדסקטופ אם items > רוחב.
- גלריות עם `coming soon` → overlay כהה non-clickable; הפתוחה clickable.
- Newsletter form → `event.preventDefault()` עד backend.
- Nav במובייל → hamburger drawer (`.nav-links` כ-fixed inset:0 + `transform:translateX(100%)` → 0).

**Micro-interactions (תמיד הוסף):**
- Card images: `transform:scale(1.04)` ב-hover, `transition:.6-.8s`.
- Links: opacity .6-.7 ב-hover.
- Outline buttons: invert ב-hover.

**סטטי (אל תזייף אינטראקטיביות):**
- Hero, taglines, copyright — לא animations.
- Section headers — לא marquee/scroll animations.
- Overlays/gradients דקורטיביים — לא clickable.

**Rule of thumb:** Figma מציג תמונה curated אחת במקום שבאתר יהיו many → צריך אינטראקציה.

**Implementation default:** vanilla JS, inline `<script>` בסוף body, ללא תלויות. הדוגמה הקנונית: `components/artwork-lightbox.js`.

---

## 2. Recurring Lessons (מאוחד — אם נתקלת בלקח קיים, חזק אותו; אל תכפיל)

### 2.1 Figma quirks

- **שמות nodes משקרים על device.** `the art works-phone` היה 1440, `artist1-13` הם 390. תמיד `absoluteBoundingBox.width`. 1440=desktop, 390=mobile.
- **שני קבצי Figma עם תוכן מראה.** `XhGH...::119:3` ≡ `Zn3N...::1213:3` (artist-alon). תאמת `fileKey` מה-URL.
- **JSON ענק → subagents חובה.** קריאה ישירה של frames גדולים נכשלת על token budget. דפי-יחיד קטנים (about) נכנסים ישירות.
- **`depth:1-2`** לסקירת canvas — מספיק בלי להציף קונטקסט.
- **`{ts1}/{ts2}/{ts3}/{ts4}` markers** = textCase metadata. רנדר כ-`<span class="lat">` (Copperplate Light), אלא אם זה לינק אמיתי → `<a>` עם `var(--link)`.
- **טעויות עיצוב ב-Figma** (כותרת loneliness בתוך how-many; caption של אומן אחד על תמונה של אחר; 4 imageRefs בערימה כאשר רק אחד רלוונטי) — **שיקול דעת**, בחר את המתאים לתמונה/JSON, לא מעתיק טעות.
- **`gap` שלילי** = `margin-top` שלילי על הילד (overlap מכוון בהומפייג').
- **`\n`/`\L`** → `<br>`.
- **figma_node_id מקבל משמעות רק יחד עם fileKey** — frames יכולים לעבור בין קבצים. אמת מה-URL.

### 2.2 Image handling

- **stacked imageRefs ב-fill** — רוב artist/hero images כוללים 2-4 imageRefs (אמיתית + Figma placeholders כמו `cb002e02e2090...`, `41074587...`, או hero של דף אחר). **בחר לפי סמנטיקה, לא לפי "אחרון בסטאק".** הורד רק את הרלוונטי.
- **`cropTransform` לא חובה** — `object-fit:cover` ב-CSS פותר. הוסף רק אם תוצאה ויזואלית שונה דרסטית.
- **בדוק קיום + hash compare לפני הנחת reuse.** ב-about, 7/7 התמונות היו חדשות למרות שצפיתי ל-hero משותף.
- **`portrait.png` קיים → אל תדרוס.** שמור figma version כ-`portrait-v2.png` ועדכן ב-`artists.json`.
- **נתיבי קבצים בעברית** (`url("פונטים/...")`) עובדים בדפדפנים מודרניים בלי URL-encoding.

### 2.3 Page architecture

- **Dynamic template + thin shells** לסטים גדולים. דפוס: `pages/<type>.html` קורא `?slug=`/`?id=` מ-URL + fetches JSON. shells per-slug `location.replace()`. דוגמאות: `pages/exhibition.html`, `pages/artists/artist.html`, `pages/opencall.html`. **כניסה חדשה = JSON entry, אין שינוי קוד.**
- **שדות אופציונליים ב-JSON** מאפשרים variant per slug בלי קוד-מותנה (`opencalls.json::emotional_triggers`, `gallery_images`). הרנדרר בודק קיום ומדלג בשקט.
- **fetch + inline JSON fallback.** ב-`file://` חלק מהדפדפנים חוסמים `fetch`. הטמע `<script type="application/json" id="fallback-...">`. **כשמעדכנים JSON — חייבים לעדכן גם fallback** עד שתהיה build infra.
- **Shared shell pattern** (nav+drawer+footer+JS): מועתק 1:1 בין דפים. אין partial loader. שינוי גלובלי = search-replace. **מעל 8 דפים — לעבור ל-partial loader.**
- **Hamburger drawer:** `.nav-links` הקיים *הוא* ה-drawer. אל תשכפל את הרשימה. `body.menu-open{overflow:hidden}`, `visibility:hidden` על drawer סגור (focus trap).
- **Logo → home בכל דף** עם `aria-label="home"`.
- **תוכן long-form (כתבות 5000+ מילים) נשאר ב-HTML**, רק meta ב-JSON. CMS עתידי יעביר.
- **דסקטופ + מובייל עם תוכן שונה → באתר חי, אחד.** Figma snapshot. הצג את כל התוכן בכל ה-viewports באופן רספונסיבי.
- **`exhibitions` ב-nav** מצביע ל-`pages/exhibition.html?id=loneliness`. כשייבנה `/exhibitions` index — search-replace בכל הדפים.

### 2.4 Lightbox

- **Event delegation** על `document.click` → תוכן מוזרק דינמית "תופס" בלי handlers. אחרי `innerHTML=...` קרא `ArtworkLightbox.refreshFocusable()` רק לנגישות-מקלדת.
- **font-stack של `.alb` הוא Copperplate בלבד** — עברית ב-caption נופלת ל-fallback מערכת. הוסף FbEzmel ב-`artwork-lightbox.css` אם נדרש.

### 2.5 Hebrew / data

- **שמות עבריים ב-Figma הם source of truth** ל-`name_he`. תמיד תמשוך, אל תשאיר null.
- **אומנים חדשים שמתגלים** בעת בנייה — עדכן `artists.json`, אל תתעלם.

---

## 3. New Lessons (template)

הוסף **רק** אם הוא לא חלק מ-§2. הפורמט: `**<topic>:** <one-line lesson>.` אם קיים — תחזק את ה-§2 הקיים.

---

## 4. Recent Lessons (timestamped)

- **2026-08-20 — אומן בלי תערוכה = קבוצת `exhibition_title_he` עם `exhibition_slug:null`, לא מסלול חדש:** שירה טורבוביץ׳ (SUMII) מציגה ב-POP UP ART RESIDENCY ולא בתערוכה מ-`exhibitions.json`. במקום ענף-renderer חדש, היצירות קיבלו `exhibition_title_he:"POP UP ART RESIDENCY"` + `exhibition_slug:null` + `exhibition_route:"sponsors/sumii"` + רשומת `exhibition_statements` — וה-renderer הקיים של דף האומן (כותרת-קבוצה→קישור, סטייטמנט, גריד) וגם עמוד היצירה הבודדת מציגים בדיוק את מבנה הפיגמה. `exRank(null)` מחזיר ‎-1 ⇒ קבוצה כזו תמיד תשקע מתחת לתערוכות אמיתיות אצל אומן רב-תערוכתי — התנהגות רצויה. **חוק:** לפני שמוסיפים שדה/ענף — לבדוק אם צירוף של שדות nullable קיימים כבר מייצר את העיצוב. **בונוס-1:** קרדיט מעורב `"שירה טורבוביץ׳ | sumii"` ב-`artist_he` נפל ל-fallback מערכת בגריד `/works/` ובכרטיסי דף-האומן כי שני ה-renderers עטפו אותו ב-`esc()` בלבד (אותו דפוס של לקח 2026-06-16 ג׳, הפעם בשדה `artist_he`) — תוקן: `latHtml`/`mixedHtml` כשיש Latin בתוך השם. **בונוס-2:** 🔴 `tools/seo/inject.py` כשהוא רץ על כל האתר **דורס תיאורים ידניים** (meta description/og/JSON-LD) ב-~90 דפי אירועים/כתבות שנכתבו אחרי עידן ה-overrides — הכלי מייצר תיאור-ברירת-מחדל מהדאטה לכל דף שאין לו רשומה ב-`tools/seo/overrides.json`. שוחזרו מ-HEAD. **לפני הרצה גלובלית:** `git diff --stat` אחרי, ולשחזר כל דף שלא התכוונת אליו; לדף חדש עדיף להוסיף רשומת override **לפני** ההרצה. (גם `og_gen.py` מצפה ל-`/tmp/seo_inject.py` — להריץ עותק שמצביע ל-`tools/seo/inject.py`.)

- **2026-08-19 — שני אריחים שנושאים את אותה תמונה = פלייסהולדר, לא גלריה של שתיים:** ב-`events/risa-and-noemi/` נוספה בפיגמה רצועת תמונות (`1578:5349` דסקטופ / `1578:5334` מובייל) עם **שני** אריחים 481×841 + peek ריק דהוי. רינדור-node הראה ששני האריחים מציגים **בדיוק את אותו צילום** — המעצבת שכפלה אריח כדי לצייר את הרעיון, לא כדי לבקש שתי תמונות. **חוק:** לפני שבונים קרוסלה/גריד לפי מספר האריחים — לרנדר ולוודא שהתוכן באמת שונה; אריח כפול נבנה **פעם אחת** בגאומטריה המצוירת. וכשנשארת תמונה בודדת מתוך פריסת 2-up מובייל (כאן 152×266 לכל אריח), האריח היחיד עולה לגודל אריח-התמונה המובייל של האתר (216×378 — אותו אספקט), אחרת הוא נראה כמו תקלה. **בונוס — סדר ה-fills במפלט ה-MCP אינו אינדיקציה לנראות:** באריחי הדסקטופ היו שני image fills (`c6d4b822…` ואז `74f7d4a0…`) והרינדור הראה שדווקא **הראשון** נצבע למעלה; במקביל בפריים המובייל ה-fill השני לא קיים בכלל — הסימן הקלאסי ל-leftover של הדבקה. ⇒ מרנדרים את ה-node ומכריעים לפי מה שנראה, ואם השכבה החבויה היא תוכן אמיתי (כאן: צילום אחר מהאירוע) — שואלים את המשתמש במקום להחליט לבד.

- **2026-08-23 — "התבנית מנצחת את הפריים" זו הנחה, לא כלל — תשווה לפריים-אח לפני שמכריעים:** בדף `artists/melani-hekimoglu/` הפריים (`1634:214`) מצייר את השם המלא ב**שורה אחת Bold**, בעוד ה-renderer המשותף מפצל תמיד ל-`.first` Bold + `.last` Light. הפיתוי היה לומר "זו סתם דרך שהמעצבת כותבת שם, התבנית קובעת" ולהשאיר את הפיצול. **הבדיקה שהכריעה:** רינדור של פריים אומנת אחרת מאותה משפחה — `hadas-tuval 797:106` — שמראה במפורש "HADAS" / "TUVAL" בשתי שורות עם משקלים שונים. כלומר המעצבת **כן** מבדילה, והשורה האחת אצל מלאני מכוונת. **חוק:** כשפריים סוטה מהתבנית המשותפת, אל תכריע מהזיכרון לאיזה צד — רנדר פריים-אח קיים שכבר מומש באתר; אם האח תואם לתבנית, הסטייה אמיתית ויש לממש אותה page-local. (אותו כלי-הכרעה שימש גם ל-fills: `724f6d54…` התגלה כצילום קטלוגי ממותג "SHORASHIM" מתחת לשתי יצירות — leftover.)
- **2026-08-23 — לפני פיצול (fork) של renderer משותף: לבדוק מה `tools/sync_works_mirrors.py` משכתב:** הפריים של מלאני מציג סטייטמנט-רזידנסי **בלי כותרת-קבוצה**, בעוד `buildExGroups` מרנדר `header` רק `if(g.title)` — ולכן נדרש פיצול מקומי בדף אחד. הסיכון האמיתי אינו העיצוב אלא ש**הסקריפט מחזיר לאחור אזורי renderer שלמים** (`OLD_GROUP_JS`→`NEW_GROUP_JS`, `OLD_JS`→`NEW_JS`) בכל `artists/*/index.html`, בשקט. קריאה של הסקריפט הראתה שהעוגנים שלו הם מחרוזות מדויקות שאינן חופפות ל-gate של הקיבוץ ול-`if(g.title)` ⇒ הפיצול בטוח; אומת בהרצה חוזרת (0 אזהרות `anchor not found`). **חוק:** לפני עריכת renderer שמופץ ל-N דפים — `grep` בסקריפטים ב-`tools/` אחרי המחרוזת שאתה משנה, והרץ אותם מיד אחרי כדי לוודא שהם עדיין מוצאים את העוגנים. **נלווה:** לוקאפ `exhibition_statements` הוא `===` על `exhibition_title_he`, ולכן "קבוצה בלי כותרת" מחייבת **`null` מפורש בשני הצדדים** (מפתח חסר ⇒ `undefined !== null` והסטייטמנט נעלם בלי שגיאה).
- **2026-08-18 — "בלעדי לגלריה" הוא מצב תוכן, לא כלל עיצובי:** `#perfume-savlanut` נבנה בלי CTA כי ב-2026-08-04 לא היה עמוד מוצר באתר הבישום. כשהעמוד עלה (`parfume-incense-amber-and-leather`) — הוחלף הטקסט הבלעדי באותו כפתור `.pp-cta` של how-many (אותו copy, URL אחר). **חוק:** `exclusive_he` / `cta_*` nullable ב-`homepage.json::perfume_promo.perfumes[]` — כשמופיע עמוד מוצר, מעתיקים את ה-CTA הקיים ולא משאירים את ה-copy האופליין.
- **2026-08-18 — תמונה בודדת עם dots מוסתרים ≠ קרוסלה:** ב-`events/alice-debellis/` נוסף סקשן `.event-photo` מ-Figma `1560:4169` (Frame 268, 1440×969). הפריים מכיל תמונה אחת 481×841 **וגם** רצועת slideshow-dots מוסתרת (leftover מתבנית). **חוק:** שכבה `hidden` לא נבנית — זו תמונה סטטית, לא `<image-gallery>`. אין frame 390; במובייל הסקשן נכנס בין `.h-address` (order 6) ל-`.event-artists` (order 8) דרך `order:7` על אותו DOM, כי `.event-hero` הוא `display:contents`.

- **2026-08-12 — "התערוכה החדשה למעלה" בדף האומן: לא לסמוך על `artist_page_pos` שיחזיק את זה:** הכרעת משתמש — בדף אומן קבוצות התערוכות מסודרות מהחדשה לישנה. בבדיקה התברר שהסדר **כבר** היה נכון בכל 8 האומנים הרב-תערוכתיים, פשוט כתוצר-לוואי של ה-`artist_page_pos` שנגזר מהפיגמה — כלומר הכלל התקיים במקרה ולא היה שום דבר שמונע ממנו להישבר בהוספת יצירה. המימוש: מיון קבוצות לפי מיקום התערוכה ב-`exhibitions.json` (מערך כרונולוגי עולה ⇒ מיון יורד), עם מירור inline רביעי `__EX_ORDER_INLINE__` שהסקריפט מזריק. **חוק:** כשמשתמש מבקש כלל-סדר שכבר "עובד" — לוודא שהוא **נאכף** ולא מקרי, ולבדוק פונקציונלית עם דאטה מכוון-הפוך (כאן: הזרקת `artist_page_pos` שמקדימה את how-many — הרינדור עדיין העלה את הקולפן למעלה). שמירה על שוויון-דירוג כ-stable sort חשובה לא פחות: שתי קבוצות של אותה תערוכה (למשל הקולפן + "הקולפן | interdependence") חייבות להישאר בסדר הפיגמה.
- **2026-08-12 — יצירה משותפת: המחבר בקרדיט חייב להיות `×` ולא `x`, ורשומה אחת מנצחת שכפול:** ביצירה `livay-levi-3` ("interdependence", ליוואי לוי × הדס טובל) הקרדיט בפיגמה כתוב `ליוואי לוי x הדס טובל` — ה-`x` ב-Copperplate בתוך טקסט עברי. בדיקת `fontTools` על מחסנית הפונטים הראתה ש**ל-FbEzmel אין בכלל גליף `x` לטיני (U+0078), אבל יש `×` (U+00D7)** ⇒ `collab_connector_he:"x"` היה נופל ל-fallback סיסטמי בתוך `.artist is-he` (הרנדררים מריצים `escapeHtml` על המחבר, לא `mixedHtml`, ולכן הוא לא מקבל `<span class="lat">`). הפתרון: `"×"` — נראה זהה, לא שובר את כלל-הזהב על אנגלית ב-Copperplate, ותקדים קיים בדאטה (`zohar ron × dandan nusha`). **חוק:** לפני שמכניסים תו לטיני בודד לתוך מחרוזת עברית ב-JSON — לבדוק שהגליף קיים ב-FbEzmel, או לבחור מקבילה טיפוגרפית ניטרלית. **בונוס-ארכיטקטורה:** להצגת אותה יצירה אצל שני אומנים — **רשומה אחת** עם `artist_pages`+`collab`+קבוצה ייעודית (`exhibition_title_he` משלה) + `exhibition_statements` לכל אומן. הקיבוץ בדף אומן הוא לפי `exhibition_title_he` **מדויק**, ולכן רשומה משותפת לא יכולה להתמזג לשתי קבוצות קיימות שכותרותיהן שונות — קבוצה ייעודית היא לא קישוט אלא הכרח. שכפול לשתי רשומות היה מציג את אותה תמונה פעמיים בגריד `/works/` (ואצל שני אומנים שיצירותיהם סמוכות במערך — גם זו ליד זו, מה שנקרא באג).

- **2026-08-06 — אימות scrim מול רינדור Figma: `%[fx:mean]` מכליל את ערוץ האלפא ומהפך את המסקנה:** בבניית `events/zohar-ron-medina/` ה-API החזיר על ה-hero שני fills — `rgba(27,27,27,.4)` ותמונה — בסדר שנראה כמו "ה-scrim שקע מתחת לתמונה" (התקדים של `the-space-between`). מדידת בהירות על רינדור ה-node נתנה 0.679 מול 0.517 בקרופ הנקי, כלומר "הרינדור בהיר יותר" ⇒ לכאורה אין scrim. **זו הייתה טעות מדידה:** רינדורי `download_figma_images` הם PNG עם ערוץ אלפא, ו-`magick -colorspace gray -format "%[fx:mean]"` מחשב ממוצע על **כל** הערוצים כולל האלפא — `(0.358+1)/2 = 0.679`. אחרי `-background white -alpha remove -alpha off` הממוצע האמיתי 0.3577 מול 0.3523 בקרופ + scrim מחושב, ו-RMSE 5.9% שכולו טקסט האוברליי הלבן ⇒ **ה-scrim כן מוחל**. **חוק:** לפני כל השוואת בהירות/RMSE מול רינדור Figma — `-alpha remove -alpha off` על שני הצדדים; ואם המסקנה היא "אין scrim" למרות שהעין רואה כיסוי, לחשוד קודם בערוץ האלפא ולא במעצבת. (בונוס: תמונות imageRef עלולות לחזור בפרופיל **Adobe98** — המרה ל-sRGB שינתה את הממוצע ב-0.4% בלבד, כלומר לא מסבירה פערי בהירות גדולים.)
- **2026-08-06 — אירוע שני לאותו אומן = slug לפי הגלריה, וכיול שבירת-שורה לכל breakpoint בנפרד:** לזוהר רון נוסף אירוע שני (הקולפן/כיכר המדינה) בזמן שה-slug `zohar-ron` תפוס ע"י אירוע ה-how-many/דיזינגוף שלו ⇒ `zohar-ron-medina` (כלל-זהב 6 אוסר לשנות slug קיים). בנוסף: הכותרת בפיגמה נשברת **אחרי "talk"** בדסקטופ ו**אחרי "with"** במובייל — כיול גודל בלבד (80→64) שיחזר את הרוחב אבל לא את נקודת השבירה, ולכן שני ה-breakpoints קיבלו class נפרד (`.dbr`/`.mbr`). אימות: מדידת קבוצות-שורות של פיקסלים כהים (רוחב ink לכל שורה) מול רינדור הפריים — כותרת 461/635px אצלנו מול 440/615 בפיגמה (+3-5%, בתוך סבילות המשפחה), עברית וכתובת בהתאמה מלאה (±1%).

- **2026-07-30 — `#events-upcoming` overlay: שדה ב-JSON ≠ שדה מרונדר:** ב-`#events-upcoming-data` כבר היה `ex_bar:["exhibition","volume 2"]` / `["the exhibition"]`, אבל ה-renderer דילג עליו והציג רק `title_en`+`ex_name`. Figma `1258:746` דורש פס Solway עם נקודה לבנה בין חלקים + תאריך Bold 14 (לא Light 20) + `HAMEDINA` (לא `medina`). **חוק:** כשמתקנים אוברליי מול Figma — לבדוק קודם אם השדה כבר ב-JSON ורק חסר ב-HTML; cqw לכרטיס חייב להיות מכויל לבסיס ה-Figma (כאן 200px), אחרת בדסקטופ 3-up (~183px) Copperplate מתנגש בתאריך.

- **2026-07-26 — קריאת טקסט verbatim מ-Figma בלי MCP (חשיפת tali-zelnik):** כשה-MCP של Figma לא מחובר, אפשר לאמת תוכן דרך Chrome על figma.com ב-Dev Mode: **שמות שכבות הטקסט בפאנל Layers = תוכן הטקסט המלא**, והפאנל הוא DOM רגיל — מרחיבים את העץ (חצים; alt+click לא עושה expand-all) וקוראים עם `get_page_text` — מקבלים עברית מלאה מדויקת בלי OCR. ⚠️ שבירות פסקאות לא נשמרות בשם השכבה (מאמתים ספירת פסקאות בזום על הקנבס); גלילת wheel/keyboard-zoom על הקנבס לא עובדות אמין דרך CDP — ניווט = בחירת שכבה בפאנל (ממקמת את הבחירה) או תפריט הזום. אימות זהות תמונות בלי הורדה: **מידות המקור בסקשן Assets** (למשל 850×1276) מול `sips -g pixelWidth` על ה-webp המקומי — התאמה מלאה = אותו קובץ.
- **2026-07-15 — `__ARTISTS_INLINE__` הוא אובייקט, הבדיקה ציפתה למערך — כל 42 דפי האומן היו שבורים ב-file://:** ה-regen של המירור (json.dumps של כל `artists.json`) כותב `{"$schema":...,"artists":[...]}`, אבל ה-inline-first check היה `__ARTISTS_INLINE__.length` (undefined על אובייקט) → נפילה ל-`fetch` שנכשל ב-file:// → "ARTIST NOT FOUND". ב-HTTP (GitHub Pages) ה-fetch עובד, ולכן אף אחד לא שם לב. **תיקון:** הבלוק מפרק `.artists` לפני הבדיקה (`var __AI=window.__ARTISTS_INLINE__; if(__AI&&__AI.artists)__AI=__AI.artists;`) — הוחל על כל `artists/*/index.html` + `_staging`. **חוק:** מירור inline ו-renderer חייבים להסכים על shape (מערך vs אובייקט-עוטף) — אחרי כל regen לבדוק דף אחד ב-file:// אמיתי, לא רק ב-HTTP.
- **2026-07-15 — עדכון סדר עבודות בדף אומן (amnon, Figma `798:173`):** גריד הפיגמה ממולא LTR (`gridColumn` 1 = שמאל) אבל `.works-grid` באתר הוא `direction:rtl` — לכן **הרצף** (קריאת LTR של הפיגמה: שמאל→ימין, למעלה→למטה) הוא הקנוני ל-`artist_page_pos`, והשורות באתר יוצאות ממוררות פר-שורה מול הפיגמה. זו הקונבנציה מאז שהוקצו ה-ids. בונוס אימות: פריט בודד בשורה אחרונה נופל ב-RTL באופן טבעי לימין — ואכן המעצבת מיקמה אותו ב-`gridColumn:2` (ימין) בפיגמה, מה שמאשר את הקריאה. אימות סדר = צילום headless (`--headless --screenshot`, רוחב 1440; ⚠️ ברוחב 390 ה-headless לא מפעיל אמולציית מובייל וה-media queries לא נתפסים — נראה קטום, זה ארטיפקט ולא באג).

- **2026-07-09 — Audit רוחבי (48 ממצאים, 35 תוקנו ב-`b18bacc`) — שלושה דפוסי-באג חוזרים:** (1) **החלפת `src` על `<img>` עם `srcset` לא עושה כלום ויזואלית** — הדפדפן ממשיך לבחור מ-`srcset`; ובנוסף `picture-upgrade.js` מדלג במכוון על imgs עם srcset. **חוק:** JS שמחליף תמונה חייב `removeAttribute('srcset')`+`('sizes')` **לפני** שינוי `src` (הסדר קריטי — כך ה-MutationObserver של picture-upgrade מעדכן את ה-`<source>` avif). קרה בפרומו הבשמים בהומפייג'. (2) **העתקת עמוד-תבנית בלי לעדכן את כל ה-head** — `works/elsa-ars-brush-6` נשא בלוק SEO שלם של talia-zoref-1, ו-talia-zoref-2/3 נשאו את הכותרת בתגי title/og; `press/time-out` נשא JSON-LD/og של the-last-station. **חוק:** אחרי שכפול תבנית, לעבור על **כל** תגי ה-head (title, description, canonical, og:*, twitter:*, JSON-LD) מול ה-data של הדף. (3) **דפוסי JS "נראה-תקין" שמתים בשקט:** `el.tabIndex = el.tabIndex || 0` לעולם לא מציב 0 (ברירת מחדל -1 truthy → `hasAttribute`); `!img.loading` תמיד false (ה-property מחזיר 'eager'/'auto' → לבדוק attribute). נתפסו ב-triptych-gallery/gallery. **שיטת האימות ששרדה:** סריקה סטטית של כל href/src/srcset מול הדיסק + `sync_works_mirrors.py` כבדיקת-drift (רץ נקי = מירורים מסונכרנים) + fan-out סוכנים לפי אזורי אתר עם אימות אדברסרי לכל ממצא (6/54 נפסלו).

- **2026-06-16 (ג) — עמוד היצירה לא עטף אנגלית ב-Copperplate (כלל-זהב §3/§4 + לקח 2026-05-13):** ב-`works/<id>/` ה-renderer רינדר את **כותרת התערוכה** (`exhibition_title_he`), **גוף ה-statement** (`statement_he`) ו**שם אומן לטיני** (sami-david, la-raz-porta) דרך `esc()` בלבד ⇒ האנגלית נפלה ל-serif fallback של `var(--heb)` במקום Copperplate (היה כבר `detailsHtml()` ל-`details_he` בלבד). **תיקון:** נוספה `latHtml()` (אותו `LATIN_RUN` של דפי האומן, רץ על raw + `esc()` per-segment) + class `.lat` (Copperplate UPPERCASE, `unicode-bidi:isolate`, `font-size:.86em` להתאמת גודל ויזואלי) — הופעל ב-111 עמודי היצירה. **חוק (חיזוק):** כל renderer שמוציא טקסט מ-JSON שעלול להכיל אנגלית — חובה לעטוף runs לטיניים ב-`.lat`/Copperplate; `esc()` לבד = bug פונט. בדוק את **כל** השדות (כותרת/אמן/פרטים/גלריה/תערוכה/statement), לא רק את הברור.

- **2026-06-16 — 🔴 `overflow-x` על `<body>` חותך גם **אנכית** את ראשי האותיות (Copperplate), לא רק שובר sticky:** 34 דפי האומן עדיין נתנו `body{overflow-x:hidden}` inline (לא הוגרו אחרי לקח 2026-06-15). תופעה נוספת מעבר ל-sticky: **כל `overflow-x` לא-`visible` מקדם את `overflow-y` מ-`visible` ל-`auto`** (כלל ה-CSS: אם ציר אחד לא-visible, השני הופך auto) → ה-`body` הופך scroll-container אנכי **שחותך תוכן שגולש מעל תיבתו**. שמות ה-hero (Copperplate 60–80px, `line-height:1`) — ה-ink של האותיות גולש ~12–18% **מעל** ה-line-box (מטריקות הפונט: cap-height > ascent), אז הצמרות נחתכות ("חתוך מלמעלה"). **תיקון:** הוחלף ל-`html{...;overflow-x:hidden}` + `body{...;overflow-x:visible}` בכל 34 הדפים (הדפוס שכבר ב-`works/<id>/`). ⚠️ **ה-override ב-`site-chrome.css` (`body{overflow-x:visible}`) לא מנצח inline** — הוא מוזרק לפני ה-CSS של הדף בקסקייד, אז כלל inline על `body` גובר; **חייבים לתקן במקור, לא לסמוך על ה-chrome.** **חוק עבודה — בדיקת חיתוך:** אחרי כל שינוי layout/typography, בדוק שכותרות (במיוחד Copperplate caps ב-`line-height ≤1.2` בראש קונטיינר) לא נחתכות מלמעלה. אימות דטרמיניסטי: puppeteer-core (`./node_modules`) → `element.screenshot()` על הכותרת (חותך בדיוק לתיבה ⇒ חיתוך אמיתי נראה), או השוואת `range.getClientRects().top` מול `el.getBoundingClientRect().top`. **לעולם אל תוסיף `overflow-x` (hidden/clip) על `body`/קונטיינר עם כותרות.**

- **2026-06-16 (ב) — חיתוך-מלמעלה של Copperplate ש"מסתדר ב-hover" = glitch של גבולות-paint, לא layout:** ב-`.ex-heading` בדפי האומן (כותרות תערוכה כמו `How Many Partners Have You Had? | sex?`) ראשי האותיות של ה-`<span class="lat">` (Copperplate) נחתכו מלמעלה **רק בפיינט הראשון**; **ריחוף עכבר תיקן** (ה-`transition:opacity` על ה-`<a>` מאלץ repaint שמצייר את הגליף המלא). סימן-היכר נוסף: התו `|` (שמרונדר ב-FbEzmel של ההורה, יושב נמוך בתיבת השורה) **לא** נחתך — רק ה-Copperplate. **שורש:** ה-ink של ה-caps נוגע **בדיוק** בקצה העליון של תיבת ה-paint (נמדד ink-top == box-top, `overAbove:0`), והדפדפן חותך את שורת הפיקסלים העליונה בפיינט הראשון. ⚠️ **לא ניתן לשחזר ב-headless** (glitch תלוי-GPU/compositing) — הופיע רק בדפדפן אמיתי; אל תסיק "אין באג" מ-headless נקי. **תיקון:** `.ex-heading{...;padding-top:.15em}` (דוחף את ה-caps מקצה התיבה ⇒ לפיינט הראשון יש מרווח). הופעל ב-31 דפי אומן (3 ה-placeholder הישנים בלי `.ex-heading`). אם נשאר: להעלות `line-height`, או `transform:translateZ(0)` להכרחת layer יציב (סיכון טשטוש). **חוק:** טקסט Copperplate/decorative caps שנוגע בקצה תיבתו = סיכון paint-clip; תמיד תן מרווח (padding/line-height), אל תשאיר ink-top == box-top.

- **2026-06-15 — 🔴 Sticky header נשבר מ-`overflow-x` על `<body>` (כל האתר):** ההידר (`site-header{position:sticky;top:0}`) נגלל החוצה במקום להיצמד, בכל הדפים, דסקטופ ומובייל. **שורש:** `components/site-chrome.css` נתן `body{overflow-x:hidden}` (וחלק מהדפים inline נתנו `overflow-x:clip`). **כל overflow לא-`visible` על ההורה של אלמנט sticky הופך את ההורה ל-scroll-container שלו** — ואז ה-sticky נצמד לתיבת ה-`body` (שזזה בתוך ה-`html`) במקום לחלון, ולכן נגלל החוצה. **`clip` שובר בדיוק כמו `hidden`** (סותר לקח 2026-05-11 למטה — תוקן). **פתרון:** חיתוך אופקי **רק על `<html>`** (`overflow-x:hidden` = ה-scroll-container ברמת החלון; sticky עובד), ו-`body{overflow-x:visible}`. תיקון יחיד ב-`site-chrome.css` (מוזרק ב-JS אחרי ה-CSS של הדף ⇒ דורס inline ⇒ מתקן את כל ~50 הדפים במכה). **אומת ב-Chrome CDP** (גלילת wheel אמיתית): התוכן נגלל 600px, `header.top` נשאר 0, בדסקטופ ובמובייל, בדפים משתי קבוצות ה-overflow. **חוק:** אסור `overflow-x` (hidden/clip) על `body` — רק על `html`.

- **2026-06-10 — דפי אומן קולפן (סקשן `XhGH...::560:3384`, 14 frames מובייל):** הזרקת תוכן ל-8 דפי placeholder + עדכון ביו ל-4 אומנים קיימים + 7 יצירות חדשות לניר ג'ורג'יו לוין. **לקחים:** (1) **🔴 שמות בפריימים משקרים גם ברמת התוכן** — frame של livay-levi נושא Profile Text "lahav barak" (טעות העתקה של המעצב); לזהות אומן לפי הביו והיצירות, לא לפי שם הפרופיל. (2) **פסקאות leftover בין פריימים** — בביו של chen-ziv הודבקו 2 פסקאות של ג'סיקה טברובסקי ("הציור התנערות..."); כשפסקה לא מתיישבת עם המדיום/יצירה של האומן — לבדוק מול frames אחרים בסקשן ולסנן. (3) **רוב יצירות האומנים כבר היו ב-`art_works[]`** מסנכרון עמוד works — לפני הוספה להשוות frame↔JSON לפי כותרות (התגלה רק פער אצל ניר 9↔2, ותיקון כתיב אצל אלסה "הסרף"→"השרף"). (4) הוספת יצירות שלא קיימות בעמוד works: להזריק צמוד ליצירות הקיימות של האומן כדי לשמר גם את סדר דף האומן וגם את בלוקי הגלריה. (5) ב-dump של MCP, `\_` הוא גם underscore אמיתי (האנדלים) וגם nbsp (פרוזה) — לפענח לפי הקשר. (6) **סדר היצירות בדף אומן ≠ סדר עמוד works** — אותן רשומות, שני סדרי Figma שונים (5 אומנים היו שונים, כולל ירדן אמיר בסדר הפוך כמעט לגמרי). נפתר עם שדה `artist_page_pos` ב-`art_works[]` + מיון ברנדרר של דפי האומן בלבד; אסור לסדר מחדש את המערך עצמו (שייך לעמוד works).

- **2026-06-10 — Artists index page (`/artists/` — Figma `landing` `523:107` דסקטופ / `523:2` מובייל):** גריד 28 אומנים (3 עמ' דסקטופ / 2 מובייל), כרטיס = תמונה ריבועית (cover) + שם דו-שורתי → `/artists/<slug>/`. **לקחים:** (1) **שמות אומנים — UPPERCASE למרות ש-Figma מציג lowercase (`textCase:LOWER`).** כלל-זהב §3 דורס את ה-mockup; אומת מול רצועת "our artists" בהומפייג' ש-`.nm` שלה כבר מרונדרת UPPERCASE דרך `body{text-transform:uppercase}` הגלובלי. צבע אפור `#828282`, Copperplate Light, `white-space:pre-line` ל-2 שורות. (2) **8 אומנים חדשים בעיצוב שלא היו ב-`artists.json`** (gilad-kenan, jessica-tabarovsky, baruch-torgeman, chen-ziv, lahav-barak, livay-levi, natasha-zeriker, talia-zoref) → placeholder pages (cp של `alon/index.html` + החלפת `data-slug`); הרנדרר מזהה `!bio_he && works.length===0` → hero placeholder (שם+פורטרט). `portrait` = תמונת הגריד. רשומים ב-`_pending_artist_pages`. (3) **`__ARTISTS_INLINE__` חייב regen בכל `artists/*/index.html` בעת הוספת אומן** — כל דף מטמיע inline את כל מערך `artists.json` ומוצא את עצמו לפי slug; סקריפט python `re.sub(r'<script id="artists-inline">.*?</script>', ...)`. בלוק נפרד מ-`__ART_WORKS_INLINE__`. (4) תמונות גריד: 28 PNG מ-Figma → `images/artists/grid/<slug>.{webp,avif}` ריבועיות `-resize 810x810^ -gravity center -extent 810x810` (+480w) — framing אחיד.

- **2026-06-02 — Responsive images (srcset) ל-109 תמונות כבדות:** וריאנטים `-480w/-768w/-1080w` (webp+avif) לכל תמונה סטטית ≥80KB+≥800px, מוזרקים כ-`srcset` + `sizes="100vw"` ב-9 דפים (120 תגיות img). **תוצאה:** events/how-many ירד מ-12.5MB ל-2.6MB (-79%), homepage -55%, about -43%; ציון perf 75→83. **לקחים:** (1) `picture-upgrade.js` עודכן למפות srcset של webp ל-AVIF source (החלפת סיומת) + העתקת `sizes` — בלעדיו ה-AVIF source (URL בודד) היה דורס את ה-srcset. (2) `sizes="100vw"` הוא הברירה הבטוחה — הדפדפן אף פעם לא בוחר תמונה קטנה מדי (אין טשטוש), והרווח העיקרי הוא במובייל. (3) תמונות שמרונדרות ב-JS (דפי אומן/תערוכה/opencall, works grid) **לא** קיבלו srcset — דורש שינוי סכמת JSON (להוסיף widths) — follow-up. (4) קונבנציה חדשה בכלל-זהב §8.11.
- **2026-06-02 — Perf pass: defer + LCP priority (40 דפים):** (1) **`embed-iframe.js` חייב `defer`** — סקריפט sync בסוף body עדיין חוסם DOMContentLoaded; הוא no-op מחוץ ל-iframe אז defer = רווח נקי. (2) **LCP עם `loading="lazy"` = אנטי-פאטרן** — נמצא ב-22 דפי אומן (פורטרט יחיד), 2 opencalls (hero), ובגריד works כל 12 התמונות היו `fetchpriority="high"` (=אף אחת לא מועדפת). תוקן: רק תמונה ראשונה high, השאר lazy. דפוס: `(i?' loading="lazy"':' fetchpriority="high"')`. (3) **🔴 אסור `media="print"` async-CSS על קומפוננטה שנוגעת ב-DOM סטטי** — `artwork-lightbox.css` מכיל `[data-artwork-src]{cursor:zoom-in}`; טעינה מאוחרת = הסמן מופיע באיחור בחיבור איטי. נוסה ובוטל. גם stacked/triptych/gallery.css חייבים להישאר blocking (מעצבים HTML סטטי). (4) **🔴 Lighthouse על iCloud Drive = רעש קיצוני** — אותו קוד בדיוק נמדד LCP 8.9s→11.9s→33.1s בריצות עוקבות (sync I/O). להשוות מדידות רק back-to-back באותו חלון זמן, ולסמוך על הציון (P) יותר מאשר על LCP הסימולציה. (`/events/how-many/` — אירוע הפתיחה):** נבנה מ-Figma `landing` `433:2` (דסקטופ) + `433:179` (מובייל), על בסיס מבנה loneliness (האירוע התאום) אבל עם CSS נקי (בלי ה-legacy nav/footer שנשאר ב-loneliness). מבנה: hero (כותרת+מטא+טקסט אוצרותי | תמונה נקייה ללא overlay) → the invitation (triptych + 7 dots) → the atmosphere (תמונה עם gradient fade | רשימת 15 אומנים) → גלריה שנייה (3 dots) → moments from the opening (14 thumbs scroll-snap). **לקחים:** (1) **סדר מובייל שונה מדסקטופ בכמה רמות** — הטקסט אחרי גלריה 1, גלריה 2 לפני atmosphere; נפתר עם אותו דפוס `display:contents` + `order` מ-close-look. ה-atmosphere נשאר סקשן אטומי (label+image+artists בעמודה) כי האומנים צמודים לו בשני הסדרים. (2) **קישורי אומנים בעקבות exhibitions.json** — `דן בן-אבי` מקושר ל-`zohar-ron-dan-ben-ari` (דף השת"פ, לפי המיפוי בתערוכה); `רחלי ראובן` נשארת טקסט (לא רשומה). (3) **Reuse:** ה-hero של הדף = `event-how-many-2026-05-26-cover.webp` הקיים מההומפייג' (אומת RMSE), רק 20 תמונות חדשות ירדו (invite/atmosphere/second/opening-01..14) ל-`images/events/how-many/`.
- **2026-06-01 — Event page close-look (`/events/close-look/` — מבט מקרוב):** נבנה מ-Figma `landing` `420:1520` (דסקטופ) + `420:1625` (מובייל), על בסיס מבנה ktuba עם התאמות. **לקחים חדשים:** (1) **Reuse לפני הורדה** — 4 מתוך 5 ה-imageRefs בעיצוב היו תמונות שכבר קיימות באתר (hero ≈ `event-5-cover`, slides = `tanya-shin/work-01/02`, פורטרט = `portrait-v2`); אומתו עם `magick compare -metric RMSE` (<2% = זהה). הורדנו בפועל רק תמונה חדשה אחת. (2) **סדר sections שונה במובייל** (תמונה→כותרת→גלריה→טקסט→אומנים, הטקסט אחרי הגלריה!) נפתר עם `display:contents` על `.event-hero`/`.row`/`.text-col` במובייל + `order` על הילדים — DOM אחד, שני סדרים, בלי שכפול. (3) **Triptych עם fit שונה למרכז ולצדדים:** `.is-center{object-fit:contain;background:#F9F9F9}` + peeks `cover` (Figma מציג עבודה ריבועית במלואה במרכז). (4) **קישור אומן על slides:** עטיפת `<img class="tri-slide">` ב-`<a href="artist">` עובדת — הקומפוננטה עושה `preventDefault` על קליק-peek (סיבוב) ומאפשרת ניווט רק בקליק על המרכז; עומד בבדיקת artist-linking §7. (5) **Hero גרעיני (film grain) לא נדחס** — חיתוך מראש ל-3:4 (היחס הנראה בפועל) + `cwebp -q70 -sharp_yuv` הוריד 2.9MB→572KB.
- **2026-06-01 — Homepage `#press` full resync (nodes `428:430` / `428:253`):** הסקשן עבר לסדר ומדיה חדשים עם 8 כרטיסים (כולל `HOW MANY PARTNERS HAVE YOU HAD?` ב-`26.5.2026` והחזרת `דה פיינל קאונטדאון` ב-`31.12.2025`). כדי לשמור התאמה בין desktop/mobile בלי כפילות markup: שומרים סדר DOM לפי מובייל, ומפעילים `press-row--swap` רק על השורות שצריכות היפוך ויזואלי בדסקטופ.
- **2026-06-01 — Homepage `#press` new event card order:** כרטיס אירוע חדש ("מבט מקרוב: מפגש פתוח בתוך התערוכה | טניה שין", `2.6.2026`) נוסף מ-Figma (`XhGH...::428:445` דסקטופ, `XhGH...::427:128` מובייל) ונכנס **ראשון למעלה** בתוך `press-rows`. בהוספת כרטיסים מעל הרשימה: מעדכנים גם `data/press.json` (entity חדש) וגם `data/homepage.json::press_section.item_ids` לפני שינוי `index.html`.
- **2026-05-31 — Homepage `#press` time-out refresh:** כרטיס `time out | תרבות` עודכן מתוכן+מדיה של Figma (`XhGH...::412:583` דסקטופ, `XhGH...::412:996` מובייל). כשיש עדכון כרטיס בלבד (לא כתבת long-form חדשה), מעדכנים את ה-card ב-`index.html` ואת meta הרשומה ב-`data/press.json` (title/date/cover + figma_home_card_*), בלי לשנות את דף הכתבה.
- **2026-05-14 — Homepage `#tribe-teaser` mobile strip:** מדיה קנונית — Figma `250:98` (`XhGH289YTRcW811wrufRJz`). ארבע המוזאות **לא** `<a href>` — `div.tribe-thumb-card` עם `data-artwork-src`/`data-artwork-title` בלבד; **ללא** `data-artwork-link` ⇒ לייטבוקס בלי כפתור מעבר לתערוכה. `data-artwork-gallery="tribe-teaser"` ל-prev/next; ניווט לתערוכה רק דרך `.tribe-teaser-head`. תמונות ב-`images/homepage/tribe-teaser/thumb-*.webp`; meta ב-`homepage.json` (`figma_gallery_strip`, `gallery_images[]`).
- **2026-05-14 — `exhibition.html` curator band:** סקשן `.ex-curator` מתחת ל־`.ex-artists` כש־`exhibitions.json` מגדיר `curator_slug` (loneliness + how-many → `korin-avraham`). עיצוב משותף עם רצועת האומנים (178×148 דסקטופ, 96×80 מובייל); תוכן הכרטיס מ־`curators.json` (`portrait`, `badge_en`, שם). קישור ל־`pages/curators/<slug>.html`. Figma: `301:2` + `119:702` (`XhGH289YTRcW811wrufRJz`).
- **2026-05-14 — Curator profile (`/curators/korin-avraham`):** דף ב-`pages/curators/korin-avraham.html` מ-Figma `314:4` + `314:97` (`XhGH289YTRcW811wrufRJz`). נתוני אוצרת ב-`data/curators.json`; כרטיסי תערוכות נבנים מ-`exhibitions.json` + `galleries.json` (אין שכפול כותרות/מיקומים). אוברליי כרטיסים תואם ל-`exhibition.html` (Solway + FbEzmel + `hero_overlay_en_only` ל-how-many). מובייל: סדר כרטיסים how-many→loneliness ב-`order:-1` על `.cur-card--how-many`. פורטרט משתמש ב-`images/curators/korin-avraham/portrait.webp` (ייצוא מ-Figma node `314:122`; LCP + `fetchpriority="high"`).
- **2026-05-14 — `<image-gallery>` seamless loop:** ב-`components/gallery.js`, כש־`data-loop` פעיל (ברירת מחדל) ויש לפחות שני slides, נבנית מסילה `[clone של אחרון] + מקוריים + [clone של ראשון]`. המעבר אחרון↔ראשון הוא אנימציה של פריים אחד; ב־`transitionend` מתבצעת התאמת מיקום ללא transition כך שלא עוברים ויזואלית על כל הסט. `gallery:change` והנקודות נשארים מבוססי אינדקס לוגי (0…n−1).
- **2026-05-11 — Event page loneliness (`/events/loneliness`):** נבנה ב-`pages/events/loneliness.html` מ-Figma `1213:1873`+`1213:2099` (`Zn3N3mBQkbYER7tTJMbCcz`). 22 imageRefs ל-`images/events/loneliness/`. מבנה: hero בנד אפור עם image card + טקסט גוף ארוך, שני slideshows, atmosphere (image+artists list), 14-thumb opening grid. במובייל: hero → תמונה full-screen + טקסט אחרי, slideshow → 3 תמונות עם peek-from-sides (left:-74, right:315 — יוצא מ-viewport מכוון), moments grid → סקרול אופקי 200px cards.
- **2026-05-11 — Share + Bookmark feature:** ב-`components/site-chrome.{js,css}` (single source of truth). כל דף עם `<site-header>` מקבל בחינם: bookmark badge, modal saved list, toast. הדף רק מציין item דרך `data-item-*` על `[data-bookmark-toggle]` + `[data-share-btn]`. localStorage key `zr-bookmarks`. תיעוד מלא ב-components.md §5.
- **2026-05-11 — Header bookmark icon = position:absolute:** `position:absolute; right:100%` של `.nav-right`. Default `display:none`, מופיע רק עם `.has-items` (`refreshCount()` ב-site-chrome.js). show/hide לא מזיז שום אלמנט.
- **2026-05-11 — Reusable `<image-gallery>`:** ב-`components/gallery.{js,css}`. דף `exhibition.html` הוא הצרכן הראשון — `gallery_images[]` ב-JSON. **כלל אצבע:** Figma → תמונה אחת + dots → `<image-gallery>`. תיעוד: components.md §2.
- **2026-05-11 — Event page ktuba (`/events/ktuba`):** נבנה ב-`pages/events/ktuba.html` מ-Figma frames חדשים ב-`landing` (`XhGH289YTRcW811wrufRJz`): desktop `119:1651`, mobile `119:1509`. הגרסה הישנה ב-`Zn3N…::1213:1644/1502` נשמרה כ-legacy. 4 imageRefs ל-`images/events/ktuba/` + 10 portraits ל-`images/events/ktuba/artists/`. מבנה: hero grey-band עם image card 492×744 (`EXHIBITION VOLUME 1`) ו-title+meta+poem בעמודה השנייה; slideshow band peek-main-peek 7-dots; paragraph תיאור; artists strip 11 כרטיסים. במובייל: image card → full-width 520h למעלה, title+meta+poem מתחת.
- **2026-05-11 — Triptych Gallery component:** הוצא מ-about.html (היה inline `atc-*`) ל-`components/triptych-gallery.{css,js}`. צרכנים: `about.html` (7 פורטרטים) + `events/ktuba.html` (slideshow band, 7 dots על 3 צילומים אמיתיים). CSS vars לקסטומיזציה פר-דף. תיעוד: components.md §4.
- **2026-05-11 — Figma frame moves across files:** frames של ktuba הועברו מ-`Zn3N…` ל-`XhGH…` ע"י המעצב. **figma_node_id מקבל משמעות רק יחד עם fileKey.** sitemap שומר canonical + legacy. נוסף field `figma_file` ל-`data/events.json`.
- **2026-05-11 — Mobile horizontal-scroll מ-titles גדולים:** הומפייג' נתן scroll אופקי במובייל בגלל `.ex-now .titles .l` (91px) ו-`.exhibitions-titles .b` (58px). **פתרון:** (1) `html{overflow-x:hidden}` בלבד למניעת scroll אופקי. ⚠️ **תוקן 2026-06-15:** **אסור `overflow-x` על `body`** (לא hidden ולא clip) — זה שובר את ה-sticky header (ראה הלקח למעלה); החיתוך חייב להיות רק על `<html>`. (2) `clamp(min, vw-value, max)` במקום fixed-px ל-≥48px. `vw-value` ≈ `figma_px / figma_viewport × 100`. (3) `white-space:nowrap` + `max-width:100%;overflow:hidden` על הקונטיינר. **חוק:** font-size ≥48px במובייל → תמיד clamp() + בדיקת word-width מול 320px.
- **2026-05-13 — Lang-aware font routing ל-JSON-driven text:** באג שהתגלה ב-`pages/artists/artist.html?slug=gal-polk` — כותרת work `״DON'T LOSE YOUR HEAD״` רוּנדרה ב-serif fallback של מערכת ההפעלה כי `.work .title` היה hardcoded `font-family:var(--heb)` ו-FbEzmel חסר Latin glyphs. אותה בעיה אותרה ב-`.alb-title`/`.alb-alt` (lightbox) וב-`.bm-title` (bookmark modal). **הפתרון:** זיהוי שפה בזמן render (regex `[א-ת]` — letters בלבד, גרשיים `״` לא נספרים) + class `is-he`/`is-en` שמפעיל font + direction + text-transform נכונים. דפוס מתועד ב-`docs/conventions.md §5.1` — להעתיק לכל renderer חדש מ-JSON. בנוסף, ב-`titleHtml()` ב-artist.html נוסף passthrough בטוח ל-`<span class="lat">` בלבד בתוך כותרות מ-JSON (טיפול ב-`la-raz-porta`, `zohar-shtrit` שמכילים markup מובנה). **חוק כללי:** כל אלמנט שמרנדר ערך מ-JSON שעלול להיות עברית **או** אנגלית — חובה lang-class. אסור להניח שפה.
- **2026-05-12 — Site-wide perf pass (כללי-זהב §8.11):** פעולות שביצעתי במכה אחת, כל אחת היא הרגל קבוע מהיום והלאה: (1) **Google Fonts הוסר** — Inter הוחלף ב-`var(--cop)` בכפתור Newsletter; חסך 3 בקשות חיצוניות חוסמות-render לכל דף. (2) **`src=*.png` הוחלף ב-`*.webp`** בכל ה-HTML/JSON — ה-PNG לא קיים בשרת (רק `_originals/` המקומי), אז קריאת `*.png` היתה 404 ואז JS היה ממיר. עכשיו הדפדפן מתחיל fetch מיד. עודכן `components/picture-upgrade.js` שיוסיף רק `<source type="image/avif">` כש-src כבר `.webp` (תאימות לאחור ל-`.png` נשמרה). (3) **`loading="lazy" decoding="async"`** נוסף לכל `<img>` חוץ מהראשון בדף (שמקבל `fetchpriority="high"` כי הוא ה-LCP). (4) **`width="N" height="N"`** הוזרק ל-102 imgs מהדימנשנים האמיתיים של הקובץ (`sips -g pixelWidth/pixelHeight`; SVG מ-`viewBox`). מונע CLS. (5) **`reel.mp4` נדחס** מ-18MB ל-11MB עם `ffmpeg -crf 30 -preset slower -vf fps=24 -an`. דפי-יחיד שמייצרים imgs ב-JS (artist.html, exhibition.html, opencall.html, ktuba artists strip) דורשים תיקון נפרד בקוד ה-JS — לא מכוסה ב-static rewrite.
- **2026-05-14 — `exhibition.html` artist strip href depth:** קישורי אומן רונדרו ב-JS עם `../artists/<slug>.html`. הקובץ נמצא ב-`pages/exhibition.html` (אותה רמת תיקייה כמו `works.html`), אז `..` יוצא לשורש האתר → בקשה ל-`/artists/...` שלא קיים (404). **תיקון:** `artists/<slug>.html` בלבד. דפים תחת `pages/press/` או `pages/events/` נשארים עם `../artists/` — זה נכון. ראה `docs/artist-linking.md` טבלת נתיבים.
- **2026-06-10 — שכבות מוסתרות ב-Figma מזהמות את ה-MCP data:** `mcp__figma__get_figma_data` מחזיר גם text layers עם `visible:false` **בלי לציין שהם מוסתרים**. כך נשאב בולרפלייט "60x80 ס״מ\nשמן על קנבס" (leftover מ-copy-paste של המעצב מכרטיס ג׳סיקה טברובסקי) ל-27 כרטיסים ב-`works.json` שבפיגמה לא מציגים אותו בכלל, וכותרת "התנערות" זלגה ל-holy-kadosh-3. **חוק: טקסט מה-data לא שווה טקסט גלוי.** לפני שמייחסים meta (פרטים/כותרת/sold) מכרטיס Figma — לרנדר את הכרטיס כ-PNG (`download_figma_images` עם nodeId בלי imageRef) ולוודא שהטקסט מופיע ויזואלית. אימות סיטונאי: ספירת "קבוצות שורות" ברצועת הטקסט (min-brightness לכל שורת פיקסלים, רינדור עם `-alpha remove` — רקע שקוף נהפך שחור ב-gray) מול מספר השורות הצפוי מה-JSON; `rendered < expected` = יש שדה מוסתר.
- **2026-06-10 — אימות סדר תמונות = ברמת הפיקסלים, לא רק טקסטים:** בגריד works התאמת artist+title לכל מיקום עברה 1:1, אבל בפועל המעצב סידר מחדש תמונות בין כרטיסים חסרי-כותרת (tal-nehoray, gal-polk, adi-duak, la-raz-porta) — טקסט זהה, תמונה שונה. **שיטה:** להוריד את כל ה-imageRefs הייחודיים, perceptual hash (24×24 grayscale, RMSE) מול הקבצים המקומיים לכל מיקום, ו-cross-match של חריגים מול כל הקבצים. ⚠️ קבצים מקומיים שעברו עיבוד (מסגור-מחדש כמו adi-duak) מנפחים RMSE — שם ההכרעה ויזואלית (montage). נמצאו גם **כפילויות תמונה בפיגמה עצמו** (אותו imageRef/פיקסלים על כרטיסים של אמנים שונים: jessica=anat-4, noemi-4=risa, noemi-6=alon-1, NGL-2≈alon-1, tanya crop2/aura1=aura2, sami 64=66) — טעויות מעצב; באתר נשארו התמונות הנבדלות, לדווח למעצב.
- **2026-06-10 — טעינה מדורגת לגרידים גדולים (works):** 102 כרטיסים לא נכנסים ל-DOM במכה: צ'אנק ראשון 12, אחר-כך 18 דרך IntersectionObserver על sentinel אחרי הגריד (`rootMargin:1800px`). ⚠️ חובה re-arm (`unobserve`+`observe`) אחרי כל צ'אנק — אם הסנטינל נשאר בתוך הטווח אין אירוע חדש והטעינה נתקעת. אחרי כל צ'אנק: `ArtworkLightbox.refreshFocusable()` (+ `PictureUpgrade.refresh`, אם כי ה-MutationObserver שלו תופס לבד). fallback בלי IO = רינדור מלא.
- **2026-06-11 — works: דסקטופ ומובייל בפיגמה עם סדר גריד שונה:** הגריד החדש של המובייל (`542:1390` בתוך `542:1365`) שונה בסדר היצירות מהדסקטופ (`542:500`) ב-63 מתוך 109 מיקומים. לאתר יש data אחד → **הדסקטופ הוא הקנוני לסדר**. כשבודקים "סדר 1:1 מול פיגמה" — לציין מול איזה frame.
- **2026-06-11 — סיווג שדות כרטיס לפי textStyle:** במקום לנחש לפי מיקום הטקסט ברשימה, ה-YAML נותן `textStyle` לכל node: סגנונות אמן (`style_WTRO46` ועוד 3), כותרת (5 סגנונות), פרטים (2), "נמכר" (`style_X47TJF`), גלריה (`style_RXVLGK`). מיפוי לפי סגנון = שיוך שדות דטרמיניסטי. ה-IDs של הסגנונות משתנים בין fetches — לזהות מחדש לפי דוגמאות בכל ריצה.
- **2026-06-11 — node-id binding לעדכוני גריד:** בעדכון חוזר של גריד קיים, למפות כרטיס→רשומה לפי ה-node-id (יציב בין גרסאות) במקום טקסטים/תמונות: סדר חדש נקלט אוטומטית, כרטיסים חדשים = nodes לא ממופים, תמונה שהוחלפה = imageRef שונה על אותו node. כך עדכון 102→109 ירד לדיף של 11 שינויים אמיתיים בלי לבנות הכל מחדש.
- **2026-07-15 — טקסט מעגלי (TEXT_PATH) ו-`textLength` בכרום:** סקשן `#big-news` בהומפייג' דרש טבעת "BIG"×6 סביב "news". `textLength`+`lengthAdjust` על `<text>` שמכיל `<textPath>` **לא נאכף בכרום** — המילים מצטופפות והסוף חופף להתחלה. הפתרון: **`<textPath>` נפרד לכל מילה** עם `startOffset` באחוזים שווים (0/16.666/33.333/50/66.666/83.333%) על אותו `<path>` מעגלי. כיול אופטי: ה-Copperplate שלנו רחב מזה שבפיגמה — "news" 55.7px בפיגמה ⇒ 56px אצלנו נותן את אותו רוחב מרונדר (171px); פונט הטבעת 45.9px ⇒ 36px על רדיוס 101 (viewBox 248). תמיד לרנדר את ה-node מפיגמה ולהשוות screenshot מקומי (headless chrome) עד התאמה.
- **2026-07-15 — שני לינקים מהמשתמש = לא בהכרח אותו סקשן:** המשתמש שלח "דסקטופ+מובייל" של "סקשן חדש", אבל בפועל `1124:240` היה מובייל של **news** ו-`1124:1030` דסקטופ של **perfume promo** (שכבר בנוי). ה-frames המשלימים אותרו ע"י `depth:2` על ה-parents (`homepage-desktop 1124:754` / `homepage-mobile 1124:21`) + רינדור PNG של המועמדים. **חוק:** לפני בנייה — לרנדר את שני ה-nodes ולוודא שהם אותו סקשן; אם לא, לחפש את בני-הזוג בתוך ה-frame ההורה, ולבדוק אם הסקשן כבר קיים באתר.
- **2026-07-26 — ⚠️ `tools/seo/inject.py` גורף = רגרסיה; להריץ רק על הדף החדש:** בבניית `events/anat-wegier/` הרצת `python3 tools/seo/inject.py` (על כל 217 הדפים) שינתה 74 דפים — רובם **רגרסיות מול ה-state המקומיט**: (1) `tools/seo/og-dims.json` המצוי בריפו **חלקי** (160 רשומות מול 209 קבצי `og/*.jpg`) — הרשומות החסרות חיו רק ב-`/tmp/og_dims.json` שנמחק מאז, אז דפים כמו aharon-bas איבדו `og:image` JPG וקיבלו fallback ל-webp; (2) `breadcrumb()` ב-inject.py **לא מסנן** עמודי אינדקס שלא נבנו (`/press/`, `/curators/`) — ה-audit של 2026-07-09 הסיר אותם מה-HTML אבל לא מהסקריפט. **מה עשיתי:** `git checkout` לכל הדפים שלא קשורים למשימה, וליטוש ידני של בלוק ה-SEO בדף החדש לפי תקדים ה-HTML המקומיט (alice-debellis) — breadcrumb של הדף בלבד, תיאור מנוסח, JSON-LD עם שם אנגלי + `startDate`/`endDate` עם שעה. **חוק עד שיתוקן:** לדף בודד — לתת ל-inject לרוץ, לשחזר את שאר הדפים מ-git, ולהצמיד את הבלוק לתקדים; או לתקן קודם את inject.py (להשלים og-dims מ-`og/` בפועל + לסנן crumbs לפי קיום `index.html`). ראה `docs/todo.md`.
- **2026-07-26 — Audit רוחבי (לינקים/מירורים/קונסול) + שני באגים שנמצאו:** סריקה של 216 דפים (4,769 הפניות פנימיות, עוגני `#id`, מירורי inline מול `data/*.json`, ו-48 דפי מפתח ב-headless chrome מעל HTTP מקומי — אפס שגיאות קונסול). נמצאו ותוקנו: (1) **חשיפת עמוד מ-`_staging` חייבת לעדכן גם `canonical`/`og:url`/JSON-LD** — שלושת דפי tali-zelnik נחשפו (46e5709) אבל עוד הצביעו על `art.zrp.co.il/_staging/...`; ה-checklist של reveal ב-CLAUDE.md לא כלל את זה. (2) **sitemap.xml פיגר ב-37 דפים** (the-peeler, 5 דפי אומנים, 2 כתבות, 29 עמודי יצירה) — דפים חדשים נוספו ל-HTML אבל לא ל-sitemap. **חוק:** כל דף חדש / חשיפה = לעדכן sitemap.xml באותו קומיט, ולגרפ `_staging` אחרי כל mv. (הערה: `13tv.co.il` מחזיר 403 ל-curl — הגנת בוטים, הלינק תקין בדפדפן.)
- **2026-08-04 — התנגשות שם מחלקה עם ה-CSS הגלובלי ש-site-chrome מזריק (`.logo`):** בעמוד `sponsors/soos/` ה-SVG של לוגו soos קיבל `class="logo"` — אבל `site-chrome.js` מזריק בזמן ריצה את `site-chrome.css`, שמגדיר `.logo` גלובלי ללוגו ההדר (`display:flex` + כללי מובייל) — והלוגו בעמוד נדחף לפינת התיבה במקום להתמרכז (מובייל בלבד). מלכודת האבחון: ההזרקה קורית **אחרי** `load`, אז בדיקת computed styles ב-`onload` מראה הכל תקין; רק הפיינט הסופי שבור. זוהה עם debug-copy עם `outline` על האלמנטים. **תיקון:** שם ייחודי `soos-logo`. **חוק:** בדף חדש, לפני בחירת שם מחלקה גנרי (`logo`, `nav`, `menu`, `toast`…) — `grep "\.<name>[{,. ]" components/site-chrome.css components/site-boot.css`; עדיף prefix ייחודי לדף.
- **2026-08-12 — מקור עם פסים שחורים (screenshot) + שני קרופים: לאפות את חלון ה-cover של הדסקטופ, לא את התמונה המלאה:** ההירו של `events/hadas-tuval/` הגיע כ-imageRef 945×2048 שהוא **צילום מסך מלוטרבוקס** — 216px שחור למעלה ו-218 למטה. Figma מחשב `scaleMode:FILL` על התמונה **כולל הפסים**, ולכן החלון הנראה בדסקטופ (492×744) הוא y 309..1738 במקור, ובמובייל (390×520) y 394..1654 — שני קרופים שונים, שניהם ממורכזים ושניהם מחוץ לפסים. **הפתרון (במקום `<picture>` עם שני קבצים):** לאפות בדיוק את חלון הדסקטופ (הרחב מבין השניים) → 984×1488, ואז `object-fit:cover` בדפדפן חותך ממנו את המובייל **בדיוק** לאותו y (החישוב מתכנס כי שני החלונות ממורכזים על אותו ציר). אימות: ממוצע אפור הכרטיס אחרי בנייה 83.5 מול 83.6 ברינדור הפיגמה בדסקטופ, ו-84.3 מול 85.3 במובייל, band-by-band. **חוק:** לפני אפיית hero — לזהות פסים (`row mean < 8`), ולבדוק אם חלון-המובייל מוכל בחלון-הדסקטופ; אם כן — קובץ אחד מספיק (השווה: `noemi-safir`, ששם המובייל **רחב** מהדסקטופ ולכן חייב `<picture>` ידני).
- **2026-08-12 — סדר ה-fills ב-`get_figma_data`: אינדקס 0 = השכבה העליונה:** בכרטיס האומנית של הדס טובל היו שני IMAGE fills — `2249c4b3` (הדס) ואחריו `1214412249` (הפורטרט של נעמי ספיר, leftover מהתבנית שהמעצבת שכפלה). הרינדור של ה-node הראה את **הראשון**. אותו דבר עם ה-scrim: `["rgba(27,27,27,0.7)", IMAGE]` — הצבע ראשון ברשימה **ומצויר מעל** התמונה (אלפא נגזרת 0.698 מתוך ההשוואה בין ה-imageRef הגולמי לרינדור). **חוק:** אל תניח סדר; לרנדר את ה-node ולגזור את האלפא אריתמטית (`a=(base-render)/(base-27)`) על שתי רצועות שונות — אם שתיהן נותנות את אותו מספר, זה scrim אמיתי ולא הבדל דחיסה.

- **2026-08-18 — גם node-id אינו מצביע יציב: המעצבת משכתבת פריימים in-place:** הפריימים של עמוד soos (`XhGH…::1318:3287`/`1318:3480`) שוכתבו בפיגמה לעיצוב חדש לגמרי — **sumii** (שם הפריים השתנה ל-"sumii - desktop/mobile", כל התוכן הוחלף). מי שסומך על ה-node-id המתועד ב-CLAUDE.md/FIGMA_LINKS היה "מסנכרן" את עמוד soos לעיצוב של עמוד אחר. זה מדרגה מעבר ללקח 2026-05-11 (frames שנודדים בין קבצים): כאן ה-id נשאר, ה**זהות** התחלפה. **חוק: לפני עבודה על node שכבר ממופה לעמוד קיים — לרנדר/לקרוא את שם הפריים והתוכן ולוודא שזה עדיין אותו עמוד.** אם שוכתב: זה עמוד חדש; לעמוד הישן כבר אין מקור חי בפיגמה (לתעד את זה ברשומה שלו).
- **2026-08-04 — צילום מובייל 390 ב-headless chrome: מלכודת רוחב-חלון מינימלי:** `--headless=new --window-size=390,...` **לא באמת נותן viewport של 390** — לכרום יש רוחב חלון מינימלי (~500px), אז הדף מרונדר ברוחב ~500 והצילום נחתך ל-390 ⇒ הכל נראה "גולש מימין" (כולל ההדר). זה הטעה לחשוב שיש overflow בדף חדש (`the-space-between`) — עד שצילום ביקורת של דף פרודקשן תקין (anat-wegier) נחתך בדיוק אותו דבר. `--force-device-scale-factor` לא עוזר (מכפיל את ה-CSS viewport, לא מקטין). **הפתרון:** עמוד עוטף עם `<iframe width=390>` שמצביע על הדף (query param), צילום העוטף ברוחב 800 + `--allow-file-access-from-files` (בשביל iframe של file://‎), ואז crop ל-390. ה-iframe מקבל viewport אמיתי של 390 וה-media queries מוחלות נכון. **חוק:** צילום מובייל שנראה חתוך אחיד מימין = קודם לצלם דף ביקורת תקין באותם דגלים, רק אז לחשוד בדף.
